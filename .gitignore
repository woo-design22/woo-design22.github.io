# OS / editor cruft
Thumbs.db
Desktop.ini
.DS_Store
.vscode/
.idea/

# 안드로이드 빌드 산출물 (build.ps1 이 매번 다시 만든다)
android-particle/build/
android-particle/dist/
android-particle/assets/
# 서명 키는 저장소에 넣지 않는다. 없으면 build.ps1 이 새로 만든다.
android-particle/debug.keystore

# 샌드믹스 / 마음결 안드로이드 빌드 산출물
android-sandmix/build/
android-sandmix/dist/
android-sandmix/assets/
android-sandmix/debug.keystore
android-counsel/build/
android-counsel/dist/
android-counsel/assets/
android-counsel/debug.keystore

# soccer 서버 (2단계) — 의존성·배포 설정·인증서는 커밋하지 않는다
soccer/server/node_modules/
soccer/server/deploy.local.json
*.pem

# event-price 수집기 실행 로그 (build_data.py 가 매번 다시 만든다)
event-price/data/build.log

# 사업 기획·시장 조사·수익화 문서는 공개 저장소에 올리지 않는다.
# (docs/custom-game-business.md 에 신분·세무 내용이 있어 특히 그렇다)
# 기술 문서를 공개하고 싶으면 아래처럼 개별 예외를 준다:
#   !docs/dot-rpg-engine.md
docs/
