# DECISIONS — 판단하고 넘어간 것들

사양서의 `[미정]`을 결정한 기록, 그리고 `[확정]`을 구현하면서 **덧붙이거나 해석한** 부분.
사양서와 이 문서가 어긋나면 사양서가 우선이고, 그런 자리를 발견하면 여기에 적는다.

작성 시작 2026-09-04.

---

## D-01. 폴더 이름은 `seat-map` `[미정→결정]`

작업 공간(`C:\Claude`)의 다른 앱들이 `shade-map`·`mood-log`처럼 영문 두 낱말이라 맞췄다.
같은 결의 서울 지도 기반 생활 정보 서비스인 `shade-map`(그늘 지도)과 짝이 된다.
서비스명 「앉을 자리」는 화면에만 쓴다. 아직 아무 데도 링크되지 않았으므로 바꾸려면 지금이 싸다.

## D-02. 문서 체계는 작업 공간 관례를 따른다 `[미정→결정]`

사양서는 `docs/DECISIONS.md`·`docs/DATA_SOURCES.md` 둘만 요구했다. 작업 공간에는 이미
다중 파일 프로젝트용 감독 문서 체계(`CLAUDE.md` → `ROADMAP.md` → `HANDOFF.md`)가 있어
`voxel-world`·`soccer`·`event-price`가 쓰고 있다. 둘을 겹쳐 쓴다.

- `CLAUDE.md` 설계도(변하지 않는 규칙) · `ROADMAP.md` 단계 · `HANDOFF.md` 일지
- `docs/DECISIONS.md` 판단 기록 · `docs/DATA_SOURCES.md` 데이터 출처와 키 상태

## D-03. 경로 탐색은 BFS로 시작한다 `[미정→결정]` (사양서 6.4)

RAPTOR·CSA는 **지금 쓰지 않는다.** 근거:

- 범위가 서울 하나이고 환승 2회 제한이다. 정류장 11,000 · 노선 600 규모에서
  RAPTOR의 이점(라운드 기반 다중 기준 최적화)이 살아나는 크기가 아니다.
- 사양서 2.3이 **오프라인 동작**을 못박았다. RAPTOR는 시간표 전량을 앞에 놓고 시작하는데,
  그러면 사양서 3.4의 「필요한 노선만 지연 로딩」과 정면으로 부딪힌다.
- 정렬 1순위가 소요시간이 아니라 **서서 가는 시간**이다. 도착시각 최적화를 전제로 짜인
  RAPTOR의 가지치기를 그대로 못 쓴다.

**M2-2를 BFS로 만든 뒤 실측한다.** 후보 경로 생성이 모바일에서 1.5초를 넘으면 그때 다시 본다.
그 측정값을 여기에 적는다.

## D-04. 지도는 라이브러리를 저장소에 담아 쓴다 `[미정→결정]` (사양서 7.6)

MapLibre GL JS를 쓰되 **CDN에서 불러오지 않고 파일을 저장소에 둔다.** 사양서 2.3의 오프라인·APK
전환 요구와, 이 작업 공간의 「CDN 의존성 없음」 규칙이 같은 답을 가리킨다.
타일은 VWorld(국토부, 무료)를 1순위로 본다 — 국내 좌표계·주소와 어긋나지 않는다.
**타일이 안 뜨는 상황(오프라인·차단)에서도 경로는 읽을 수 있어야 한다** — 지도 없이
구간 목록과 좌석 배치도만으로 화면이 성립하게 만든다. 확정은 M3에서.

## D-05. 폴백 시뮬레이션은 노선 단위다 (역별 차이 없음)

`engine/sim-seoul.js`는 호선별 피크 혼잡도와 시간대 곡선만 갖는다. 역별 차이는 넣지 않았다.
없는 정보를 그럴듯하게 지어내면 실데이터가 들어왔을 때 **무엇이 바뀌었는지 알 수 없게 된다.**
「6호선 월곡 08:00」은 지금 「6호선 08:00」과 같은 값이다. 검증 테스트가 SIM/REAL 모드를 찍는 이유다.

## D-06. `pSeated`를 새로 만들었다 (사양서 5.1·5.2 해석)

사양서는 `P_board`(승차 즉시)와 `P_trip`(가는 도중)을 따로 정의한다. 그런데 사용자가 묻는 것은
**"이 경로에서 앉을 수 있나"** 하나다. 그래서 셋을 다 돌려준다.

```
pBoard  = 탈 때 바로 앉을 확률
pDuring = 못 앉고 탔을 때 가는 도중 앉게 될 확률 (사양서의 P_trip)
pSeated = 1 - (1-pBoard)·∏(1-q)      ← 화면에 쓰는 값
```

식을 바꾼 것이 아니라 합친 것이다. `pDuring`은 사양서 정의 그대로 남겨 두었다.

## D-07. 광역버스 잔여 0석에서 확률이 0이 아니라 0.30인 것은 의도다

사양서 5.1의 식 `1/(1+exp(-(잔여좌석-1)/1.2))`을 그대로 쓰면 잔여 0석에서 0.303이 나온다.
0이 되지 않는 이유는 **잔여좌석 자체가 예측치**이기 때문이다(도착정보 API가 주는 값이고,
정류장 두어 개 사이에 사람이 내린다). 「확실히 못 탄다」로 잘라 말하려면 식이 아니라
실시간 만차 판정을 따로 둬야 한다. 지금은 사양서를 그대로 따른다.

## D-08. 5분 보간은 Catmull-Rom + 이웃값 가두기 (사양서 3.3)

부드럽게 잇되 **보간값이 이웃한 두 원천값 사이를 절대 벗어나지 않게** 가둔다.
매끄러움을 위해 오버슛을 허용하면 원천에 없는 최댓값이 생기고, 그건 "없는 정보를 만들어낸 것"이다.
단위 테스트가 1분 간격 전 구간에서 이 성질을 검사한다.
화면에는 `SeatInterp.noteFor()`의 문구(「30분 단위 통계를 부드럽게 이은 예상값입니다」)를 반드시 붙인다.

## D-09. 서울 열린데이터광장을 먼저 붙인다 `[제안→결정]`

사양서 M1-2는 「역별승하차인원 일일 수집」이 1순위인데, 그 API(공공데이터포털)는 **활용신청 승인에
며칠 걸린다.** 그동안 못 받은 날짜는 영영 못 받는다 — 우선순위를 정한 이유 자체와 어긋난다.
그래서 **인증키가 즉시 발급되는 서울 열린데이터광장을 1순위로 붙이고**, 공공데이터포털은
승인되는 대로 같은 크론에 더한다. 수집기는 두 소스를 모두 시도하고 **하나라도 성공하면 그 날은 확보**로 본다.

## D-10. 요일 구분에 공휴일 표가 필요하다 (사양서 3.2·4.2)

「공휴일은 일요일 프로파일」이라면 어느 날이 공휴일인지 알아야 한다. 양력 고정 공휴일 8개는
코드에 넣었지만 **설·추석·부처님오신날·대체공휴일은 계산으로 못 구한다.**
공공데이터포털 특일정보 API로 받아 `data/holidays.json`에 채운다
(`collect_subway_daily.py --holidays 2026`). 표가 비어 있어도 파이프라인은 돌아가되,
그 경우 명절이 평일로 섞인다 — 집계 전에 반드시 채울 것.

## D-11. OD 배분에서 종점 승차는 0으로 잘라내고, 잘라낸 양을 보고한다

폴백 시뮬레이션의 첫 판에서 **종점에서 탄 사람이 조용히 사라져** 「탄 사람 ≠ 내린 사람」이 됐다
(오차 4e-5, 단위 테스트가 잡았다). 인원 보존이 OD 방식을 쓰는 유일한 이유이므로 그냥 두면 안 된다.
종점 승차는 갈 데가 없으니 0으로 못 박고 `droppedBoardings`로 얼마를 뺐는지 함께 돌려준다.

## D-12. 운행 0회인데 재차인원이 있는 칸은 0으로 정리한다

`a18Num00h`는 그 시간대 **운행의 평균**이므로 운행횟수가 0이면 값이 있을 수 없다.
그냥 두면 「새벽 3시에도 사람이 타 있다」가 그대로 화면에 나간다.
0으로 정리하되 **몇 칸을 정리했는지 세어 로그에 찍는다** — 이 숫자가 크면 스키마 해석이 틀린 것이다.

## D-13. 배포본은 우선 JSON, 바이너리 샤드는 M1-5로 미룬다 `[제안→결정]`

사양서 3.4의 계산(41MB → 노선 단위 샤딩)은 맞다. 다만 **화면이 실제로 무엇을 읽는지 확정되기 전에**
uint8로 굳히면 필드를 하나 더할 때마다 포맷을 다시 짜야 한다. 지금은 노선당 JSON으로 내고,
M3에서 화면이 읽는 필드가 확정된 뒤 같은 내용을 바이너리로 굽는다. 노선 단위 샤딩 자체는 지금부터 지킨다.

## D-14. 요일별 집계의 분산은 「날짜 안」 + 「날짜 사이」를 더한다

같은 화요일을 8주 모을 때 편차는 두 갈래다 — 그날 다닌 차들 사이의 편차(평균·최대·운행횟수로 역산)와,
주마다 다른 편차. 앞의 것만 쓰면 「매주 화요일은 늘 같다」고 우기는 셈이 된다.
운행횟수를 가중치로 두 성분을 합산한다(`within + between`).

## D-15. 키는 저장소에 절대 넣지 않는다

`pipeline/keys.json`은 `.gitignore`에 있다. 환경변수도 받는다.
로그·예외 메시지에 URL을 찍을 때 `mask_url()`을 통과시킨다 — 키가 경로나 쿼리에 박히는 API가 많아
그냥 찍으면 그대로 샌다. 저장소가 공개이므로 **한 번 커밋되면 지워도 히스토리에 남는다.**

## D-17. 인증키 없이 받을 수 있는 「파일 데이터」를 먼저 쓴다 ★

**D-09를 한 걸음 더 밀어붙인 결과이고, 이 프로젝트에서 가장 값싼 진전이었다.**

열린데이터광장은 데이터셋마다 OpenAPI(인증키 필요)와 **파일(인증키·로그인 불필요)**을 따로 준다.
파일 쪽은 POST 한 번이면 받아진다. 즉 **키를 기다리는 동안에도 실측으로 일할 수 있다.**

```
POST https://datafile.seoul.go.kr/bigfile/iot/inf/nio_download.do?&useCache=false
     infId=OA-12928&seq=<파일번호>&infSeq=1
```

받은 것(2026-09-04):

| 데이터 | infId | 비고 |
|---|---|---|
| 서울교통공사_지하철혼잡도정보 | OA-12928 | 2021~2026-06 분기별 12개가 있다. 최신 1,671행을 씀 |
| 서울시 버스정류소 위치정보 | OA-15067 | 2026-09-02판, 좌표 포함 |

이 덕분에 검증이 **[SIM] → [REAL]** 로 바뀌었다. 남은 키 의존은 T-DATA(분산)와 실시간뿐이다.

**파일번호(seq)는 분기마다 늘어난다.** 표를 손으로 적어 두면 낡으므로
`fetch_open_files.py --list OA-12928` 로 현재 목록을 뽑아 갱신한다.

## D-18. 원천 파일의 모양이 분기마다 바뀐다 — 파서는 반드시 소리 내고 죽어야 한다 ★

같은 데이터셋인데 실측으로 이렇게 달랐다:

| 분기 | 형식 | 요일 컬럼 | 역 컬럼 | 시각 표기 | 요일 구분 위치 |
|---|---|---|---|---|---|
| 2025-11 | csv (cp949) | `요일구분` | `출발역` | `5시30분` | 한 장에 다 |
| 2026-03·06 | xlsx | `구분` | `역명` | `05:30~06:00` | **장(sheet) 3개로 분리** |

마지막 칸이 특히 위험했다. **첫 장만 읽고도 프로그램은 멀쩡히 돌았다** — 1,671행 중 557행만
들어온 채 "평일 데이터"로 보였고, 주말 통계가 통째로 사라졌는데 아무 오류도 안 났다.
원천 행 수(포털이 표시하는 1,671)와 대조해서야 알았다.

그래서 두 가지를 규칙으로 둔다.
1. **컬럼을 못 찾으면 0으로 채우지 말고 그 자리에서 멈춘다.** 실제로 xlsx 머리글이 다를 때
   `컬럼을 못 찾았다: 요일구분(요일구분/구분 중 하나)` 로 정확히 멈췄고, 그래서 바로 고쳤다.
2. **행 수를 원천 표시와 대조한다.** 조용한 절반 손실은 오류보다 훨씬 오래 안 들킨다.

## D-19. 「호선 피크 혼잡도」 = 전 역·전 방향 최댓값, 기대치는 실측 기준 `[결정]` ★

실측을 넣자 사양서 10장의 세 줄이 어긋났다. **모델 결함이 아니라 정의가 안 정해진 것이다.**

2026-06-30 실측(서울교통공사 공식):

| 호선 | 전 역·전 방향 최댓값 | 그 지점 | 역별 최댓값의 중앙값 | 90분위 |
|---|---|---|---|---|
| 5호선 | **133.3%** | 길동 하선 | 83.4% | 108.0% |
| 6호선 | **110.3%** | 동묘앞 하선 | 66.0% | 95.0% |
| 2호선 | 144.6% | 사당 외선 | 92.2% | 119.7% |

사양서 기대는 5호선 110~130%, 6호선 70~85%. **어느 정의로도 둘 다 맞지 않는다.**
- 최댓값 기준이면 5호선은 3.3%p 초과, 6호선은 25%p 초과
- 중앙값 기준이면 둘 다 크게 미달

참고로 6호선 **월곡**의 최댓값은 84.4%로 사양서 범위 안이다. 처음 만든 테스트가
5호선은 「전체」로, 6호선은 「월곡」으로 물어서 6호선이 **우연히 통과했었다.**
기준을 같게 맞추자 드러났다 — 통과하던 시험이 틀렸던 것이다.

**2026-09-04 사용자 결정: 「실제 숫자에 맞춰」.** 그래서 이렇게 정했다.

- **호선 피크 = 그 호선 전 역·전 방향의 칸별 최댓값.** 가장 보수적이고
  "내가 서게 되나"라는 이 서비스의 질문에 가장 가깝다.
- 기대 범위는 `tests/validate.test.js` 의 `EXPECT` 한 곳에 모아 두고 실측 기준으로 잡았다.
  원천이 갱신되면 이 표를 고치고 왜 달라졌는지 HANDOFF 에 적는다.
  **시험을 통과시키려고 범위를 넓히지 않는다** — 그러면 표가 아무것도 안 지킨다.
- 정의가 바뀌어도 뒤집히면 안 되는 것은 따로 시험한다(6호선이 5호선보다 한산하다).

## D-20. 「빈 좌석 20석 이상」은 방향을 밝혀야 한다 `[미정]`

6호선 월곡 평일 06:00 실측: **상선 24.8%(빈 14석) / 하선 10.8%(빈 37석)**.
사양서는 방향을 밝히지 않았고, 기본 키는 보수적으로 **더 붐비는 쪽**을 쓴다. 그래서 14석이 나와
「20석 이상」에 못 미친다. 같은 조건의 **착석 확률은 91.8%로 90% 기준을 통과**한다.

착석 확률 쪽이 사양서 5.4의 취지("확률보다 시간이 와닿는다")에 더 가깝고 방향에 덜 민감하다.
빈 좌석 조건을 유지하려면 방향을 못 박아야 한다.

## D-21. 필터는 축을 살려 저장하고, 조합은 브라우저에서 한다 ★

사용자 요구(2026-09-04): 역별·노선별·버스정류장별·버스노선별·시간별·10분단위·평일/주말별로
**각각 따로** 걸러져야 한다.

**합쳐서 평균 낸 값은 축을 영영 못 되살린다.** 그래서 저장은 축이 살아 있는 채로 하고,
조합은 `engine/query.js` 가 브라우저에서 한다(사양서 2.3 의 오프라인 요구와도 맞는다).

- 10분 값은 저장하지 않는다. 원천(30분·1시간) 그대로 두고 `interp.js` 가 만든다(D-08).
  **승하차는 합계량이라 10분 눈금에서 `perStep` 을 따로 준다** — 시간당 값을 그대로 쓰면 6배로 부풀어 보인다.
- 버스는 노선 단위 파일로 쪼갠다(666개, 노선당 약 19KB). 검색 한 번에 필요한 노선만 받는다(사양서 3.4).
- 정류장 12,548개는 목록으로 못 고른다 → 이름·ARS 번호 검색(`findStops`).
  노선 검색은 **번호가 앞에서 맞는 것을 먼저** 준다(「6」을 치면 6번·600번을 기대한다).

**버스에는 요일 축이 없다**(원천이 월 집계). 화면이 요일을 눌러도 같은 값이 나오므로
`noDayType` 문구를 반드시 함께 띄운다. 조용히 두면 사용자가 속는다.

## D-22. 원천마다 역 이름이 다르다 — 부역명을 떼서 맞춘다 ★

혼잡도(OA-12928)는 「월곡」, 승하차(OA-12921)는 「월곡(동덕여대)」로 적는다.
**사양서 6.2-② 가 경고한 그 버그가 우리 데이터 안에서 그대로 났다** — 두 자료가 안 이어졌다.
괄호 안 부역명을 떼서 키로 쓰고(역 347개 → 280개로 합쳐짐), 보여줄 이름은 `stationNames` 에 남긴다.

## D-23. 길찾기의 세 재료를 인증키 없이 다 구했다 ★

**이 서비스의 본체는 길찾기다**(2026-09-04 사용자 지시). 길찾기에는 셋이 필요하다.

| 필요한 것 | 어디서 | 확인 |
|---|---|---|
| 노선의 **경유 순번** | 서울시 버스노선별 정류소 정보 **OA-1095** | 41,688행, 순번·NODE_ID·ARS·이름·좌표가 한 줄에 |
| 정류장 **이름과 좌표** | 같은 파일 | 좌표 100% (앞서 OA-15067 로는 86%였다) |
| 지하철 역 **순서** | 혼잡도 파일의 **역번호 오름차순** | 실측 확인: 응암→역촌→불광→…→봉화산→신내 |

**버스는 왕복이 순번 하나로 들어 있다**(147번 = 1~115, 기점→회차→기점).
그래서 「순번 i < j」이면 그것이 곧 방향이다 — 방향을 따로 관리하지 않으므로
사양서 6.2-④(방향 라벨 뒤집힘)가 구조적으로 불가능해진다. 지하철만 역번호 오름/내림 두 줄.

**지하철역 좌표만 무키 파일이 없다.** 역사마스터(OA-21232)·노선별역정보(OA-15442)는 API 전용이고
T-DATA 좌표 파일은 로그인이 필요하다. 그래서 **「◯◯역」 버스정류장의 좌표를 빌렸다** —
출입구 앞이라 100m 안쪽이고 어차피 150m 로 한 노드가 된다. 264/277역이 붙었고,
못 붙은 13역은 대부분 서울 밖(5호선 하남 구간)이라 서울 버스가 안 간다.

**9000번대 역번호는 뺐다** — 지선·순환 표시(응암S·성수E)라 본선 순서에 섞이면 노선이 엉킨다.
2호선 성수·신정지선과 6호선 응암순환은 아직 없다.

## D-24. 환승 노드는 union-find 로 묶으면 안 된다 ★

「150m 안이면 잇는다」를 union-find 로 했더니 **연쇄 병합**이 일어났다 —
A~B 150m, B~C 150m 면 A~C 가 300m 라도 한 덩어리다.
정류장이 100m 간격으로 늘어선 큰길이 통째로 노드 하나가 됐다(13,161점 → **3,611개**).
그러면 노선이 같은 노드를 몇 번씩 지나 경로 계산이 무너진다.

**대표점 기준(greedy)** 으로 바꾸고 붙일 조건을 하나 더 걸었다:

> 거리 ≤ 150m **그리고** (종류가 다르거나 · **정식 이름이 똑같거나**)

환승 노드의 목적이 그 둘이기 때문이다 — ① 지하철역과 그 앞 정류장 ② 같은 이름의 양방향 정류장.
결과 8,886개, 버스+지하철이 함께인 노드 221개.

**같은 종류끼리는 정규화 이름이 아니라 정식 이름을 본다.** 정규화로 묶었더니
4호선 「미아」와 「미아사거리」가 한 역이 됐다(「사거리」를 떼기 때문). 붙어 있는 두 역이
하나로 뭉개지면 노선이 같은 노드를 연달아 지난다 — 테스트가 그 2곳을 잡았다.

## D-25. 「자료가 없으면 유리해지는」 구조를 없앴다 ★★

**이 프로젝트에서 가장 위험했던 버그다.**

첫 길찾기 결과에서 **오전 8시에 심야버스 N51 이 1순위**로 올라왔다.
그 시각 승하차가 전부 0이라 「23자리가 모두 비어 있습니다 · 서서 0분」으로 계산됐기 때문이다.
승객이 0명인 것은 **텅 비어 앉을 수 있다는 뜻이 아니라 안 다닌다는 뜻**이다.

두 가지를 규칙으로 못 박았다.

1. **승객 0명 = 안 다님.** `loads.js` 가 `notRunning` 을 돌려주고, `rank()` 가 그 경로를 뺀다.
   지하철도 혼잡도 0 이면 같다(첫차 전·막차 후).
2. **모르는 구간은 서서 간다고 본다.** 처음엔 「절반만 센다」로 뒀는데, 그래도
   자료가 없는 노선이 정렬에서 유리했다. **모르는 것을 유리하게 세지 않는 것**이 정직함이다.

고친 뒤 결과가 상식과 맞았다 — 06:00 서서 0분 / 08:00 서서 14분, 1순위는 6호선→2호선 31분.

## D-26. 이름 검색은 「들어 있으면 맞다」로 두면 안 된다

「월곡」을 치면 **상월곡역**이, 「을지로4가」를 치면 **방산시장.을지로4가역6번출구**(버스정류장)가
먼저 나왔다. 실제로 그렇게 골라져서 엉뚱한 곳에서 길을 찾았다.
점수를 매긴다 — 정확히 같은 이름 → 「역」만 뺀 이름 → 앞에서 시작 → 그냥 포함.
같은 점수면 지하철역을, 그 다음엔 이름이 짧은 쪽을 먼저 준다.

## D-27. 「차 한 대에 몇 명인가」는 지금 추정값이다 — 반드시 밝힌다

원천은 전부 **역·정류장 단위 총량**(시간당 몇 명)인데 착석 확률에 필요한 것은 **차 한 대 안의 인원**이다.
그 사이를 「시간당 몇 대가 다니는가」로 나눠 건너는데, 그 대수가 지금은 추정이다
(지하철 열차시간표·T-DATA 운행횟수는 인증키가 있어야 한다).

- 지하철: 출퇴근 20대/시 · 평시 12대/시 · 첫막차 7대/시, 8칸
- 버스: 간선 10 · 지선 8 · 마을 12 · 광역 4 대/시 (피크 ×1.4, 심야 ×0.4)

**혼잡도(%)는 추정이 아니다** — 정원 대비 비율이라 그대로 칸 안 인원이 된다.
추정이 섞인 구간은 `estimated` 와 그 이유를 달고 나오고, 화면이 그대로 띄운다.

## D-28. 출발지·도착지는 주소·지명도 받는다 — 로컬 먼저, 온라인은 보태기 ★

2026-09-04 사용자 지시: 「지하철·버스가 아니라 **지명 및 주소를 모두** 넣을 수 있어야 한다」.

주소 전체를 담은 자료는 **인증키 없이 못 받는다** — 도로명주소 건물DB(juso.go.kr)는 기관 승인이 필요하고,
역사마스터·주소 좌표 API도 모두 키가 있어야 한다. 그런데 사양서 2.3 은 오프라인 동작을 못박았다.
그래서 나눴다.

| | 무엇 | 언제 |
|---|---|---|
| ① 로컬 | 정류장·역 이름 8,886곳 | 즉시, 오프라인, **항상 된다** |
| ② 온라인 | 임의의 주소·건물 (Photon·Nominatim, OSM 계열, 인증키 불필요) | 0.45초 뒤, 인터넷 있을 때만 |

**시간 순서와 화면 차례를 나눠서 본다.**
- **시간**: ①이 먼저 그려진다(즉시). 반대로 하면 인터넷이 느릴 때 앱이 멈춘 것처럼 보이고, 끊기면 아무것도 안 나온다.
- **차례**: 온라인 결과가 도착하면 **②(주소·상호)가 위, ①(정류장·역)이 아래**로 놓인다
  (2026-09-04 추가 지시). 「국립중앙박물관」을 쳤을 때 **「국립중앙박물관.용산가족공원」이라는
  버스정류장**이 먼저 나왔기 때문이다 — 찾는 것은 박물관이지 그 앞 정류장이 아니다.
  같은 곳이 양쪽에 잡히면 자리는 위쪽을 쓰되 알맹이는 로컬 것을 남긴다(좌표가 확실하고
  「지하철역·버스정류장」 부연이 붙는다). 서울 정류장 이름에는 아파트·학교·시장·랜드마크가 이미 잔뜩 들어 있어
(「월계사슴아파트3단지」·「인덕대학교후문입구」·「북서울꿈의숲」) 로컬만으로도 상당히 쓸 만하다.

- **`engine/geocode.js` 는 네트워크를 부르지 않는다** — 주소 만들기·응답 정규화·순위만 한다.
  실제 fetch 는 화면이 한다. 그래야 시험이 인터넷 없이 돈다(실제 응답 표본을 넣어 검사).
- **번지가 있으면 주소를 제목으로 올린다.** 「종암로 128」을 쳤는데 제목이
  「탕화쿵푸 종암사거리점」이면 맞는 좌표인데도 엉뚱한 데를 찾은 것처럼 보인다.
- 서울 밖 결과는 걸러낸다. 이 서비스가 답할 수 없는 곳이다.
- **Photon·Nominatim 은 공용 무료 서버다.** 몰리면 느리거나 막힌다(shade-map 도 같은 처지다).
  글자를 칠 때마다 부르지 않고 0.45초 쉰 뒤 한 번만 부른다. 실패하면 조용히 로컬만 남는다.
  **이용약관상 출처를 화면에 밝혀야 한다** — 바닥에 항상 띄운다.

## D-29. 도보는 직선거리가 아니라 「돌아 걷는 거리」로 잰다 ★

사람은 직선으로 못 걷는다. 실제 걷는 거리 ÷ 직선거리를 **우회계수**라 하고 도시에서 1.2~1.4 다.
`WALK_DETOUR = 1.3` 을 곱한다.

**안 곱하면 도보 시간을 짧게 부른다.** 500m 직선이면 실제 650m 이고 보통 걸음으로 9분인데,
직선으로 계산하면 6.9분이라 2분을 속이는 셈이다.
**어르신·교통약자가 주 사용자인데 도보 시간을 짧게 부르는 것이 가장 나쁘다.**

제대로 하려면 실제 보행 경로(OSRM 같은 것)를 물어야 하고 그건 인터넷이 필요하다.
오프라인에서도 답이 나와야 하므로 기본은 이 계수다. 화면에 「×1.3 을 곱해 잡았습니다」라고 밝힌다.

## D-30. 가까우면 「그냥 걸어가기」도 답이다

직선 2.5km 안이면 도보 전용 경로를 후보에 넣는다. 네 정거장 타려고 두 번 갈아타는 것보다 나을 때가 있다.
서서 가는 시간이 0 이라 정렬 1순위에 오른다 — 걷는 동안 앉아 있는 것은 아니지만,
이 서비스가 재는 것은 **차 안에서 서 있는 시간**이다. 그래서 화면에 「걸어서」라고 분명히 쓴다.

## D-31. 확률은 퍼센트로 말하고, 다섯 단계로 나눈다 ★

2026-09-04 사용자 지시: 「**서서 가실 수 있습니다**가 아니라, 확률로 정확하게 퍼센트로 알려줘야 된다」
그리고 다섯 단계 분류.

세 단계로는 **30% 와 5% 가 같은 말**이 됐다. 그 둘은 전혀 다른 얘기다.

| 확률 | 문구 | 색 |
|---|---|---|
| 80~100% | 웬만하면 앉아 갑니다 | 진한 초록 |
| 60~80% | 앉을 가능성 높습니다 | 초록 |
| 40~60% | 앉을 수도, 설 수도 있습니다 | 노랑 |
| 20~40% | 앉기 힘듭니다 | 주황 |
| 0~20% | 못 앉습니다 | 빨강 |

- 화면에는 **「앉을 확률 92% · 웬만하면 앉아 갑니다」** 처럼 숫자와 문구를 함께 낸다.
  숫자가 글자로 들어가므로 사양서 7.3(색만으로 알리지 말 것)도 그대로 지킨다.
- 60~80% 칸의 사용자 원안은 「앉을 확률 높음」이었는데, 앞에 「앉을 확률 79%」가 붙어
  **말이 겹친다.** 뜻은 같게 두고 「앉을 가능성 높습니다」로 적는다(시험이 겹침을 검사한다).
- **모르는 구간은 숫자를 지어내지 않는다** — 「앉을 확률 알 수 없음」이라고 쓴다.

**여정 전체의 확률은 정의를 못 박아야 한다.** 구간마다 다른 차를 타므로
「앉아 있는 시간 비율」을 퍼센트로 부르면 거짓말이 된다. 그래서
**가는 동안 한 번이라도 앉게 될 확률** = 1 - ∏(1 - 구간별 착석확률) 로 정의하고,
화면에 그 뜻을 한 줄로 적는다.

「서서 24분」과 「앉을 확률 92%」가 한 카드에 있으면 모순처럼 읽힌다 — 둘 다 맞는 말이다
(앉게 되기까지 24분을 서 있는 것이다). 사양서 6.2-⑤ 가 경고한 자리라 목록에도 설명 한 줄을 붙였다.

## D-32. 출발지·도착지는 저장하지 않는다. 걸음 속도는 늘 「보통」에서 시작한다

2026-09-04 사용자 지시. 지난번 자리가 채워져 있으면 그걸 못 보고 그대로 눌러 엉뚱한 길을 받는다.
걸음 속도도 한 번 느리게 골라 둔 것이 몇 달 뒤까지 남아 모든 경로가 길어 보이면 더 나쁘다.
**요일·시각만 남긴다** — 그 둘은 값이 화면에 그대로 찍혀 있어 확인하고 고치기 쉽다.

## D-33. 「앉을 확률」은 **탈 때 바로** 앉을 확률이다 ★★

2026-09-04 사용자 지시: 「앉을 확률이란 그냥 해당역에 탔을 때 **타자마자 앉을 확률**을 말한다.
중간에 가다가 누가 내려서 그 자리에 앉을 확률이 아니라」

앞서는 「가다가라도 앉을 확률」을 앉을 확률이라 불렀다. 그래서 **출근시간 4호선 만원 열차에
34%** 가 찍혔다. 사용자 말대로다 —

> 앉아있는 사람은 34%겠지만 이미 사람들이 서있고 만원이라면 앉을확률은 0%지

만원 열차에 타면 이미 서 있는 사람이 백 명이다. 그들이 자리를 먼저 가져간다.
그래서 화면의 「앉을 확률」은 **P_board**(사양서 5.1) 하나로 못 박는다.
같은 조건에서 34% → **0%** 로 바뀐다.

**가다가 앉게 되는 것은 버리지 않는다.** 두 군데에 살아 있다.
- **서서 가는 시간** — 앉으면 그 뒤로 안 쌓인다. 정렬 1순위 키가 이미 이걸 쓴다.
- **「가다가 자리가 날 가능성 NN%」** — 10% 이상이면 따로 적고, 어느 역에서 많이 내리는지도 적는다
  (2026-09-04 추가 지시: 「사람들이 너무 많이 빠져서 자리가 빌 정도의 확률이라면 표기해줘야 한다」).
  실측 예: 4호선 미아→충무로 08시 = 탈 때 0% · 가다가 18%(충무로에서 많이 내립니다).

여정 전체 값은 구간을 **타는 시간으로 가중평균**한다. 「한 번이라도」로 합치면
21%·21%·34% 짜리 세 구간이 58% 가 되어 앉을 것처럼 읽혔다(실측).

## D-34. 혼잡도는 **방향별로** 읽어야 한다 ★★

실측 — 4호선 미아 평일 08:00: **상선 15% / 하선 103%.** 일곱 배다.
둘 중 큰 값을 쓰고 있었으므로 한산한 방향으로 가는 사람에게도 「못 앉는다」고 말했다.

**처음엔 규칙을 손으로 박았다가 틀렸다** — 그 경위는 D-36 에 이어 적는다.
지금은 `pipeline/build_graph.py` 가 **자료로 판정해** `route.dirLabels` 에 넣고,
`engine/loads.js` 는 그걸 읽기만 한다.

고친 뒤: 미아→충무로(도심행) 탈 때 **0%** / 충무로→미아(외곽행) 탈 때 **61%**.

**하차 인원은 방향이 없다**(역 전체 합계). 그대로 쓰면 반대 방향 하차까지 내 열차에서
내리는 것으로 세어 빈자리를 두 배로 본다. 나눌 근거가 없으므로 절반으로 잡고 추정으로 표시한다.

## D-36. 방향 라벨은 **규칙을 박지 말고 자료로 판정**한다 ★★★

D-34 에서 「`dirs[0]`(역번호 오름차순) = 하선」이라고 손으로 박았다. **두 호선에서 틀렸다.**

| 호선 | 실제 (번호 증가 방향) | 박아 둔 규칙 | 결과 |
|---|---|---|---|
| **1호선** | **상선** | 하선 | **정확히 반대 방향의 혼잡도를 읽었다** |
| **2호선** | **내선** (상선/하선이 없다) | 하선 | 매칭 실패 → 늘 **둘 중 큰 값**으로 물러났다 |
| 3~8호선 | 하선 | 하선 | 맞음 |

1호선이 뒤집힌 이유: 서울교통공사 1호선 구간(서울역 150 ~ 동묘앞 160)은 **전체 1호선의 가운데 토막**인데
자체 번호를 붙였고, 상·하 표기는 전체 노선 기준(소요산 쪽이 상행)을 따른다. 그래서 둘이 어긋난다.
2호선은 순환선이라 애초에 상선/하선이 아니라 **내선/외선**이다.

**틀렸을 때 얼마나 나빴나** — 청량리→종각(도심행) 평일 08:00:

| | 혼잡도 | 화면에 나온 말 |
|---|---|---|
| 박아 둔 규칙 | 12% | **「빈자리 35석 · 앉을 확률 100%」** |
| 실제 | 66% | 「만석 · 앉을 확률 0%」 |

정확히 반대로 말했다. 2호선은 큰 값 폴백이라 도심행은 우연히 맞았지만
반대 방향(외선 40%)까지 91%로 읽어 「0%」라고 했다 — 지금은 15%.

**판정 방법 (물리로 정한다, `detect_directions`)**

> 진행 방향으로 한 정거장 갈 때 재차인원의 증감은 그 역의 **(승차 − 하차)와 부호가 같다.**
> 라벨 L 을 「번호 증가 방향」으로 놓고 상관계수를 재서 가장 큰 쪽이 그 방향이다.

평일 08시·18시를 함께 넣어 재고, 1등과 꼴찌의 차이가 0.15 미만이면 판정을 포기하고 로그를 남긴다.
실측(2026-09-04): 1호선 상선 +0.80 / 하선 −0.62 · 2호선 내선 +0.28 / 외선 −0.16 ·
3~8호선 하선 +0.39~+0.78. 두 시각이 같은 답을 냈다.

**교훈**: 「상행선은 기점 방향」 같은 **상식 규칙을 코드에 박으면, 예외가 있는 노선에서
조용히 정반대 답이 나온다.** 자료 안에 판정 근거가 있으면 자료로 정한다.

## D-37. 버스는 이미 방향별이다 (확인함)

「버스도 상하행 나눠져 있으면 다 구분해야지」 — 확인해 보니 이미 그렇다.

- 노선의 경유 순번(OA-1095)은 **왕복이 한 줄**이라 「순번 i < j」가 곧 방향이다.
- 왕복 정류장은 **서로 다른 NODE_ID** 다(147번: 114개 정류장 중 27쌍이 같은 이름·다른 ID).
  승하차 자료(OA-12913)가 NODE_ID 로 키를 잡으므로 **방향별 값이 이미 들어온다.**
- 같은 NODE_ID 가 한 노선에 두 번 나오는 노선이 718개 중 223개인데, 대부분 **기점·종점 한 곳**이다
  (물리적으로 같은 정류장이라 맞는 처리다).

지하철만 방향이 따로 저장돼 있어 짝을 지어야 했고, 그게 D-34·D-36 이다.

## D-38. 문구 — 「탈 때」를 이름 안에 넣는다

「앉을 확률 0% · 못 앉습니다  **탈 때 바로**」 뒤에 「**가다가** 자리가 날 가능성」이 이어져
**「탈때바로가다가」** 로 읽혔다(사용자 지적). 꼬리말을 떼고 뜻을 이름 안에 넣었다.

- 「**탈 때 앉을 확률** 0% · 못 앉습니다」
- 「**서서 가다가 앉아 갈 가능성** 18% (충무로에서 많이 내립니다)」

## D-39. 이름이 정확히 맞는 역은 주소보다 위다 (D-28 재조정)

주소를 위로 올렸더니 **「교대」를 쳤을 때 「서초대로 지하294」**라는 도로 주소가 1등이 됐다.
「교대」라고 친 사람은 교대역을 찾는 것이다. 그래서 차례를 셋으로 나눈다.

> ① 친 이름과 **정확히 맞는** 역·정류장 → ② 주소·상호 → ③ 나머지 역·정류장

「국립중앙박물관」은 로컬에 「국립중앙박물관.용산가족공원」(정류장)뿐이라 ①에 안 걸리고
주소가 위로 간다 — 두 요구가 함께 지켜진다.

## D-40. 「그냥 걸어가기」는 거리가 아니라 **시간**으로 자른다

2.5km 로 잘랐더니 **35분 걷는 길이 1순위**로 올라왔다(서서 가는 시간이 0이라서).
주 사용자가 어르신·교통약자인데 35분을 걸으라는 것은 답이 아니다.
**20분**으로 자른다 — 걸음 속도 설정이 그대로 반영된다(보통 약 1.1km, 「아주 느림」 약 550m).

## D-41. 저녁 두 번째 봉우리는 **오류가 아니다** (실측 확인)

「저녁 9시 30분보다 10시 30분이 더 앉을 확률이 낮다, 오류다」는 지적을 받고 원자료를 봤다.
**자료가 실제로 그렇다.**

```
4호선 혜화 상선 평일 (원자료 30분 격자, 보간 없음)
  20:30 55%   21:00 55%   21:30 50%   22:00 69%   22:30 80%   23:00 55%
```

22시대 두 번째 봉우리가 **혜화·동대문·한성대입구**(대학로·동대문 상권)에 몰려 있다 —
늦은 저녁 귀가 러시다. 전 노선으로 보면 22:30이 더 붐비는 곳은 520개 역·방향 중 **70개뿐**이라
특정 상권의 실제 현상이지 전반적 오류가 아니다.

확률이 20% → 0% 로 급락한 것은 충무로 혼잡도가 36% → 57% 로 **좌석 만석선(34%)** 을 넘었기 때문이다.
사양서 5.1 의 `k = 좌석수 × 0.11`(≈ 6명)이라 그 언저리에서 급하게 꺾인다 — 「탈 때 바로 앉을 확률」이므로
좌석이 다 차면 실제로 못 앉는 게 맞다. **고치지 않았다.**

## D-42. 결과창에서 바로 고치고, 경유지는 「＋」로 더한다

사용자 지시. 길을 다시 찾으려고 처음 화면으로 되돌아가는 것이 번거롭다.

- 결과 목록 위에 **편집 바**: 출발 · (경유…) · 도착 · 요일 · 시각. 무엇을 바꾸든 **바로 다시 찾는다.**
- **고치는 칸은 그 자리 바로 밑에 연다.** 한 자리에 고정해 뒀더니 출발지를 고치는데
  입력칸이 **도착지 밑**에 떠서 헷갈렸다. 입력칸은 하나만 두고(둘로 두면 한쪽만 고치게 된다)
  자리만 옮긴다.
- **경유지는 작은 「＋」 아이콘 하나로만** 늘린다(34px 동그라미, 둘까지).
  글자 붙은 큰 버튼을 놓았더니 안 쓰는 사람에게도 자리를 차지했다 — 아이콘만 두고,
  누르면 그때 칸이 생긴다. 「×」로 뺀다.
- 경유는 구간을 나눠 따로 찾아 이어 붙인다(`combineVia`). **환승 2회 제한은 구간마다** 적용된다 —
  경유지에서 내렸다 타는 것은 사용자가 원해서 내리는 것이지 갈아타는 게 아니다.
  그래서 「4호선 → (경유) → 4호선」이 나올 수 있고, 화면에 **「📍 경유지에 들렀다 다시 탑니다」**로 적는다.
- 경유지에 머무는 시간은 더하지 않는다 — 얼마나 머물지는 사람마다 다르다.

## D-43. 시각은 10분 단위, 「시 + 분」 두 칸

30분 단위로는 부족하다는 지적. 05:00~24:00 을 10분으로 끊으면 **115개**라 목록 하나로는 못 고른다.
**시 20칸 · 분 6칸**으로 나눈다 — 어르신이 훑기에도 이 쪽이 쉽다.
「오전·낮·저녁·자정」 같은 말은 붙이지 않는다 — 24시간 표기면 이미 명확하고 칸만 좁아진다.
사양서 3.2 가 요구한 「5~10분 단위」와도 맞는다(저장은 원천 해상도, 10분은 보간 — D-08).

## D-44. 구간마다 **실제로 타는 시각**으로 계산한다 ★★

「5시에 4호선에 왜 못 앉아?」라는 지적에서 찾은 진짜 결함이다.

출발 시각 하나를 **모든 구간에** 그대로 넘기고 있었다. 걷고 버스 타고 갈아탄 뒤
**한 시간 가까이 지나서 타는 지하철도 출발 시각의 혼잡도**로 계산했다.
아침에는 30분 차이로 혼잡도가 두 배가 된다(4호선 미아 하선 06:00 37% → 07:00 68%).

`describe()` 가 구간마다 `offsetMinutes`(출발부터 그 차를 타기까지 걸린 분)를 담고,
`loads.js` 가 `출발 시각 + offset` 으로 자료를 읽는다. 경유지로 이을 때는 앞 구간 시간만큼 더 민다.
화면에도 **「05:47쯤 탑니다」**로 적는다.

실측 — 월곡두산위브아파트 → 중구청 05:00 출발:
4호선 구간이 **05:00 → 05:47** 로 바뀌며 탈 때 확률 2% → 11%,
2호선 구간은 05:56 로 계산돼 「54자리 가운데 10자리 비어 있습니다」가 됐다.

## D-45. 자료 격자 밖의 시각은 **잘라 쓰되 밝힌다**

혼잡도 격자는 **05:30~24:30** 이다. 05:00 을 물으면 `valueAt` 이 조용히 05:30 값으로 자른다.
그 사실을 숨기면 없는 정보를 아는 척하는 것이다.

구간마다 「그 시각엔 아직 첫차가 없을 수 있습니다 — 자료가 05:30부터라 그 값으로 봤습니다」를 띄운다.
막차 뒤도 같다.

**첫차 시간대가 한산할 것이라는 짐작은 틀렸다** — 실측 4호선 미아 하선 05:30 = **48.9%**(칸당 78명, 만석).
새벽 근무자가 몰린다. 전 노선 평균은 05:30 이 22.9% 로 낮으니 특정 구간의 실제 현상이다
(2호선 사당·방배 외선은 05:30 에 80~88%).

## D-46. 「앉아 가는 차 찾기」 — 길찾기의 반대 ★

사용자 지시: 「반대로 어딘가로 가기 위해서 앉아갈 확률이 아주 높은 버스나 지하철을 찾는 UI.
맨 처음 화면에서도, 검색결과 화면에서도 보이게 하되 **작은 탭으로 숨겨** 놔라」

길찾기는 「A 에서 B 로」를 묻고 서서 가는 시간으로 줄 세운다.
이 화면은 반대로 **「여기서 앉아 갈 수 있는 차가 무엇이냐」**를 묻는다.
앉으려고 조금 기다리거나 시각을 옮길 수 있는 사람에게 필요하다.

- 평소엔 **작은 탭 하나**(🪑)로만 있고, 누를 때 열린다. 두 화면(처음·결과)에 같은 탭.
- `engine/route.js seatableRoutes()` — 출발지에서 걸어갈 수 있는 모든 정류장·역의
  모든 노선·방향에 대해 **탈 때 앉을 확률**을 재고 줄 세운다.
  같은 값이면 **덜 걷는 쪽**을 위로 — 앉으려고 십 분을 더 걷는 것은 답이 아니다.
- 목록을 둘로 나눈다: **「도착지로 갈 수 있는 차」**(문턱 없이) + 「그 밖에 앉아 갈 수 있는 차」(50% 이상).
  **도착지 쪽은 문턱으로 거르면 안 된다** — 「그쪽엔 앉을 만한 게 없다」도 답이기 때문이다.

**「도착지로 갈 수 있는 차」를 직통 노선으로만 보면 틀린다.** 월곡→을지로4가는 갈아타야 하는데
직통만 보면 「그쪽으로 가는 차가 없다」가 된다. 그래서 실제 길찾기를 돌려 **첫 구간에 나오는 노선**을 쓴다.
다만 찾은 경로를 전부 세면 반대 방향으로 갔다 되돌아오는 길까지 들어와
6호선 **신내역 방면**(도심 반대)이 「을지로4가 쪽」으로 찍혔다 —
**덜 걸리는 순 위 8개**의 첫 구간만 센다.

실측(월곡역 → 을지로4가역, 6호선 응암 방면 탈 때 앉을 확률):
**08시 0% · 11시 91% · 14시 97% · 21시 100%.** 시각을 바꿔 가며 보라는 것이 이 화면의 쓸모다.

## D-47. 버스 승객 수가 **30배 부풀어** 있었다 ★★★

축별 결함 검사(워크플로)가 찾아낸 것. **이 프로젝트에서 가장 큰 수치 오류였다.**

OA-12913 의 한 행은 그 달의 **총계**다(첫 열이 `사용년월`). 그런데 집계 코드가
행 수(= 월 파일 개수 3)로만 나눠 「월 평균」을 만들어 놓고 단위를 **「하루 평균」**이라 적었다.
`loads.js` 가 그 값을 **시간당 인원**으로 읽어 시간당 버스 대수로 나누니
**한 달치 승객을 버스 열 대에 다 태우는** 셈이 됐다.

검산 — 2026-05 원천 전체 승차 **150,975,269명 ÷ 31일 = 487만명/일.**
서울 버스 하루 승차(약 480~490만)와 맞는다. 배포본은 152번 한 노선의 한 달치 886,093명을
「하루」라고 들고 있었다(맞는 값은 28,710).

**증상**: 간선버스가 전부 「앉을 확률 0%」. **승객이 많은 노선일수록 정렬에서 구조적으로 꼴찌**가 되고,
승객이 적은 지선만 100%로 1순위에 올랐다. 사용자가 「버스는 왜 다 못 앉나」로 느꼈을 원인이다.

고친 뒤 — 월곡→을지로4가 평일 14시: 간선 152·100번이 **0% → 98%**,
「앉아 가는 위치」 결과가 3곳 → 14곳.

지하철은 처음부터 `day_count`(평일 254일)로 제대로 나누고 있었다 —
**두 원천이 30배 어긋난 채 같은 취급을 받고 있었던 것**이다.

## D-48. `'서울역역'.rstrip('역')` 이 「서울」이 된다 ★★

역 이름에 '역' 을 붙여 두었으므로 서울역은 `'서울역역'` 이 되는데,
파이썬 `rstrip` 은 인자를 **문자 집합**으로 보고 끝에서부터 반복해 뗀다 → `'서울'`.
**서울역만** 이 함정에 빠진다(다른 역은 '역' 이 한 번만 붙는다).

그러면 혼잡도 키(`4|서울역|weekday|하선`)를 못 찾아 `line|전체|day`,
즉 **그 호선 전 역·전 방향의 최댓값**(D-19 의 호선 피크)으로 물러났다.

실측 — 4호선 서울역 평일 08:00: 실제 **상선 57.4% / 하선 30.8%** 인데 **131.4%** 로 읽어
「54자리가 모두 찼습니다 · 앉을 확률 0%」라고 말했다.
`re.sub(r'역$', '', …)` 로 한 번만 떼도록 고쳤다.

**교훈**: `rstrip`·`lstrip` 은 접미사를 떼는 함수가 아니다. 접미사를 뗄 때는 정규식을 쓴다.

## D-49. 「앉아 가는 대중교통 위치」의 정의 (D-46 재작성) ★

D-46 에서 「출발지 근처에서 앉을 수 있는 차」로 만들었는데 **요구를 잘못 읽었다.**

> 「이 앉아가는 대중교통 위치에서는 **출발지가 전혀 고려되지 않아.**
>  목적지만 설정하고 그 목적지까지 100% 앉아 갈 수 있는 **위치**를 찾는 게 목적이다.
>  그중에서 **가장 가까운 위치**를 표기하는 거지.」

- **출발지를 쓰지 않는다.** 목적지 하나만 받는다.
- 목적지로 가는 모든 노선·방향을 훑어, 거기서 타면 **앉을 확률 95% 이상**인 승차 지점을 찾는다.
- **목적지에서 가까운 순** — 덜 거슬러 올라가는 쪽이 낫다.
- 노선·방향마다 **목적지에 가장 가까운 자리 하나**만 남긴다(더 거슬러 올라갈 이유가 없다).
- 거르는 조건 셋: 타는 시간 60분 이하 · **최소 3정거장** · 35정거장 이내.
  1~2정거장(실측 「강남역에서 166m, 1정거장」)은 그냥 걸어가면 되므로 답이 아니다.
- 100% 자리가 여섯 곳 미만이면 「거의 앉습니다(70% 이상)」를 따로 묶어 덧붙인다. **섞지는 않는다.**

## D-50. 화면 코드를 덩어리째 바꾸지 말 것 — 시험으로 막는다 ★

`index.html` 을 고치다 **코드 덩어리를 통째로 지운 적이 두 번** 있다.
「⑤ 화면 블록부터 길찾기 블록까지」를 한 번에 갈아 끼우면서 그 사이에 있던 것을 함께 날렸다.

| 사고 | 증상 | 발견 |
|---|---|---|
| 편집 바 블록 삭제(`openPick`·`setEdTime`) | 시각을 바꿔도 결과가 안 바뀐다 (엄격 모드 ReferenceError) | 내가 디버깅하다 |
| 「다음」·「길 찾기」 클릭 핸들러 삭제 | **버튼을 눌러도 아무 일이 없다** | **사용자가** |

문법은 멀쩡하고 시험도 다 통과했다 — Node 시험이 화면을 안 건드리기 때문이다.

**규칙 둘**
1. 넓은 범위를 통째로 바꾸지 말고 **좁은 문자열**을 바꾼다. 어쩔 수 없이 범위를 바꾸면
   바꾸기 전 그 범위에 무엇이 있었는지 먼저 읽는다.
2. `tests/ui.test.js` 가 정적으로 막는다 —
   **조작부마다 핸들러가 걸려 있는가**(직접·변수·대신 걸어 주는 함수 셋 다 인정),
   화면을 넘기는 버튼 열 개가 살아 있는가, 쓰는 이름이 다 선언돼 있는가.
   실제로 났던 두 사고를 흉내 낸 사본으로 검사식이 잡는 것을 확인했다.

## D-35. 심야·새벽버스는 낮에 후보에서 뺀다

08시 경로에 **「새벽A160」**이 올라왔다. 승하차 자료가 아예 없는 노선이라
「자료 없음」으로 빠졌고, 그러면 시간대 판정을 못 받아 그대로 남는다.
D-25(승객 0명 = 안 다님)가 **자료가 있는 노선만** 걸러 주기 때문이다.

이름으로 심야를 가리고(`N…` 과 「새벽」) **23시~05시 밖이면 자료와 무관하게 뺀다.**
`route_kind` 에 「새벽」을 넣어 심야버스가 18개 → 22개가 됐다.

## D-16. 사양서 12장(수익모델)은 이번 단계에서 구현하지 않는다

`[참고]`로 표시돼 있고 개발 우선순위에 영향이 없다고 사양서가 밝혔다.
광고·결제·제휴 코드는 넣지 않는다. 설계가 그쪽을 막지만 않으면 된다.

## D-51. 버스 왕복을 회차점에서 두 방향으로 가른다

**증상.** 평일 08:00 「월곡두산위브아파트 → 중구청」이 열두 개 경로 전부
「탈 때 앉을 확률 100%」였다. 출근시간인데.

**원인.** 노선-정류장 원천(OA-1095)에 방향 열이 없어 **왕복을 한 줄로** 두고 있었다.
그런데 자료에는 방향이 **살아 있었다** — 정류장 ID 가 방향마다 다르다
(「동대문역.흥인지문」이 100000398 / 100000365 로 따로 있고 승하차도 따로 집계된다).
한 줄로 두면 OD 배분이 회차점을 넘어 이어져, 도심으로 몰리는 아침 승객이
돌아 나오는 빈 차편과 섞여 평준화된다.

**판단.** `split_round_trip()` 으로 **기점에서 가장 멀어지는 자리**를 회차점으로 잡고 가른다.
세 가지를 확인하고 하나라도 어긋나면 **가르지 않는다**(순환 노선을 가르면 안 되므로):
① 양쪽 4정류장 이상·길이가 심하게 안 치우침 ② 돌아온 끝이 기점 2km 안
③ **되짚어 온다** — 가는 쪽 정류장의 절반 이상이 오는 쪽 160m 안에 짝이 있다.
③ 이 순환을 걸러 낸다(순환은 되짚지 않고 한 바퀴 돈다).

실측: 718개 중 **606개가 두 방향**, 112개는 한 줄로 남았다.
152번 08시 한 대 최대 재차 21.1명 / 30.9명으로 방향이 갈렸다(전에는 한 값 27.6명).

**함께 고칠 것.** `loads.js` 의 OD 캐시 키에 **방향과 요일**이 반드시 들어가야 한다.
빠지면 아침 도심행의 값이 한산한 반대 방향에 그대로 쓰인다.

## D-52. 지하철역은 「가까운 순」에서 자리를 따로 남긴다

**증상.** 같은 화면에 **지하철이 한 줄도 없었다.** 월곡에서 도심을 가는데 6호선이 없다.

**원인.** 출발지 근처 노드를 「반경 900m 안에서 가까운 순 12개」로 뽑는데,
그 반경 안에 버스정류장이 **68곳**이라 741m 떨어진 월곡역이 **46번째**가 되어 잘렸다.

**판단.** `nearbyMixed()` — 정류장은 촘촘하고 역은 드물다. **거리만으로 자르면 드문 쪽이 늘 진다.**
반경 안 지하철역을 최대 3곳 먼저 확보하고 나머지를 거리순으로 채운다.
역은 수송력도 배차도 정류장과 급이 달라 후보에서 빠지면 안 된다.

**시험도 같은 함수를 쓰게 바꿨다** — 시험만 다른 경로를 타면 검증이 거짓말을 한다.

## D-53. 앉겠다고 세 배 돌아가는 길은 버린다

**증상.** 같은 화면 1·2위가 **201분·137분**짜리였다. 서서 가는 시간이 0분이라는 이유로.
같은 출발·도착을 64분에 가는 길이 있었다.

**판단.** 정렬 1순위가 「서서 가는 시간」인 것은 이 서비스의 존재 이유라 안 바꾼다.
대신 **후보에서 미리 걷어낸다** — 가장 빠른 경로의 **1.6배** 또는 **+25분** 중 큰 쪽을 넘으면 버린다
(`prune`). 사양서 1.2 의 예시도 41분 대 52분(1.27배)이지 세 배가 아니다.
비율만으로 자르면 짧은 길에 야박해서 「+25분」쪽을 함께 본다.
전부 잘려 나가면 자르지 않는다 — **빈 화면보다는 이상한 답이 낫다.**

## D-54. 버스의 요일 차이는 지하철 실측 비율로 되돌린다

**증상.** 화면에서 요일을 바꿔도 **버스 숫자가 1도 안 변했다.** 일요일 아침 버스가 출근버스와 같았다.

**원인.** 버스 원천(OA-12913)은 **월 총계**라 요일이 아예 없다. 날짜 수로 나눈 「하루 평균」에는
평일 출근 첨두가 주말에 희석돼 들어가 있다.

**판단.** 지하철 승하차(OA-12921)는 **요일별 실측**이므로 그 비율을 뽑아 버스에 쓴다(`dayFactors`).
같은 도시의 같은 시각 통행이라 근사로 쓸 만하고, **안 쓰는 것보다 훨씬 낫다.**

**하루 한 개 값이 아니라 시간대별 표**여야 한다 — 하루 전체로는 토 0.76·일 0.57(평일=1)인데
**08시만 보면 토 0.33·일 0.23** 이다. 출근 첨두는 요일 차가 훨씬 크다.

| 08시 보정계수 | 평일 | 토 | 일 |
|---|---|---|---|
| | **1.28** | 0.43 | 0.29 |

자료가 얇은 시간대에서 튀지 않게 계수를 0.2~2.0 으로 가둔다.
**버스 요일별 실측이 생기면 이 함수를 걷어낸다.** 화면의 근거 문구에 「요일 보정 ×N」을 밝힌다.

## D-55. `makeLoadFor` 가 버스에 껍데기 ctx 를 넘기지 않는다

D-54 를 넣었는데도 값이 안 변해서 찾은 것. `makeLoadFor` 가 버스에는
`{ minutes, busRoute }` 만 담은 객체를 **매번 새로 지어** 넘기고 있었다. 둘이 조용히 죽어 있었다:

① `dayType`·`ride` 가 안 실려 **요일 보정이 버스에 영영 안 먹었다**
② `_busById` 를 매번 `null` 로 지어 넘겨 정류장 색인 memo 가 늘 헛돌았다

객체 하나를 만들어 두고 값만 갈아 끼운다.
**교훈:** 필요한 것만 골라 담은 ctx 는 나중에 넣는 기능을 조용히 굶긴다.

## D-56. 노선 종류는 원천의 「교통수단타입명」으로 — 이름으로 추측하지 않는다

버스 승하차 원천(OA-12913)의 마지막 열이 노선 종류를 직접 알려 준다.
예전에는 번호 자릿수로 추측했는데 665개 중 **44개가 틀렸고 전부 좌석 수까지 틀렸다** —
741번은 4자리지만 간선이고, 9401-1·「서울01출근」은 광역인데 마을버스(15석)로 보고 있었다.
광역은 입석 금지라 모델 경로 자체가 다르므로 이 오류는 컸다.
`build_datasets.py` 가 `vehicleType` 으로 실어 나르고 `build_graph.py` 가 그것을 먼저 본다.
순환버스(01A·01B·상암A21)는 종류를 새로 만들되 차는 마을버스급 소형으로 둔다.

## D-57. 배차는 종류별 상수가 아니라 **노선별 인가 배차간격**으로 나눈다

**증상.** 출근길 간선버스가 「23자리 가운데 2자리 비어 있습니다 · 68%」로 나왔다.
사용자: "100번버스 진짜 2자리정도 비어있냐 저기?"

**원인.** 시간당 대수를 종류별 상수(간선 10 × 첨두 1.4 = 14대/시 = 4.3분 배차)로 박아 두었다.
세 갈래 검산이 전부 「과다」를 가리켰다:
- 서울시 인가대수(2026-01: 시내 7,383 + 마을 1,626 = 9,009대)로 역산하면
  옛 가정은 첨두에 **15,972대**를 요구한다 — 서울에 있는 것의 1.8배
- 노선기본정보(OA-15262) 실측 배차 중앙값: 간선 11분 · 지선 12분 · 마을 12분
- 100번 인가 배차 10분 = 6대/시. 우리는 14대/시로 나눠 재차인원을 절반 이하로 희석했다

**판단.** OA-15262 의 **2024년 1~4월 판**(seq 25)에 노선별 평균배차간격(CARALC)이 있다
(최신 판에서는 이 열이 사라졌다 — 노선별 배차의 마지막 무키 원천).
`build_graph.py load_headways()` 가 노선마다 `headwayMin` 으로 싣고,
`loads.js busesPerHour()` 가 그것을 먼저 쓴다(647/718개 노선). 없는 노선만
종류별 중앙값(간선 5.5 · 지선 5.0 · 마을 5.0 · 광역 4.0 · 심야 2.5대/시)으로 물러난다.
첨두 배율은 1.4 → **1.25** — 1.4면 인가대수로 낼 수 있는 상한(간선 8.3대/시)을 넘는다.
심야버스는 시간대 배율을 안 얹는다(밤이 본업이다 — `nightBusRunning` 이 시각을 따로 가른다).

2년 묵은 값이라는 한계는 분명히 적는다. 최신 노선별 배차는 공공데이터포털 버스노선정보
API(인증키 필요)에 있다 — 키가 나오면 갈아 끼운다.

**결과.** 100번 08시: 재차 21 → 25명, 「2자리 · 68%」 → 「만석 언저리 · 31%」.

## D-58. OD 감쇠(decayStops)는 15 → 종류별 5~6 — 저장소 안 실측으로 보정했다

**감쇠값이 「버스에 몇 명 타 있나」를 지배하는데 15의 근거가 어디에도 없었다.**
정답지는 자료 안에 있었다 — 버스 자료에는 정류장별 승차와 하차가 **둘 다** 있으므로
그대로 누적하면 감쇠 모형 없이 재차 곡선이 나온다.
승·하차 합이 15% 안에서 맞고 누적이 음수로 안 떨어지는 깨끗한 방향 429개(08시)와 대조:

| decayStops | OD 최대재차 ÷ 실측 (중앙값) |
|---|---|
| 15 (옛 값) | 간선 1.42 · 지선 1.35 — **40% 부풀림** |
| **6** | 간선 1.01 · 지선 1.04 · 마을 1.06 |

실측 평균 탑승 정거장: 간선 5.6 · 지선 5.2 · 마을 4.4.
광역만 다르다 — 평균 12정거장쯤 타므로 20으로 따로 둔다(표본 6개 방향, 얇음).
`DECAY_STOPS = { village: 5, branch: 6, trunk: 6, night: 6, circular: 5, express: 20 }`.
`tests/calibration.test.js` 가 이 대조를 회귀 시험으로 박아 두었다 —
예전에는 15를 5로 바꿔도 30으로 바꿔도 시험 111개가 전부 통과했다.

## D-59. 빈자리 경쟁: 타는 사람도, 「내가 서 있다」는 정보도 넣는다

**증상.** "서있는 시간이 다들 너무 짧아서 현실성이 없다. 출근시간인데 1분만 서는 게 말이되냐."

**원인 둘.** ① `pSitAtStop` 분모가 「서 있는 사람 수」뿐이라, 그 정거장에서 **타는 사람**이
같은 빈자리를 노리는 것이 빠져 있었다. ② 분모의 재차가 **여러 대의 평균**이라
재차 25/좌석 23이면 「서 있는 사람 2명」이 되는데, pBoard 는 같은 상황을 「69%가 서서 탄다」고
말한다 — **못 앉고 탄 사람은 평균보다 붐비는 차에 탄 것**이므로 두 식이 모순이었다.
그 결과 재차가 좌석 근처로 내려오는 순간 q 가 상한 0.92에 붙어
서 있을 확률이 한 정거장에 0.575 → 0.046으로 무너졌다(6호선 실측 추적).

**판단.** 사양서 5.2의 α(0.55)·상한(0.92)은 그대로 두고 분모만 고친다:
`rivals = max(1, load + 좌석×0.11 − seats) + 타는 사람`.
- 타는 사람 = 재차 변화로 역산(지하철) / OD 승차(버스) — 새 자료 필요 없음
- 조건부 보정은 pBoard 가 이미 들고 있는 차간 편차 척도 k(=좌석×0.11)를 그대로 쓴다 —
  **새 상수가 아니다.** 만원 구간에서는 티가 안 나고 좌석 언저리에서만 작동한다
실측 변화: 6호선 월곡→삼각지 「가다가라도 앉을 확률」 100% → 87%,
4호선 91% → 72%, 100번 구간 서서 1분 → 4분.

## D-60. 내리는 역에서 자리 나는 것은 안내하지 않는다

「서서 가다가 앉아 갈 가능성 73% (**충무로**에서 많이 내립니다)」 — 충무로에서 **내가 내린다.**
`bestOffAt` 루프의 마지막 바퀴에서 next 가 목적지 자신이라 생긴 일이다.
확률 계산은 무사했다(`ride()` 가 마지막 구간의 하차를 안 쓴다) — 틀린 것은 화면의 이름뿐이지만,
그 한 줄이 숫자 전체를 못 믿게 만든다. 가는 도중의 역만 세도록 고쳤다(삼각지→충무로는 「명동」).

## D-61. 여정에는 「탈 때 앉을 확률」이라고 쓰지 않는다

여정은 탈 때가 여러 번이다(구간마다 100%와 68%였는데 머리에는 「탈 때 앉을 확률 75%」).
그 75%의 정체는 **타자마자 앉은 채로 가는 시간의 비율(기댓값)** 이므로 그렇게 말한다 —
「타는 시간의 75%는 앉아 갑니다」. 퍼센트 표기와 다섯 단계 분류(D-31)는 그대로다.
구간 문구는 「탈 때 앉을 확률」 그대로다(D-33).
버스 좌석 문구에는 「하루 평균으로 낸 값이라 차마다 다릅니다」를 붙인다 —
월 총계 평균 + 배차 가정으로 낸 추정을 단정형으로 말하지 않는다.

## D-62. 승객이 겪는 재차 = 차량 평균 × 1.25 (버스만) — 실사용 후기와의 교차검증에서

D-57·58까지 고친 뒤에도 100번 08시가 「서서 4분」이었다. 실사용 후기(나무위키 100번:
"출퇴근 시 동소문로에서 **입석은 기본**이고 심하면 도착할 때까지", "정체로 **배차가 불규칙**하며
가축수송")와 대조하면 아직 낙관이었다. 원인이 후기 안에 있다 — **배차 불규칙.**

앞차와 3분 벌어진 차는 비고 15분 벌어진 차는 미어터지는데, 승객은 벌어진 틈에 더 많이
쌓이므로 **붐비는 차에 탈 확률이 높다**(대기시간 역설). 간격 변동계수 CV면 승객이 겪는
평균 재차는 차량 평균의 (1+CV²)배 — 도심 혼합차로 CV 0.5로 **1.25배**(보수적인 쪽).
재차·하차·승차에 똑같이 곱해 「승객이 겪는 그 차」의 세계로 통째로 옮긴다.
지하철에는 안 곱한다(열차 배차는 규칙적, CV≈0.1).

결과: 100번 08시 탈 때 31% → **4%, 서서 9분** — "입석 기본"과 부합.
낮 96~99%·일요일 100%는 그대로다(빈 차에 1.25를 곱해도 빈 차다).
152번 27%(「총 승객수 1~3위 가축수송」 후기와 부합), 6호선 만석 0%(혼잡도 하위권 후기 —
동쪽 구간은 이태원·한강진에서 자리가 난다는 것까지 실측과 후기가 같은 말을 한다).

**이 계수는 자료가 아니라 문헌값 근사다** — 이 저장소에서 유일하게 실측으로 못 박지 못한 수.
정답은 T-DATA 의 차량별 재차인원이고, 키가 나오면 걷어낸다. `tests/calibration.test.js` 가
1.1~1.5 범위를 지킨다.

## D-63. 배차간격을 「오늘 값」으로 — 서울시 노선정보조회 API (키 확보, 2026-09-05)

공공데이터포털 키가 승인되면서 배차의 원천이 한 단계 올라갔다:
**현재 API 값(1순위) → 2024 인가값(보충) → 종류별 중앙값(최후)**.
`pipeline/fetch_headways.py` 가 빈 검색 한 번으로 전 노선(1,363건)을 받아
`data/bus/headways.json` 에 저장하고, `build_graph.load_headways()` 가 1순위로 쓴다.
실측 대조(644개 노선): 60% 그대로, 84% ±2분 안 — 그러나 **마을버스 여럿이 절반/두 배**
(노원03 25→13분, 광진04 9→18분). 2024값을 계속 쓰면 그 노선들이 두 배 틀린다.
결과 배차 탑재율 647 → **698/718**.

같은 날 배운 것 둘:
- **TAGO(국토부 버스노선정보)는 서울이 없다.** 도시 138개 목록에 서울만 빠져 있다
  (서울은 자체 BIS 운영). 서울 배차는 서울시 서비스(15000193)가 정본.
- 공공데이터포털 키는 **계정에 하나** — 서비스마다 활용신청만 하면 같은 키가 먹는다.
  Encoding/Decoding 두 벌 중 **Decoding** 을 쓴다(사용자가 Encoding 을 넣어 URL 디코딩으로 복구했다).

## D-64. 버스 요일·시간 계수를 버스 자신의 실측으로 (T-DATA 구간 재차, 2026-09-05)

**T-DATA 의 진짜 통로는 API 가 아니라 파일이다.** 오픈API(TaimsTpssA18RouteSection)는
키가 통과돼도 측정값이 전부 비어 온다 — 최근 날짜는 뼈대만, 과거(2021~2025)는 행 자체가 0
(여덟 시점 실측). 반면 데이터제공의 파일판(data_id=25)은 로그인 없이 받아지고
「재차인원합」(시간대별, 전 운행 합 = 구간 재차 유량)이 실제로 차 있다.
월 1회 갱신, 최근 이틀치(일요일 + 평일 하나). 평균·최대·운행횟수 열은 파일에도 비어 있다.

**시간 귀속 함정** — 원본은 「운행 출발 시각」 기준이다. 4시에 출발한 버스가 6시에 지나는
구간도 4시로 적혀, 그대로 대조하면 새벽이 3~6배 부풀고 심야가 꺼진다.
정류장순서 × 2.0분(인가 운행소요시간 실측치)만큼 앞으로 밀어 통과 시각으로 되돌렸다
(밀고 나니 낮 시간대 실측/모델이 1.02~1.04 로 붙었다 — 재배치가 맞았다는 뜻).

**전면 대조 결과** (76,348개 구간, 시간대 3개 × 요일 2개):
- 구간 상관 0.84~0.95 — 재차가 노선 어디서 쌓이는지는 OD 배분이 맞힌다. **구조는 옳다.**
- 그러나 지하철 차용 계수(D-54)로는 배율이 계통적으로 틀렸다:
  첨두 ×1.4~1.6 과대 · 낮 ×0.7 과소 · 일요일 ×0.54 과소.
  **버스는 통근 쏠림이 지하철보다 완만하다** — 지하철의 가파른 곡선을 씌우면 안 된다.

**판단.** 종류×요일×시간 계수를 실측에서 직접 뽑아(data/bus/tdata-calib.json) 1순위로 쓰고,
표본이 얇은 칸(null)만 지하철 차용(D-54)으로 물러난다. 실측 08시 계수: 평일 1.19(차용은 1.28),
일요일 0.55(차용은 0.29). 토요일은 실측이 없어 지하철 실측의 평일↔일요일 위치로 보간.
재현 사슬: `python pipeline/fetch_tdata_file.py` → `node pipeline/build_tdata_calib.js`
(계수의 분모를 실제 엔진 odLoads 로 만들어야 해서 노드다 — 파이썬으로 다시 쓰면 어긋난다).

**한계** — 평일 표본이 월요일 하루(8/31, 개학일)라 그날의 특이가 계수에 섞인다.
달마다 파일이 갱신되면 다시 받아 날을 쌓는다. 차량 간 편차(평균 대 최대)는 파일에도 없어
D-62 의 ×1.25 는 그대로 남는다.

## D-66. 「가다가 앉기」는 기본 숨김 + 앉게 되는 **정거장 이름**까지 (2026-09-05)

사용자 요구 둘. ① "가다가 앉을 가능성 91%"만 던지면 못 믿는다 — **어디쯤에서** 앉게
되는지(역·정류장 이름)까지 말해야 근거가 선다. ② 사람들이 진짜 궁금한 건 「타자마자
앉느냐」다 — 가다가 앉는 얘기는 중요도가 낮으니 **기본으로는 아예 표기하지 말고**
토글 뒤에 숨겨라.

**어디서 앉는가**: `ride()` 가 못 앉고 탄 사람의 서 있을 확률이 절반 아래로 떨어지는
첫 정거장(**중앙값**)을 `seatAtIdx` 로 내고, `evaluate()` 가 노드 이름을 붙인다
(`leg.seatAtName`). 절반을 못 넘기면 null — 화면은 솔직하게 「내릴 때까지 서 있기
쉽습니다」로 말한다. 확률은 못 앉고 탄 **조건부**(`pLaterCond`)로 말한다 — 앞줄의
「탈 때 앉을 확률」과 앞뒤가 맞으려면 그래야 한다.
실측 예(평일 08시): 100번 탈 때 9% → 「못 앉고 타도 대개 **미아리고개(2정거장 · 약
5분 뒤)**쯤부터는 앉아 갑니다」(길음역 4호선 환승 하차 실데이터가 근거).
**두 가지 추가 규칙**(사용자 지시): ① 예측 확률의 상한은 **90%** — 100% 는 물론 99% 도
너무 높다, "아무리 확실해도 90 정도를 한계로"(`pctLater()`, 시험이 지킨다).
② 이름만으로는 부족하다 — **몇 정거장째 · 약 몇 분 뒤**인지 반드시 같이 쓴다.

**기본 숨김 → 그 자리 펼침으로 진화**: 처음엔 우상단 토글이었는데 "연상이 안 된다"는
지적대로였다 — 정보와 단추가 화면 반대편에 있었다. 지금은 **「못 앉습니다 · 서서 8분」
바로 그 줄 밑**의 「언제쯤 앉게 되나요? ▾」 단추다. 이름도 기능명이 아니라 궁금한 문장
그대로. 기본은 접힘 — 목록·머리글에서는 아예 안 보인다.
`bestOffAt`(많이 내리는 역) 표시는 seatAtName 이 대신하므로 화면에서 뺐다(값은 남긴다).

## D-67. 브라우저 뒤로가기 = 화면의 「뒤로」 (2026-09-05)

한 파일 앱이라 브라우저 뒤로가기가 **페이지 자체를 떠났다** — 어르신이 가장 자주 누르는
단추가 앱을 통째로 닫는 셈. 화면을 넘기는 자리(`nav()`)마다 `history.pushState` 로
한 칸 쌓고 `popstate` 에서 그 화면으로 돌아간다. 같은 화면 재검색은 안 쌓는다
(뒤로가 여러 번 필요해지는 함정). 화면의 「뒤로」 단추도 history 가 쌓여 있으면
`history.back()` 을 타서 두 길이 완전히 같다. 상태가 빈 화면(4인데 고른 경로 없음)은
한 단계 앞으로 접는다. 실측: 4→3→2 뒤로, 2→3 앞으로, 화면 뒤로 3→2 전부 정상.

## D-68. 지도 — 라이브러리 없이 그린 타일 지도 (2026-09-05)

상세 화면 맨 위에 경로 지도. **MapLibre 를 안 넣었다** — 집 규칙(CDN·번들러 금지)대로
캔버스에 직접 그린다(타일 불러오기·웹메르카토르·끌기·확대 합쳐 200줄).
- 타일: **OSM 표준**(tile.openstreetmap.org). 처음엔 CARTO 를 썼는데 이제 키 없이는
  「API KEY REQUIRED」 무늬가 찍힌다(실측). 출처 표기(#mapNote)는 이용약관 — 지우지 말 것.
- 색이 곧 내용이다: **초록 = 앉아 가는 구간, 주황 = 서서 가는 구간**, 점선 = 걷기.
  탈 때 절반 못 앉는 구간은 「앉게 되는 정거장」까지 주황 → 그 뒤 초록으로 갈라 그린다.
- 이름표: 출발·도착 시각, 노선별 탑승 시각, 그리고 **「HH:MM쯤부터 앉을 듯」**(사용자 요구 —
  지도 위에 시각까지).
- 조작은 양쪽 다: 끌기(포인터 이벤트라 마우스·터치 공용) · ＋－ 단추 · 휠 · 화살표 키.
- **캔버스는 화면이 보인 뒤에 재야 한다** — hidden 상태에서 재면 0×0 이라 회색 빈 판
  (실제로 났다). go(4) 에서 한 번 더 그린다.
- 타일이 안 오면(오프라인·file://) 회색 바탕에 선만 — 앱은 계속 돈다.

**같은 날 보강 — "네이버 느낌으로"(사용자).** 키 없이 깨끗한 타일(CARTO voyager·Stadia)은
전부 워터마크/401 로 막혀 있어(실물 확인), 스타일을 그리는 쪽에서 만든다:
① OSM 타일을 그릴 때 `ctx.filter = saturate(.45) brightness(1.07)` 로 채도를 눌러
   파스텔 바탕으로(모르는 브라우저면 원색 — 죽지 않는다)
② 경로선에 **흰 테두리(케이싱)** — 밑에 흰 9px, 위에 색 5.5px. 지도앱 선의 비밀이 이것이다
③ 핀은 이모지가 아니라 CSS 로 직접 그린 **파란 풍선**(기기마다 다른 이모지는 지도앱 티가 안 남)
④ 이름표를 살짝 키우고 굵게.
더 위를 원하면 VWorld(국토부, 키 즉시발급) 타일로 갈아끼우는 길이 있다 — 같은 엔진에 URL 만 바꾸면 된다.
**2026-09-05 사용자 결정: 당분간 지금 지도 유지.** 카카오·네이버 SDK 는 도메인 등록으로
경쟁사 시스템에 흔적이 남는 게 싫다는 판단(사양서 11장의 경쟁 구도와 일관된 걱정) — 
**다시 SDK 를 권하지 말 것.** 바꾸게 되면 1순위는 VWorld(경쟁사 신호 0)다.

## D-69. ~~PC 는 PC 답게~~ → **철회** (2026-09-05, 같은 날)

넓은 화면에서 판 1040px + 목록 두 칸을 만들었다가 **당일 철회했다** —
사용자: "PC 버전 엄청 구리다. 따로 만들지 말고 폰 레이아웃 그대로 유지해라."
두 칸 격자는 카드 높이가 제각각이라 시선이 지그재그로 튀었다. 한 줄이 읽기 흐름에 맞다.
지금은 화면 폭과 무관하게 같은 판(max-width 760px)이다. **다시 만들지 말 것.**

## D-70. 지도에서 고르기 — 콕 찍기가 아니라 「움직여 맞추기」 (2026-09-05)

첫 화면의 출발·도착 옆 「지도」 단추 → 전체 화면 지도가 열리고, **지도를 움직여 가운데
📍에 맞춘 뒤 「이 위치로 정하기」**를 누른다(네이버·카카오와 같은 문법).
콕 찍기보다 손이 떨려도 되는 방식이라 어르신에게 맞다.

- 지도는 D-68 의 부품을 그대로 쓴다 — 이번에 `makeSlippy()` 로 부품화해서
  상세 경로 지도와 고르기 화면이 같은 엔진이다(따로 두면 한쪽만 고치게 된다).
- **이름은 두 겹**: 근처 정류장·역 이름(우리 자료 — 오프라인에도 됨)을 즉시 보여 주고,
  도로명 주소(Nominatim 역검색, 3초 시한)가 도착하면 「동숭3길 · 혜화역1번출구 부근」처럼
  겹쳐 보인다. 좌표는 브라우저가 OSM 에 직접 묻는다 — 우리 서버로는 아무것도 안 간다.
- 움직임이 멎고 350ms 뒤에 한 번만 묻는다(끌는 동안 매 프레임 묻으면 차단당한다).
- 서울 밖을 고르면 확정 대신 안내(다른 입력 경로와 같은 규칙).
- **브라우저 뒤로가기 = 취소** — 고르기 화면이 열려 있으면 화면 이동보다 먼저 그것만 닫는다.

## D-71. 한 카드 안에서는 잣대가 하나다 — 그리고 산수가 닫혀야 한다 (2026-09-05)

**증상(사용자 검산).** 「서서 가는 시간 8분」 옆에 「타는 시간의 28%는 앉아 갑니다」 —
전체 64−걷기 15 = 49분의 72%면 35분을 서야 하는데 8분이라니, 한 카드가 자기모순.

**원인.** 두 숫자의 잣대가 달랐다. 8분은 **가다가 앉는 것까지 포함**한 기대 서는 시간,
28%는 **탈 때 기준**(pBoard) 가중평균 — 가다가 앉은 시간을 「앉음」으로 안 쳐줬다.

**판단 셋.**
① 카드의 큰 퍼센트는 머리기사와 **같은 잣대**(pSeatedTime = 1 − 서서/타는)로 —
   여정은 「타는 시간의 N%는 앉아 갑니다」(**비율**), 구간은 「탈 때 앉을 **확률** N%」.
   확률과 비율을 섞지 않고 낱말로 가른다.
② **분을 병기**해 검산이 눈으로 되게 — 「(타는 36분 중 28분)」. 반올림은 **한 번만**:
   앉는분 = 반올림(타는) − 반올림(서서). 따로 반올림하면 합이 1분 어긋난다(실제로 났다).
③ 전체 시간의 산수도 카드에서 닫는다 — 「전체 64분 = 걷기 15 + 기다림 13 + 타기 36」.
   기다림이 안 보이면 "64−15=49인데?"가 된다(사용자가 정확히 그렇게 물었다).

시험이 지킨다: 랭킹 전 경로에서 (1−비율)×타는시간 ≡ 서서시간(±0.5분), 그리고
카드 퍼센트가 pSeatedTime 에서 다시 벗어나면 실패.

## D-72. 자리는 차가 **비워질 때** 난다 — 하차가 아니라 순하차 (2026-09-05)

**증상(사용자: "진짜 중간에 앉아가는 거 맞냐? 후기 확인해봐").** 화면이 「못 앉고 타도
대개 미아리고개(3정거장)쯤부터는 앉는다」고 말했는데, 실사용 후기(나무위키 100번)는
「출퇴근 시 **동소문로에서 입석은 기본**, 심하면 도착할 때까지 서서」 — 정확히 그 구간이다.

**원인.** 하차 실측이 큰 정거장(길음역·돈암)을 자리가 나는 곳으로 셌다. 그런데 그 하차는
대부분 지하철로 갈아타는 **서서 가던 단거리 승객**이라 좌석을 안 비운다. 게다가 내린 만큼
새로 탄다 — 우리 자신의 OD 재차도 25→25→22→22 로 **순감소가 거의 0**(갈아타기 회전문).
D-59 는 승차를 분모에 더하는 손질이었는데, 회전문의 본질(하차≠좌석)을 못 잡았다.

**판단.** 자리의 분자를 **순하차 = max(0, 하차 − 승차)** 로. 차가 실제로 비워지는 곳
(6호선 이태원·한강진, 100번의 혜화 이후 도심 하차)에서만 자리가 난다 — 후기와 일치한다.
승차는 분자에서 뺐으므로 분모에 다시 더하지 않는다(이중 벌점 방지). 분모의 조건부 재차
(k=좌석×0.11, D-59 의 살아남는 절반)와 α·상한(사양서 5.2)은 그대로.

**실측 변화(평일 08시 월곡→중구청):** 100번 구간 서서 8→**12분**, 자리 예고
미아리고개(3정거장) → **혜화동로터리(6정거장)** — 동소문로를 서서 통과. 순위도 재편
(1017→2호선이 1위로). 6호선 월곡→삼각지의 「이태원쯤부터 앉음」은 그대로 —
그쪽은 진짜로 비워지는 구간이라 규칙이 구분해 낸다. 회귀 시험: 회전문(하차=승차)이면
자리 확률 0, 비워지면 >0.

## D-73. 확률 표기의 상한은 90 — 그리고 「발로 버티는 시간」 합산 (2026-09-05)

**"이거 진짜 97%로 앉는 거 맞냐?"** — 검증부터 했다: 맞다. 202번은 신당 직전
**상왕십리역에서 재차 24→13명으로 뚝 떨어진다**(2호선 환승 대량 하차 = D-72 의 「진짜
비워짐」). 왕십리 구간 만석(31명)이던 차가 신당에선 빈자리 9석으로 온다 — 데이터가
말이 된다. 그러나 사용자 판단: **"90% 초과는 표기하지 마라. 틀릴 수가 너무 높다."**

맞는 원칙이다 — 예측이 97·100 을 입에 담으면 한 번의 반례로 신뢰 전체가 무너진다.
`seatChance()` 가 **표기만** 90 에서 자른다(계산·정렬·다섯 단계 분류는 원값).
가다가 예측(pctLater, D-66)은 이미 90 상한 — 이제 확률 표기 전부가 같은 상한이다.
「앉아 가는 대중교통 위치」의 배너 「앉을 확률 95% 이상」도 숫자를 뺐다(모순 방지).

**머리기사 합산**(같은 지시): 「서서 가는 시간 10분 **(+ 걷기 22분 · 총 32분 걷거나
서 있음)**」 — 무릎이 아픈 사람에게는 걷기도 버티는 시간이다. 목록·상세 양쪽.

## D-74. 기다림도 서 있는 것이다 — 그리고 「서는 시간·걷는 시간」 (2026-09-05)

**사용자 지시 셋.** ① 문구를 「서는 시간」「걷는 시간」으로. ② 정류장·승강장에서
기다리는 시간은 **기본적으로 서 있는 것으로** 센다(벤치는 복불복이니 없다고 가정하는 쪽이
정직하다). ③ 바로 그 옆에 「앉아서 기다림」으로 바꾸는 단추를 둔다.

**구현.** `evaluate()` 가 `vehicleStandingMinutes`(차 안 몫)와 `waitMinutes` 를 따로 남기고,
`standingMinutes = 차 안 + (가정에 따라) 기다림`. **정렬 키가 이 값이므로 가정을 바꾸면
순위까지 다시 선다** — 기본값에서는 기다림 짧은 지하철 조합이 위로 올라온다(실측: 월곡→중구청
1위가 1113→100 에서 6호선→202 로). 전환 단추(`기다릴 때: 서 있음 ⇄`)는 목록 머리와 상세
머리기사 옆 두 곳, 누르면 다시 계산·정렬하고 보던 경로를 다시 찾아 보여 준다.

**잣대 관리(D-71 유지).** 「타는 시간의 N%」와 그 괄호(타는 18분 중 8분), 「언제쯤 앉나」는
**차 안 이야기**이므로 차 안 몫으로 계산한다 — 여기 기다림을 섞으면 다시 자기모순이 된다
(실제로 섞였다가 「18분 중 1분」이 나와 바로 잡았다). 머리기사 괄호가 분해를 보인다:
「서는 시간 17분 (차 안 10분 + 기다림 7분 · 걷는 시간 22분 따로 → 총 39분 걷거나 서 있음)」.

시험: 가정 켬/끔의 차이가 정확히 기다림과 같다 · 차 안 몫과 비율은 가정에 안 흔들린다 ·
배지·문구 「서는」 정합. 124개 통과.

## D-75. 머리기사 확정 레이아웃 — 앉는/못 앉는/이동 세 줄 (2026-09-05, 당일 두 번 다듬음)

한 줄 네 지표로 갔다가 사용자가 최종 형식을 못 박았다:

    앉는 시간 8분
    못 앉는 시간 39분 (서는 시간 17분 + 걷는 시간 22분)
    ────────────────
    이동시간 47분

**앉는 시간은 이동 − 못 앉는 으로 유도**한다 — 세 줄의 합이 반올림 후에도 정확히 맞고(D-71
정신), 「기다림을 앉아서」 가정이면 그 몫이 자동으로 앉는 시간에 들어간다.
앉는 시간은 항상 보이고, 「못 앉는 시간」 「이동 시간」 두 줄은 목록 위 「보기」 칩으로
끈다 — 끄면 모든 카드·상세에서 함께 사라지고 선택은 저장된다(`seat_show_*`).
분해 소줄 「(차 안 10분 + 기다림 7분)」과 전환 단추는 그 밑에 그대로.
칩 배선은 한 줄씩 명시 — ui.test 정적 배선 검사가 눈으로 보게.

## D-76. 퍼센트는 화면의 분에서 유도한다 (2026-09-05, 사용자 검산 2호)

「타는 시간의 75%는 앉아 갑니다 (타는 18분 중 14분)」 — 14/18 = 78% 인데 75%?
원인: 분은 반올림-유도(D-71, 합이 닫히게)인데 퍼센트는 반올림 **전** 원값(4.4/17.8)에서
나왔다. 같은 문장의 두 계산이 딴소리를 했다.

**규칙: 화면의 퍼센트는 화면의 분에서 다시 계산한다** — `pSeatedShown = (반올림 타는 −
반올림 차안서기) ÷ 반올림 타는`. 괄호와 퍼센트가 어긋날 길이 구조적으로 없다.
원값(pSeatedTime)은 검증·시험용으로 남는다. 불변식 시험: 퍼센트 ≡ round(100×괄호분자/괄호분모).
실측: 전 카드 44%=8/18 · 45%=9/20 · 0%=0/14 · 48%=13/27 — 전부 일치.

교훈(D-71 과 짝): **한 문장 안의 숫자는 한 계산에서 나와야 한다.** 표시용 반올림을 했다면
그 뒤에 오는 파생값도 반올림된 값에서 유도한다.

## D-77. 정렬 = 못 앉는 시간, 0%·100% 는 문장으로 (2026-09-05, 사용자 지시)

**정렬 잣대 교체.** 「서는 시간이 짧은 순」은 반쪽이었다 — 서는 시간만 재면
「서기 3분 + 걷기 30분」이 「서기 8분 + 걷기 5분」을 이긴다. 발로 버티기는 매한가지다.
정렬 키를 **못 앉는 시간(서는 + 걷는)** 으로 바꾼다 — 카드 머리기사(D-75)의
「못 앉는 시간」과 같은 잣대. 배지는 「못 앉는 시간이 가장 짧은 길」.
부수 효과: 걷기만 하는 경로가 공짜 1위(서기 0분)를 먹던 것도 같이 고쳐졌다.

**비율 문장의 양 끝.** 「타는 시간의 0%는 앉아 갑니다」는 어색하다(사용자) →
「타는 동안 못 앉아 갑니다」. 그리고 길음→시청 첫 카드가 「타는 시간의 **100%**는
앉아 갑니다」라고 말하고 있었다 — 백 프로 절대 금지(D-73) 위반. 90 초과는 숫자를
입에 담지 않고 「웬만하면 타는 내내 앉아 갑니다」로 말한다. 괄호의 분수(타는 N분 중
M분)는 그대로 나가므로 산수는 닫힌 채, 문장에서만 확신을 뺀다. 양쪽 다 label 을
비운다 — 붙이면 「못 앉아 갑니다 · 못 앉습니다」처럼 같은 말이 두 번이다.

교훈: 표시 상한(D-73)은 %가 나가는 **모든** 문장에 걸어야 한다 — 새 문장을 만들 때마다
상한을 다시 심으면 하나는 꼭 빠진다.

## D-78. 배지 문구는 「서는 시간이 가장 짧음」 (2026-09-05, 사용자 결정)

D-77 의 배지 「못 앉는 시간이 가장 짧은 길」이 길고 부정적이다 → 사용자가
**「서는 시간이 가장 짧음」** 으로 정했다. **정렬은 못 앉는 시간(D-77) 그대로다.**
정렬의 정확한 문장은 목록 머리의 「못 앉는 시간이 짧은 순으로 놓았습니다」가 담당하고,
배지는 친근한 이름표다. 이론상 1위가 서는 시간만으로는 1위가 아닌 경우가 있을 수 있으나
(걷기가 정렬에 드므로), 실제 후보들의 걷기 차가 작아 드물다 — 문제되면 그때 다시 본다.

## D-79. 혼잡 원천의 빈 행·동명이역 — 성수 「앉을 확률 90%」의 뿌리 (2026-09-05, 사용자 검산 3호)

「여기서 타면 앉습니다」가 **성수역 2호선 시청 방면 평일 08시**를 「탈 때 앉을 확률 90%」로
내놓았다(사용자: "못 앉아갈 때가 많았던 것 같은데"). 파 보니 코드가 아니라 자료였다:

- **원천(지하철혼잡도 OA-12928)에 「성수」가 두 역이다** — 본선(역번호 211)과
  성수지선(9002). 수집기가 이름으로만 키를 만들어 **나중 행이 앞 행을 덮어썼다**
  (본선 내선 42.2% → 지선의 0). 신도림(234/9003)도 같은 병 — 이쪽은 본선 외선이
  0으로 덮여 **「그 시각에는 열차가 다니지 않는다」**가 되어 있었다.
- **전부 0인 행이 41개** — 종점의 없는 방향과 지선 접점의 빈 방향이 0으로 온다.
  0은 값이 아니라 빈칸이다. 값으로 읽으면 notRunning 이 된다.
- **본선 성수의 외선(시청 방면) 행은 원천에 아예 없다**(전부 0). 그래서 남은 지선 행
  15.6%(재차 25명)가 「성수 외선」 노릇을 했다 — 화면의 「54자리 가운데 29자리」가 그 숫자다.

수술 셋: ① 전부 0인 행은 버린다(빈칸이므로), ② (호선, 역이름)의 주인을 **가장 작은
역번호**로 한 번만 정하고 다른 역번호의 행은 전부 버린다, ③ 방향 값이 없는 역은
엔진이 **같은 방향 이웃 역**(가는 방향 앞쪽 우선 — 뒤쪽은 지선 이어붙임 경계일 수 있다)
값으로 메운다(estimated 표시). 결과: 성수→시청 08시 재차 83명(뚝섬 52%로 메움),
탈 때 앉을 확률 1%. 웹 교차확인: 내선순환=시계방향, 성수→시청=외선(한국일보·나무위키).

## D-80. 지하철 그래프 기하 수술 — 좌표 세 함정과 지선 분리 (2026-09-05)

D-79 김에 **출근 상식 자동검증**(`tools/verify_rush.js`, 아래)을 만들어 전수 훑기를 했더니
그래프 자체의 병이 드러났다. 역 좌표는 「◯◯역」 버스정류장에서 빌리고 순서는 역번호인데:

- **동명 딴 곳**: 「종합운동장역」 정류장은 전부 **안양**에 있다 — 2호선 종합운동장이
  16.9km 밖에 있었다(잠실새내↛종합운동장 인접거리로 발각). 「남한산성입구」는 산 입구
  등산 정류장(4.4km 북쪽)을 빌렸다. → 후보를 무리 지어 번호 이웃 가운데·호선 중심에
  맞는 무리만 쓰고, 그래도 어긋나면 이웃 가운데로 되돌린다(반복 검사).
- **정규화 통 섞임**: 「미아역」이라는 정류장은 없고, 정규화(canon)가 「미아사거리」의
  꼬리를 떼어 「미아」로 만든다 — 미아가 미아사거리 위에 앉아 **두 역이 한 노드**가 됐다.
  옛 평균 방식은 우연히 그 사이에 떨어져 들키지 않았을 뿐이다. → 무리 고르기 1순위는
  **번호 이웃의 가운데에서 2km 안**인 무리.
- **번호는 지리가 아니다**: 나중에 생긴 역은 큰 번호를 받는다(동묘앞 159 → 배열 맨끝,
  남위례 2828). 지선은 본선 뒤에 이어붙고, 성수지선 용두는 250번이라 **신정지선 번호
  사이**에 있다. 그대로 두면 길찾기가 없는 선로를 태운다(신답→강남 22정거장 제안 실측).
  → 큰 틈(3.2km)에서 자르고, 홑 역은 본선의 가장 싼 틈에 복귀시키고(남위례·강일),
  덩어리는 **최근접 사슬**로 다시 엮어 지선으로 등록한다. 접속역은 기하로 못 가른다
  (둔촌동은 실제 접점 강동보다 길동에 가깝다) — **원천에 두 번 적힌 역**(강동 2549 두 줄,
  성수 211/9002, 신도림 234/9003)을 접점 힌트로 쓴다.

결과: 인접 3.2km 초과 0곳, 지선 4개(신도림~까치산 · 성수~신설동 · 강동~마천)가 실제
선형과 일치, 8호선은 한 줄로 복원. 남은 어림: 용답(좌표 원천 없음 — 뺐다), 남위례가
산성과 한 칸 자리바꿈(계산 무영향), 성남 꼬리 일부는 보간 좌표(오차 ~1km).

**상설 검증**: `node tools/verify_rush.js` — ① 전 역 08시 훑기 ② 호선별 출근 비대칭
(9개 앵커: 미아 하선 165 vs 상선 24 등) ③ 통근 20쌍 길찾기 상식 대조 ④ 앉는자리 화면
도심 4곳 전수. 굳은 사실은 calibration.test.js 에 못 박았다(130개).
남은 일: 8101 같은 출근전용 맞춤버스의 **운행시간대**를 엔진이 모른다 — 노선정보조회
API 의 firstBusTm/lastBusTm 이 000000(미제공)이라 다른 원천이 필요하다.

## D-81. 심층 검증 2차 — 퇴근·심야·무작위·몬테카를로 (2026-09-06, 사용자 지시)

`tools/verify_deep.js` 를 만들어 아침 훑기(D-80)보다 넓게 돌렸다:
퇴근(18:30) 비대칭 9앵커 · 시간 곡선(05~23시, 출근 방향은 아침 봉우리 / 퇴근 방향은 저녁
봉우리) · 요일 곡선 · **무작위 40쌍 × 3시각 = 여정 1,135개 불변식 전수** · 심야/새벽 ·
극단(사당→방배 재차 204명 0%) · **몬테카를로 4,000판 대 해석해**(성수→시청 43% vs 44%,
미아→동대문 4% vs 3%, 화곡→여의도 2% vs 2% — 좌석 모델이 시뮬레이션과 일치).

무작위 1,135개 여정에서 불변식 위반 0. 대신 심야에서 다섯을 잡아 고쳤다:

1. **02시에 지하철이 살아 나왔다** — outOfRange 를 「자료 가장자리 값 + 딱지」로 눙치고
   있었다. 혼잡도 자료 범위(05:30~24:30)가 곧 운행 시간이다: 40분 넘게 벗어나면
   notRunning. 자정 직후(00~03시)는 하루의 연장으로 계산해 00:30 막차 시간대는 살아 있다.
2. **02시에 공항·간선버스가 살아 나왔다** — 낮 버스는 01:30~04:00 공백에 notRunning.
   (노선별 첫·막차는 API 가 000000 이라 공통 공백만. 8101 낮 시간은 여전히 남은 일.)
3. **심야버스가 제 시간대에 죽었다** — 승하차 표본이 얇은 N15·N16이 「승객<1 = 안 다님」
   규칙(낮 유령 노선용)에 걸렸다. 심야 창의 night 노선만 면제.
4. **걷기 경로가 상위 12개 자르기에 밀려났다** — 600m 거리에서 차편 12개가 「9분 걷기」를
   밀어냈다. 걷기는 잘려도 한 자리를 얹어 준다.
5. **화면 구멍 둘** — 시간 선택지가 05~24시뿐이라 새벽을 물을 수 없었다 → 「새벽 1~4시」
   (25~28 = 하루의 연장, 엔진과 같은 표현) 추가. 그리고 안 다니는 시각의 목록 카드가
   경고 없이 멀쩡한 척 섰다(경고는 상세에만 있었다) → 카드 맨 위 경고 + 전멸이면
   머리말 「이 시각에 다니는 차를 못 찾았습니다 — 참고용」.

검증기 자신의 오류도 하나 고쳤다: 저녁 봉우리를 출근 방향에 묻고 있었다 — 미아→충무로의
저녁은 역출근이라 한산한 것이 맞다. 저녁 봉우리는 퇴근 방향(역방향)에서 본다.

## D-82. 근본 원인 분석 — 왜 결함이 이렇게 많이 나왔나 (2026-09-06, 사용자 질문)

「정형화된 객관적 혼잡도 API 를 받았는데도 왜 이렇게 문제가 많나」에 대한 답.
결정 80건 중 결함에서 태어난 ~45건을 다섯 뿌리로 가른다.

**① 「정형화」는 형식의 약속이지 의미의 약속이 아니다 (~18건).**
공식 파일은 칸이 반듯할 뿐, 의미 규약은 사람 열람 관행 속에 있다 — 문서 어디에도 없다.
실측한 것만: 전부-0 행 41개(빈칸의 뜻), 같은 이름 두 역(성수 211/9002), 한 번호 두 이름
(강동 2549), 역번호 ≠ 지리 순서(동묘앞 159·용두 250), 본선 성수 외선 행 부재, 분기마다
머리글 형식 변경(D-18), 좌표는 아예 미제공(그래서 정류장에서 빌리다 안양 종합운동장을
집었다), API 의 firstBusTm=000000, 문서와 다른 필드명(T-DATA), 2024 이후 배차열 삭제.
사람이 표를 읽을 땐 전부 문맥으로 넘어가는 것들이고, 기계 재해석에선 전부 지뢰다.
**우리 일의 본질이 「다른 목적으로 만든 원천 6개를 좌석 예측으로 재해석」이라
지뢰 수 ∝ 원천 수 × 암묵 규약 수**다.

**② 원료가 답이 아니다 (~12건).** 혼잡도(재차%)는 「내가 어디쯤 앉나」에 직접 답하지
않는다. 방향(7배 차)·요일×시간·노선별 배차·감쇠·순하차·기다림… 유도 사슬의 고리마다
모델 결정이 있고, 결정마다 틀릴 기회가 있다. 이건 버그가 아니라 **모델이 현실의 결을
따라잡는 정상 과정**이고, 각 고리는 이제 실측으로 고정돼 있다(위 표).

**③ 정답지가 없는 문제라 오류가 조용하다.** 「그때 그 역에서 앉았나」의 실측 로그는
세상에 없다. 그래서 오류는 죽음(예외)이 아니라 **그럴듯한 숫자**로 나타난다 — 컴파일러도
단위시험도 못 잡고, 상식 대조만 잡는다. 그 상식 대조를 사람(사용자 검산 3회)이 하다가
이번 주에야 자동화했다(verify_rush·verify_deep·simulate_hour). **결함이 「계속 나오는」 게
아니라, 검출기가 생기자 과거의 재고가 일괄 발굴된 것이다.** 증거: 검출기 완비 후 여정
100만+ 개 재검에서 실코드 결함 1건(반올림 반 점)뿐.

**④ 죽지 않는 설계의 대가.** 「자료가 없어도 앱은 돈다」(폴백 사다리)는 집 규칙인데,
폴백은 오류를 숨긴다 — 성수 사건이 정확히 그것(지선 값이 조용히 본선 행세). 그래서
ctx.stats 층 계기를 달았다: 실측 **방향값 98.7% · 이웃메움 0.9% · 역최대 0.4% · 호선피크 0%**.
이제 폴백 비율이 튀면 자료 회귀를 그 자리에서 본다.

**⑤ 검출기를 새로 만들면 검출기의 오류가 먼저 나온다.** 오늘 깃발의 다수는 하네스 자신
이었다 — 몬테카를로 질량 보존 2판, 요동 규칙, 검색시각/탑승시각 혼동, 계기 오분류.
검증 도구도 코드다. 그래서 하네스의 판정 근거도 실측으로 못 박으며 간다(중계 절벽
27→12→2 단조 실측, 8101 일 1,100명 실수요, 김포공항 기점 28%).

**앞으로의 방어선**: 시험 132개(회귀) + 상설 검출기 3종(유입 시점 검출) + 층 계기(조용한
폴백 관측) + 금요일 재차 실측 재보정(정답지의 첫 조각) + M4 피드백 α(궁극의 정답지).

## D-83. 앉는 자리 화면 진위 판정 + 경로 유형 딱지·필터 (2026-09-06, 사용자 질문·지시)

**「진짜 맞아?」 판정.** 중구청 08시의 「탈 때 90%」 행렬을 카드별로 해부했다 — 전부 실계산이다:
01B 대당 7.3/15석(남산 셔틀 기점 2번째), 105 동대문역사문화공원 14.6/23석(기점서 35/44번째 —
종점 꼬리), 421 남대문 14.7/23석(30/72번째). **이 화면의 본질이 「종점 꼬리·역방향·한산
노선」을 일부러 찾는 것**이라 90%가 줄지어 나오는 게 설계다. 갓 시작한 실측 수집(공식
혼잡도 단계)도 같은 말을 한다: 금요일 퇴근 18시조차 152번 차량의 55%가 「여유(=좌석에
앉을 수 있는 정도)」(79여유/59보통/6혼잡) — 러시라도 노선 어딘가엔 앉는 지점이 있다.
남은 의심(463 여의도행 등 정방향 카드의 대당 재차)은 **월요일 아침 실측이 쌓이면 자동
대조**한다(금요일 재보정 범위).

**경로 유형 딱지·필터.** 네이버 길찾기의 탭을 참고해 목록 카드마다 색+글자 딱지
(🚇 지하철 / 🚌 버스 / 🚇+🚌 지하철+버스 / 🚶 걸어서)를 붙이고, 목록 위 탭(전체·유형별,
개수 표기)으로 일부 유형을 숨길 수 있게 했다(단일 선택, `seat_typefilter` 저장).
있는 유형만 탭으로 만들고(두 종류 미만이면 탭 자체를 숨김), 걷기 카드는 어느 탭에서든
보여 준다 — 짧은 거리의 가장 정직한 답이라서. 색만으로 가르지 않는다(사양서 7.3).

## D-84. 안내 문구도 「서는 시간」, 개발용 성능 표시 제거 (2026-09-06, 사용자 결정)

목록 머리 「못 앉는 시간이 짧은 순」 → **「서는 시간이 짧은 순」** — 배지(D-78)와 같은
사용자 어휘 결정. 정렬 잣대(못 앉는 시간 = 서는+걷는, D-77)는 그대로다.
「(12개 · 8ms)」 같은 개수·계산시간 표시는 개발용 잔재라 목록·앉는자리 두 화면에서
지웠다 — 어르신 화면에 셈 속도는 소음이다.

## D-85. 수도권 확장 1단계 — 경기·인천 전철 28개 노선 (2026-09-06, 사용자 요청)

「경기도까지 대중교통 더 필요해」. 원천은 **전국도시철도역사정보 표준데이터**(국가철도공단,
KRIC 레일포털 직다운·키 불필요, 2026-06-30판, 1,100역) — `pipeline/fetch_metro_standard.py`.
신분당·분당·수인·경의중앙·경인·경부·경원·안산과천·일산·경춘·경강·서해·공항철도·9호선·
김포·신림·우이신설·의정부·에버라인·인천 1·2호선 등 **28개 노선 796역**이 들어왔다.

- **좌표가 파일에 직접 있다** — 정류장 빌리기(D-80의 함정)가 통째로 사라진다. 덤으로
  1~8호선의 「좌표 못 찾은 역」(지축·용답·여의나루·하남 4역)도 이 좌표로 구제했다 —
  지축이 살아야 일산선이 3호선과 클러스터로 이어진다.
- **역번호는 노선 내 순번**이되 계통이 섞인 노선이 있다(경의중앙에 서울역 지선 번호가,
  수인선에 두 계통이). 광역은 12km 초과 틈에서 잘라 긴 토막을 본선으로, 나머지는 최근접
  사슬로 엮어 별도 계통 노선으로 둔다(경의중앙선(청량리~지평)·수인선(달월~고색) —
  실제 운행 계통과 부합, 연결은 환승 클러스터). 실제 인접 최대는 공항철도 청라~영종 10.1km.
- **역간 시간은 좌표로 만든다** — 인접거리 평균 ÷ 표정속도 34km/h (도시철도는 2.0분 유지).
- **혼잡·승하차 자료는 아직 없다** — 집 규칙(D-25) 그대로 「모름 = 서서 간다」로 계산되고
  화면이 「혼잡 자료가 없어 알 수 없습니다」라고 밝힌다. 광역 노선은 `wide` 표지를 달아
  위생 시험(인접 12km)·방향 라벨 시험에서 따로 다룬다.
- **탐색 두 곳을 손봤다**: ① 2환승 관문 60→150 — 외곽은 버스 1환승 조합만으로 원시 88개가
  차서 지하철 등뼈가 통째로 생략됐다(주엽→시청이 버스 130분). ② 2환승 순회를 「지하철
  먼저·길게 타는 것부터」로 — 200 한도를 버스 잔가지가 먼저 채우면 정작 등뼈가 차례를
  못 받는다. 실측: 주엽→시청 72분(일산선→3호선), 부평→서울역 69분(인천1→공항철도),
  수원→강남 117분(경부선 완행→2호선), 서울 기존 경로 전부 불변, 시험 133개.

**남은 어림(정직하게)**: 경의중앙 본선에 용산선 일부 역 순서 끼임, 8호선 별내·7호선
인천 연장은 「(연장)」 별도 노선(직결인데 환승 1회로 셈), 진접선 3역만 분리, 급행 미구분.

**다음 단계**: ① 코레일 광역 역별 승하차 → 광역 좌석 추정(odLoads 방식) ② 9호선 혼잡도
(서울열린데이터 승하차는 이미 9호선 포함) ③ 경기 GBIS 버스(정류소·노선 파일데이터 확인됨).

## D-86. 그물 잇기·문구 총칭·종류 평균 어림 (2026-09-06, 사용자 지시 셋)

**① 끝점 접속 — 광역과 도심이 실제로 이어졌다.** 표준데이터는 직결 구간을 운영사 경계로
갈라 놓는다(안산과천선이 선바위부터, 경인선이 개봉부터, 경의중앙 끝 「서울」과 1호선
「서울역」은 좌표 180m 차로 클러스터를 놓침). 규칙: 노선의 양 끝 노드를 딴 노선이 안
나누면 **2.4km 안에서, 아직 이 노선이 어디서도 만나지 않는 노선 무리별로 최근접 1곳씩
(최대 2곳)** 끝에 이어붙인다. 「이미 만나는 무리 제외」가 핵심이다 — 4호선은 사당에서
2호선을 만나는데 방배를 또 붙여 배열이 꼬였고(사당,남태령,사당,선바위 실측), 경인선은
온수에서 7호선을 만나는데 광명사거리가 자리를 차지해 정작 구로(경부선·2.27km 직결)가
밀렸다. 결과 26곳 접속: 남태령↔선바위, 개봉↔구로, 별내선↔구리, 9호선 두 운영사 구간
병합(canon 의 겹공백 정규화), 서울역 3중 연결. 실측: 산본→미아사거리 76분(안산과천→4호선),
성북10 마을버스 → 미아사거리 21분 2위 등장. **직결을 한 노선으로 완전 통합(환승 0회
표기)과 구간별 역간 시간(경부선 도심 구간이 4.8분/역으로 과대)은 남은 일.**

**② 카드 총칭 「서는 시간」 (사용자 결정).** 둘째 줄이 「못 앉는 시간 N (서는 시간 A +
걷는 시간 B)」였는데 → **「서는 시간 N분 (자리 없음 A분 + 걷는 시간 B분)」**. 발로 버티는
전부가 「서는 시간」이고, 그 속의 차내·대기 몫이 「자리 없음」이다. 정렬 안내(D-84)·배지
(D-78)와 어휘가 한 줄로 선다. 보기 칩 이름도 같이 바꿨다.

**③ 종류 평균 어림 (사용자 지시).** 승하차 자료가 없는 버스(마을 24개, 앞으로의 경기
버스)를 「알 수 없음」으로만 두지 않는다 — `pipeline/build_kind_load.js` 가 자료 있는
666개 노선으로 **종류×시간대 「대당 평균 재차」 중앙값**(엔진과 같은 셈: OD 배분 ÷ 시간당
대수, 평일 원값)을 만들고, 런타임은 요일 계수·승객 가중(×1.25)을 똑같이 얹는다.
실측 중앙값: 마을 08시 6.1명 · 간선 8.9명 · 심야 03시 3.2명. 어림은 카드에 어림이라고
밝힌다(「같은 종류 버스들의 시간대 평균으로 어림」).

## D-87. 직결 통합·노선 색 도트·버스 종류 표기 (2026-09-06, 사용자 지시)

**직결 통합 — 「미아사거리→산본」이 4호선 한 줄이 됐다.** 표준데이터는 운영사 경계로
노선을 가르지만 열차는 갈아타지 않는다. 공지된 직결 계통만 표로 적는다(수십 년 안 바뀌는
사실이라 정당): **4호선**=진접선+4호선+안산과천선(진접~오이도 51역) · **3호선**=일산선+3호선
(대화~오금 44역) · **수인분당선**=분당선+수인선(왕십리~인천 97역) · **1호선**=1호선+경부선+
장항선(서울~신창 55역, 경인선은 「1호선(인천 방면)」으로 개명). 실측: 미아사거리→산본
**4호선 단독 84분·환승 0**.
- 조각 잇기는 끝점 거리 탐욕(뒤집기 포함), **이음매 3.8km**(진접선 당고개~별내별가람
  3.3km·수인분당 매교~고색 3.6km 가 실제 한 구간), 못 이은 조각은 지우지 않고 남긴다
  (경원선은 내부 순서 문제로 보류 — 자체 이름으로 존속). 씨앗을 잘못 잡고 미접속 조각을
  지우던 첫 판은 4호선을 진접선 3역으로 만들었다 — 병합은 「씨앗=공사 조각, 흡수한 것만
  삭제」가 철칙이다.
- **코레일 구간엔 혼잡도가 없다** — 그 역이 낀 구간은 noCong 표로 호선피크 후퇴를 막고
  통째로 「모름 = 서서」(D-25)로 둔다. 산본 낮 시간에 4호선 피크값을 씌우면 없는 만원을
  지어내는 셈이다. 도심 안 구간(미아~사당)은 혼잡도를 그대로 쓴다.
- 광역 끝점 수리 문턱 8km→3.2km(일산선 머리의 원흥이 직결 이음매를 막았다).

**노선 색 도트·버스 종류 (사용자 지시).** 검색 자동완성부터 역에는 **공식 노선색 동그란
도트+호선명**, 정류장에는 지나는 버스 **종류 딱지**(간선·지선·마을·광역·심야·순환)를
붙이고, 결과·상세의 노선 알약도 노선색/종류색으로 칠한다(글자색은 밝기로 자동, 색만으로
가르지 않는다 — 글자 항상 동행). 색표는 공지 상징색 상수(LINE_COLORS·KIND_META).

**남은 일**: 경원선 직결 편입(내부 순서 수리 후), 구간별 역간 시간(경부선 도심 4.8분/역
과대), noCong 구간의 부분 평가(아는 구간만이라도 혼잡 반영).

## D-88. 광역시 확장 1단계 — 부산·대구·광주·대전 도시철도 (2026-09-06, 사용자 요청)

「대도시로 확대는 어렵나? 광역시 위주로」. 이미 받아 둔 전국 표준데이터에 다 있었다 —
이름 필터를 서비스권 상자 5개(수도권·부산울산김해·대구경산구미·광주·대전)로 바꾸는
반나절 작업. **+13개 노선**: 부산 1~4호선·부산김해경전철·동해선(부전~태화강),
대구 1~3호선·대경선, 광주 1호선, 대전 1호선. 실측: 서면→해운대 32분, 노포→다대포 69분,
반월당→동대구 17분, 정부청사→대전역 17분, 금남로4가→광주송정 25분 — 전부 환승 0.

- **「서울 밖」 가드가 이미 거짓말이었다** — 경기 확장(D-85) 뒤에도 서울 상자 하나로
  산본·정자를 「서울 밖」이라 했다. `SeatGeo.AREAS`(상자 5개)와 `areaOf()`로 일반화,
  문구는 「서비스 지역(수도권·부산·대구·광주·대전) 밖」.
- **동명 지명이 지역을 넘본다** — '중구청'을 치면 **대전 중구청역**이 정확일치 1위가 되어
  시험 4개가 무너졌다(월곡→대전을 찾고 있었다). 검색 상세에 지방 권역을 밝힌다
  (「지하철역 · 대전」). 시험 표본도 서울 정확명·제주 좌표로 교정.
- 좌석 예측은 수도권 광역과 같은 정직 단계: 혼잡도 원천이 없어 「모름 = 서서」.
  다음 단계(문서): 4개 공사의 시간대별 승하차(전부 공개 확인)로 추정 연결, 지역 버스.
- 확장 그래프 재검: 시험 133개 + 3분 시뮬 여정 19만 개 위반 0.

## D-89. 빠른 순 상위 5 보장 + 근처 도보 환승 부재의 기록 (2026-09-06, 사용자 지적)

서울대입구→월곡두산(일 21:40)에서 「2호선→사당→4호선→미아사거리→성북10」이 빠졌다.
원인 둘: ① **빠른 길 탈락** — 정렬(서는 시간)이 동률일 때 87·88분짜리 빠른 조합이 12개
상한에 밀렸다 → **빠른 순 5위 안은 상한에 걸려도 뒤에 얹는다**(정렬 자체는 안 바꾼다 —
걷기 보존(D-81)과 같은 꼴). ② **근처 도보 환승 부재(구조)** — 갈아타기는 「같은 노드」
(150m 클러스터)에서만 이뤄져서, 지하철역과 150m 밖 마을버스 정류장(미아사거리역 ↔
성북10 정류장)은 후보조차 안 된다. 당장은 **경유지('+' 단추)**가 해답이다 — 경유
「미아사거리역」을 넣으면 「2호선→4호선→성북10 · 80분」이 실제로 2위에 나온다(실측).
근처(300m) 도보 환승 후보 확장은 탐색 폭발을 다스려야 하는 큰 공사라 남은 일로 못 박는다.

## D-90. 근처 도보 환승 + 「찍은 역의 노선은 꼭」 (2026-09-07, 사용자 지시)

D-89 에서 「남은 일」로 미뤄 둔 구조 문제를 구현했다. 혜화→월곡두산(평일 20:50)에서
「4호선 + 성북10」이 통째로 안 나오던 이유: 갈아타기가 **같은 노드(150m 클러스터)**
에서만 됐는데, 미아사거리역과 성북10 정류장(롯데백화점미아점)이 **95m 떨어진 딴 노드**였다.
클러스터 경계 하나가 실제로 가장 빠른 길을 지워 온 것이다.

- **공간 격자 + 근처 환승**: `buildIndex` 가 노선이 지나는 노드만 0.005°(≈555m) 격자에
  넣고, 환승 자리마다 **400m 안 이웃 6곳**까지 다음 구간 후보로 본다(`nearNodes`·`hopsAt`).
  2회 환승은 이웃 3곳으로 좁혀 폭발을 막는다. 실측 42~133ms(상한 2,500ms).
  환승 도보 시간은 이제 **실제 거리**로 잰다(같은 역 안이면 기존 5분 유지 — 개찰구를
  빠져나오는 시간이 있으므로 그보다 짧아지지 않는다). 결과: **4호선→성북10 39분** 등장.
- **찍은 역의 노선은 목록에 꼭**: 출발·도착이 지하철역이거나 300m 안이면 그 노선을 타는
  가장 좋은 경로를 노선마다 하나씩 얹는다(최대 4개). 혜화에서 4호선을 타는 길이 서는 시간
  순 15위라 목록 밖으로 떨어졌던 것을 막는다.
- **보강 카드에는 표지를 단다** — 목록은 「서는 시간 짧은 순」이라고 말하는데 D-89·D-90의
  보강이 그 순서를 깨므로, 화면에 「⚡ 빠른 길」·「🚉 이 역 노선」 딱지를 붙이고
  `j.appended` 로 검증기의 정렬 검사에서도 뺀다(시뮬레이션이 이 모순을 먼저 잡았다).
- **덤 — 동명 지명 실사고**: 서울에서 「중구청」을 치면 **대전 중구청역**이 1위였다
  (정확일치 + 역 가산점). `findNodes(graph, q, limit, near)` 가 이미 고른 반대편 지점을
  기준으로 **40km 밖은 등급을 뒤로 민다**(매칭 등급 자체는 안 흔든다). 검색창은 출발을
  고르면 도착 검색에, 도착을 고르면 출발 검색에 그 좌표를 넘긴다.

검증: 시험 134개, 시뮬레이션 여정 16만 개 위반 0, 화면 실물 확인(⚡ 빠른 길 · 4호선 →
마을 성북10 · 39분). 남은 것: 근처 환승 400m 문턱은 어림이라 실측 피드백으로 다듬을 것.

## D-91. 찍은 역의 노선은 1위 바로 다음 자리 (2026-09-07, 사용자 지시)

D-90 은 「목록에 넣기」까지만 했다 — 목록 **끝**에 얹었더니 혜화역→월곡두산 21:10 에서
4호선 카드 셋이 13·14·15위였고, 사용자는 스크롤로 못 봤다("여전히 안 나오는데?").
자리를 옮긴다: **출발·도착이 역에서 50m 안이면 그 노선의 가장 좋은 길을 1위 바로 다음에**
꽂는다(노선·방향 조합마다 하나, 최대 4개). **1위 자리는 안 건드린다** — 이 서비스의 약속이
「서는 시간이 가장 짧은 길」이라 그 자리는 정렬이 정한다. 옮긴 카드에는 「🚉 이 역 노선」
표지가 그대로 붙어 왜 순서 밖에 있는지 밝힌다.

50m 는 사용자가 정한 값이다 — 「역이나 역 이내 50미터를 찍으면」. 300m 규칙(D-90)은
목록에 넣기만 하는 느슨한 보장으로 남는다. 실측: 혜화→월곡두산 21:10 에서
「4호선→1164 · 36분」이 2위, 화면에서 「🚉 이 역 노선 · 가장 빨리 가는 길」로 확인.
시험 135개.

## D-92. 출발↔도착 바꾸기 단추 (2026-09-07, 사용자 요청)

오는 길을 보고 나서 가는 길을 보는 것이 흔한 일인데 두 칸을 다시 타자 쳐야 했다.
**두 자리에 넣는다**: 첫 화면의 **「출발」 라벨 줄 오른쪽에 작게**, 결과창 편집바의 ＋ 옆에
큰 단추로(결과를 보면서 바로 뒤집는 자리). 첫 화면은 글자만 바꾸고, 결과창은 바꾸자마자
다시 찾는다.

**첫 판은 두 입력칸 사이에 한 줄로 넣었다가 물렀다** — 그 한 줄이 출발과 도착을 벌려 놓아
한 쌍으로 안 읽혔다(사용자 지적: 「너무 띄워져 있음」). 첫 화면에서는 **줄을 새로 만들지
않는 것**이 규칙이다. 이미 있는 라벨 줄에 얹으면 높이가 하나도 안 늘어난다.

**경유지가 있으면 순서도 뒤집는다** — A→v1→v2→B 를 뒤집으면 B→v2→v1→A 다.
안 뒤집으면 뒤집힌 여정에서 경유 순서만 옛것으로 남아 엉뚱한 길이 된다.
단추는 44px 이상(사양서 7장 손가락 규칙), 글자 「바꾸기」를 함께 둔다 — 화살표만으로는
무슨 기능인지 어르신에게 안 읽힌다. 실측: 혜화역↔월곡두산 양방향 교체 확인, 시험 135개.

## D-93. 노선도 창 — 노선 이름을 누르면 시점~종점 (2026-09-07, 사용자 요청)

「이 버스가 어디서 어디로 가는 차인가」는 타기 전에 늘 궁금한 것이다. 상세 화면의 노선
이름을 누르면 그 노선 전체를 지도에 그린다 — **내가 타는 구간만 진하게**, 나머지는 흐리게,
시점·종점·타는 곳·내리는 곳에 이름표. 정류장 점을 이은 선이라 실제 도로 모양이 아니라는
것을 화면에 밝힌다. 뒤로가기로 닫힌다(D-67 규칙, 지도 고르기 창보다 먼저 닫는다).

**전체 화면 창은 크기를 명시한다** — `position:fixed; inset:0` 만 주었더니 실측에서 창이
0×0 으로 잡혀 캔버스가 통째로 안 그려졌다. `width:100%; height:100dvh` 로 못 박고,
그래도 0 이면 화면 크기로 잡는 안전망을 둔다(검증 중 브라우저 창이 가려지면 레이아웃이
0 이 되는 것도 같은 증상으로 보였다 — 안전망이 그 경우도 덮는다).

## D-94. 조금 더 걸어서 다른 노선 타기 (2026-09-07, 사용자 요청)

「출발지·목적지 근처 역까지 20분쯤 걷는 게 더 나을 수도 있다」. 900m 안에서만 차를 찾으니
조금 더 걸으면 앉아 갈 수 있는 노선이 아예 안 보였다. `nearbyPlus` 가 기본 후보(900m·12곳)에
**그 안에 없는 노선을 새로 물어오는** 먼 정류장을 1.6km 까지 4곳 더 붙인다.
「새 노선을 안 물어오면 더 걸을 이유가 없다」가 규칙이라 후보가 헛되이 불지 않는다.
걷는 시간은 「서는 시간」에 그대로 들어가므로(D-77) 헛되이 먼 곳은 정렬이 알아서 내린다 —
**후보만 늘리고 판단은 안 바꾼다.** 실측(통일로 97 → 월곡두산 평일 18:20):
「간선 101 · 환승 0 · 걷기 10분 · 앉는 시간 50분」이 1위로 올라왔다 — 사용자가 짚은 바로 그 길.

## D-95. 걷는 길을 실제 길로 (2026-09-07, 사용자 지시)

지도의 도보 구간이 직선이라 「저기로 어떻게 가라는 거냐」가 됐다. OSRM 보행 경로
(키 불필요, OpenStreetMap 자료)로 실제 길을 받아 선을 갈아 끼운다. 실측: 직선 640m 구간의
실제 도보 989m·48개 점(꺾임 계수 1.54).

**그림만 바꾼다** — 걷는 **시간**은 여전히 직선×1.3(D-29)이다. 시간이 뒤늦게 바뀌면
정렬(서는 시간)이 화면을 다 그린 뒤에 흔들려 목록이 춤춘다. 실패·지연(4초)은 조용히
직선으로 두고(인터넷 없어도 앱은 죽지 않는다), 좌표쌍으로 메모리 캐시한다.
60m 이하는 부르지 않는다. 출처 표기 「도보 경로 © OSRM」을 바닥글에 넣었다 —
이용약관 사항이니 지우지 말 것. 남은 일: OSRM 실측 거리로 **걷는 시간**까지 맞추기
(정렬이 흔들리지 않게 검색 전에 한 번에 받아오는 구조가 필요하다).

## D-96. 버스는 차도를 따라 그린다 (2026-09-08, 사용자 지적)

「8101 버스가 왜 건물 뚫고 가게 표시되냐」 — 정류장 점을 바로 이어 그린 탓이다.
도보(D-95)와 같은 방법으로 **OSRM 자동차 경로**를 받아 실제 도로를 따라 그린다.
실측: 정류장 직선 몇 점 → **58점·483점**의 도로선.

- **지하철은 부르지 않는다** — 선로는 도로가 아니다. 차도를 따라 그리면 없는 길을 그리는
  셈이라 더 큰 거짓말이다. 역 사이는 곧게 잇고, 화면 설명에 그렇게 밝힌다.
- 「여기서부터 앉을 듯」에서 선 색이 갈리는 구간은 **두 토막을 따로** 받는다(색 경계 유지).
- 좌표가 24개를 넘으면 사이를 솎아 보낸다 — 공용 서버 부담을 줄이면서 도로를 따르는
  모양은 유지된다(양 끝은 반드시 남긴다). 노선·방향·구간·토막으로 캐시.
- **그림만 바꾼다** — 시간·좌석 계산에는 아무 영향이 없다(D-95 와 같은 원칙).
  실패·시한(5초)이면 조용히 직선으로 둔다.

## D-97. 두 손가락 확대·축소 (2026-09-08, 사용자 요청)

＋－ 단추만으로는 불편하다. 손가락 둘을 각각 기억해 사이 거리의 비율로 배율을 정하고
(log₂), **두 손가락 가운데 있던 지점이 그 자리에 머물도록** 지도를 옮긴다 — 안 그러면
확대할 때마다 보던 곳이 화면 밖으로 달아난다. 손가락 하나를 떼면 남은 손가락 자리에서
끌기를 이어 간다(안 그러면 지도가 튄다).

전제로 **타일을 소수 배율로 그리게** 고쳤다 — 타일 번호는 정수 배율에만 있으므로 가장
가까운 정수 타일을 가져와 차이만큼 늘려 그린다. 선·이름표는 원래부터 소수 배율을 쓰던
`o.px` 를 그대로 타므로 손댈 것이 없었다. 실측: z 14 → 15.4 로 벌려도 붙잡은 지점
(37.59966, 127.025708)이 화면 같은 자리에 남는다. 세 지도(상세·고르기·노선도) 모두 적용.

## D-98. 한국 주소·상호 검색의 한계 — 원인 규명 (2026-09-08, 사용자 지적)

「미아동 202-11 을 쳤는데 삼양로27길 95(삼각산동)가 나온다」·「◯◯내과로 못 찾는다」.
실측으로 원인을 갈랐다 — **우리 코드가 아니라 자료의 한계**다:

| 질의 | Photon | Nominatim |
|---|---|---|
| 미아동 202-11 | 「202」를 건물 이름으로 보고 **삼양로27길 19/95** 를 준다 | 「미아동」 행정구역만 |
| 미아 내과 | 0건 | 0건 |
| 미아동 정형외과 | 0건 | 0건 |

즉 OpenStreetMap 계열 검색은 ① **한국 지번주소(법정동+번지)를 다루지 못하고**
(도로명 기반이라 「202-11」을 번지로 못 읽는다) ② **한국 상호(병원·가게) 색인이 거의 없다.**
지금 화면이 「지번 → 엉뚱한 도로명」을 보여 주는 것은 검색이 억지로 비슷한 것을 집어
오기 때문이다.

**해결 방향(다음 작업)**: 정류장 8,886곳처럼 **로컬 색인을 하나 더 만든다** — 상호명·지번·
도로명·좌표를 함께 가진 공개 자료(지방행정 인허가 LOCALDATA, 서울 열린데이터광장 인허가
파일 등)를 파이프라인으로 받아 이름 접두어 색인으로 굽는다. 그러면 「미아동 내과」도,
「미아동 202-11」도 **인터넷 없이** 찾힌다(집 규칙과도 맞는다). 이번 조사에서 확인한 것:
LOCALDATA 사이트는 이 기계에서 접속이 막혔고(회선 문제로 보인다), 서울 열린데이터광장은
열린다 — 우리가 이미 키 없이 파일을 받아 쓰는 경로다. 후보 자료 확정과 용량 다이어트
(사람이 목적지로 삼는 업종만 추리기)가 다음 단계의 일이다.

## D-99. 상호로 찾기 — 장소 목록을 우리가 들고 다닌다 (2026-09-08, 사용자 요청)

D-98 에서 원인만 밝혔던 것을 고쳤다. **자료를 우리가 갖는다** — 정류장 8,886곳과 같은 방식.

- **받기** `pipeline/fetch_poi.py` — 심평원 병의원·약국(공공데이터포털). 좌표가 XPos·YPos 로
  **WGS84 그대로** 와서 변환이 필요 없다. 우리가 이미 쓰는 `DATA_GO_KR_KEY` 로 되지만
  서비스마다 활용신청이 따로다 — 지금은 403 이라 `--probe` 가 신청 주소를 찍어 준다.
- **굽기** `pipeline/build_poi.py` — `[이름, 위도, 경도, 지번주소, 종류]` 다섯 칸으로 줄이고
  좌표는 소수 다섯 자리(약 1m)로 자른다. 서비스권(D-88) 밖과 좌표 없는 줄은 버린다 —
  찾아 놓고 길을 못 찾으면 없느니만 못하다.
- **찾기** `SeatGeo.poiSearch` — 「미아동 내과」처럼 **동 이름 + 업종**이 사람의 말버릇이라,
  낱말을 다 만족하면(이름이든 주소든) 맞은 것으로 본다. 이름에도 걸리면 앞으로 낸다.
  지번주소로도 찾힌다(「미아동 202-11」) — OSM 이 못 하던 바로 그 일이다.
- **순서 바로잡기** `merge` — 「◯◯동」을 넣어 찾았는데 그 동이 온라인 결과 어디에도 없으면
  **맨 뒤로 민다**(지우지는 않는다 — 맞을 때도 있다). 실측으로 「미아동 202-11」에서
  삼양로27길 95(삼각산동)가 1위였던 것이 뒤로 갔다.
- **자료가 없어도 앱은 돈다** — `data/poi/poi.json` 은 있으면 쓰고 없으면 조용히 넘어간다.
  엔진은 그물을 모른다(집 규칙) — 앱이 파일을 읽어 `setPoi` 로 넣어 준다.

남은 일: 활용신청 승인 뒤 실제로 받아 굽기(용량·건수 실측), 병의원·약국 밖의 업종
(음식점·학원·관공서 등)은 원천을 더 붙이는 문제 — LOCALDATA 가 한 판에 다 있는데
이 기계에서는 접속이 막혀 있다.

## D-100. 장소 목록 실전 투입 — 병의원 21,513곳, 「칠 때 받기」 (2026-09-08)

D-99 의 파이프라인에 실제 자료가 들어왔다. 사용자가 병의원 API 활용신청을 눌러 승인됐고,
수도권·광역시 전체 **21,809건**을 받아 **21,513곳**으로 구웠다(서비스권 밖·좌표 없는 줄 폐기).
실측 검색: 「미아동 내과」 → 미아동 소재 내과 4곳(신내과·내일내과·시원내과·연세내과),
「월곡동 정형외과」 → 월곡 일대 정형외과. **심평원 주소가 「도로명, (법정동)」 꼴이라
법정동 검색이 그대로 먹는다** — 사용자가 지적한 「법정동·도로명 불일치」가 여기서 풀린다.

- **파일은 「칠 때」 받는다** — 2.9MB 라 시작할 때 받으면 폰에서 첫 화면이 느려진다.
  검색을 시작한 순간 한 번만 받아 두고 그 뒤로는 메모리에서 찾는다. 못 받아도 조용히
  넘어가고 정류장·역 검색은 그대로 돈다.
- **약국·음식점 등은 원천이 하나 더 필요하다** — 심평원은 병의원·약국뿐이고, 사용자가
  물은 「음식점 등등」은 **소상공인시장진흥공단 상가(상권)정보**가 답이다(상호·업종·지번·
  도로명·위경도가 한 줄에). `--store` 로 받도록 붙여 뒀고, 개발계정 하루 상한 때문에
  **어디까지 받았는지 적어 두고 이어 받는다**(store_state.json). 업종은 사람이 목적지로
  삼는 넷(의료·음식·학문교육·스포츠)만 고른다 — 전부 받으면 서울만 57만 곳이다.
  지금은 두 서비스 모두 403 이라 활용신청이 필요하다:
  약국 https://www.data.go.kr/data/15051059/openapi.do ·
  상가 https://www.data.go.kr/data/15012005/openapi.do

## D-101. 「1%」는 우리 자료가 뒷받침하지 못한다 — 실측 기준값으로 당긴다 (2026-09-08, 사용자 지적)

「141번 탈 때 앉을 확률 1%가 맞냐? 차 100대 중 한 대만 앉는다는 거냐? **1%와 99%는 유의하라**」.
우리 수집기가 모은 실시간 혼잡 단계(간선 4만여 대)를 시간대별로 폈다:

| 시각 | 여유 | 시각 | 여유 |
|---|---|---|---|
| 05시 | 98% | 15시 | **76%** |
| 07시 | 76% | 17시 | 65% |
| 08시 | 68% | 18시 | 61% |

**15시엔 간선버스 4대 중 3대가 「여유」**(좌석에 앉을 수 있는 정도)다. 그 시각 한 정류장에서
「1%」라고 단언할 근거가 우리에게 없다. 더 놀라운 것은 반대쪽 — 노선 전체로 보면 우리 모형은
어느 시각이든 **95~100%가 여유**라고 말한다(실측 61~98%). 즉 스케일이 아니라 **분포의 모양**이
틀렸다: 한산한 정류장은 너무 비게, 붐비는 정류장은 너무 차게 만든다. 배율 하나로는 못 고친다
(곱하면 141 은 더 나빠진다).

**고친 방법** — per-정류장 추정은 신호 하나일 뿐이고, 그 시각 그 종류 버스의 **실측 여유
비율**이 강한 사전값이다. 둘을 로그오즈에서 같은 무게로 섞는다(`blendBase`). 정류장별 차이
(우리 자산)는 남고 극단만 사라진다. 자료가 없으면 안 섞는다. 표기 하한도 **5%** 로 뒀다
(상한 90 과 짝, D-73). 실측: 141 논현아이파크 15:06 → **1% → 13%**.

**덤으로 잡은 조용한 굶주림(D-55 재발)** — `makeLoadFor` 의 `busCtx` 는 ctx 에서 골라 담는
껍데기다. 새 자료 두 개(`kindLoad` D-86, `loadCalib` D-101)를 ctx 에만 넣고 여기 안 옮겨서
**두 기능이 조용히 죽어 있었다**(자료 없는 버스의 종류 평균 어림이 한 번도 안 돌았다).
새 자료를 ctx 에 넣을 때는 반드시 busCtx 도 함께 고친다 — 주석으로 못 박았다.

**남은 일**: 분포의 모양 자체를 고치는 것(OD 감쇠·배차·승객 쏠림의 상호작용)은 더 큰 일이고,
금요일 재보정 때 차량별 실측이 더 쌓이면 정류장 단위로 맞출 수 있다.

## D-102. 역 옆에서 출발하면 그 역 노선을 위로 (문턱 50m → 300m) (2026-09-08, 사용자 지시)

「출발지가 언주역 바로 옆이라 언주역 경유 경로를 무조건 하나 넣어야 한다. 다른 장소도 똑같이」.
D-91 의 고정 문턱이 50m 였는데 유상호정형외과 → 언주역은 **77m** 라 걸리지 않아 9호선 경로가
5위에 묻혔다. 사람이 「역 옆」이라 느끼는 거리는 몇십 미터가 아니라 걸어서 3~4분까지다 —
D-90 의 「목록에 넣기」와 같은 **300m** 로 맞췄다. 1위 자리(서는 시간이 가장 짧은 길)는
그대로 두고 2위부터 꽂는다. 실측: 유상호정형외과 → 월곡두산 14:50 에서 「9호선→148」이 2위.

---

## D-103. 부산 — 혼잡도가 없으면 승하차에서 되짚는다 (2026-09-08)

부산은 서울과 원천이 다르다. 서울은 「혼잡도(정원 대비 %)」를 방향별로 주는데,
부산교통공사가 여는 것은 **역별 시간대별 승하차 인원**뿐이고 거기엔 방향이 없다.
(1호선 열차혼잡도 파일이 따로 있지만 2020~2021년, 코로나 시기라 지금 값으로는 못 쓴다.)

**받는 법부터가 문제였다.** 활용신청이 필요한 것은 오픈 API 뿐이고 파일데이터는
「로그인 없이 다운로드」인데, 버튼이 주소 하나가 아니라 세 단계다
(`selectFileDataDownload.do` → `check-limit.json` → `fileDownload.do`).
①을 건너뛰고 ③만 부르면 **0바이트가 조용히 내려온다** — 실패가 아니라 빈 파일이라
알아채기 어렵다. `fetch_busan.py` 가 그 흐름을 그대로 밟고, 1만 바이트 미만이면 죽는다.

**호선별로 따로 풀면 안 된다.** 환승 승객은 승하차 자료에 안 잡힌다 — 다대포에서 타
서면에서 2호선으로 갈아타고 해운대에서 내리면 기록은 「다대포 승차·해운대 하차」뿐이다.
1호선만 떼어 풀면 이 사람이 서면에서 사라진다. 그래서 **1~4호선을 한 망으로** 놓고
최단경로(환승벌점 4)에 싣는다. 중력모형 씨앗 → IPF 로 승차·하차 양쪽 가장자리를 맞춘다.

**실측이 심판을 봤다.** 2021년 1호선 실측(차량별·행선지별 혼잡도)과 맞대어 방식을 골랐다:

| 방식 | 상관 | 모양오차 |
|---|---|---|
| 양쪽맞춤(IPF) + 지수감쇠 L=10 | **0.954** | 8.0%p |
| 한쪽맞춤 + 지수감쇠 | 0.918 | 10.0%p |
| 한쪽맞춤 + 거듭제곱감쇠 | 0.923 | 7.9%p |

고르는 도중 **방향 부호를 뒤집어 붙인 실수**가 있었다(색인 증가 = 노포행인데 다대포행으로
읽었다). 그때 상관 0.59, 고친 뒤 0.95 — 이 한 자리가 전부를 갈랐다. 연도 차는 공표값으로
보정했다(1일 평균 2020년 67.3만 → 2025년 87.3만 = 1.30배).

**편성·정원은 서울 값을 쓰면 안 된다.** 부산 1~3호선은 17.5m 중형이라 1칸 정원 118·좌석 44,
4호선은 9.1m 경전철이라 53·21 이다(공사 공표 제원). 서울 대형차(160·54)로 계산하면 같은
혼잡도에서 앉을 확률이 통째로 틀린다. 그래서 `capacity`·`cars`·`tph`(24칸 배차표)를
그래프의 노선에 실어 두고 엔진이 그것을 먼저 읽게 했다 — 없으면 서울 값으로 물러난다.

**공휴일은 표로 적지 않고 자료가 말하게 한다.** 설·추석은 해마다 날짜가 바뀌고 대체·임시
공휴일은 예고 없이 생긴다. 대신 출근 첨두 비율(07~09시 승차 ÷ 하루 승차)을 본다 —
평일 0.16, 주말 0.07 로 두 배 넘게 벌어진다. 이 방법이 2026년 1~7월에서 신정·설 사흘·
삼일절 대체·근로자의날·어린이날·부처님오신날 대체·지방선거일·제헌절 **열 날을 정확히** 집어냈다.

**남은 한계**(화면에 「추정」이라고 밝힌다): 2~4호선은 맞대 볼 실측이 없다 · 토·일은
정답지 자체가 없다(2021 파일이 평일뿐) · 한낮 값이 2021 실측보다 높은데, 2026년 승차의
시간대 모양(12시=08시의 0.73)과 2021 혼잡도 모양(0.68~0.75)이 거의 같은 것으로 보아
**코로나 때 한낮 통행이 더 눌렸던 몫**으로 읽는다. 배차 가정(출퇴근 4분·평시 6분)은
공표값과 일치한다.

## D-104. 추정으로 센 재차라면 확률도 그만큼 무뎌야 한다 (2026-09-08)

D-101 에서 「1%와 99%는 유의하라」는 지적을 버스에서 받았다. 부산 자료는 같은 함정이
더 깊다 — 혼잡도 자체가 추정이고 오차가 평균 8%p(한 칸 9~10명)인데, 좌석 44석이면
로지스틱의 k 가 4.8명뿐이라 **열 명만 틀려도 답이 뒤집힌다.** 그 오차를 무시하고 넣으면
「앉을 확률 3%」 같은 확신이 나온다.

그래서 오차를 곡선에 합친다(로지스틱 ≈ 정규분포, 표준편차 = 1.814k):
`k_eff = √(k² + (σ/1.814)²)`. σ 는 `congestion.modelErrorPct` × 정원에서 온다.
**실측 자료는 σ=0 이라 서울 계산이 한 자리도 안 바뀐다**(심층 훑기 위반 0건으로 확인).

곁가지로 `rideSpread` 가 `ride` 에 `seatBase`·`loadSigma` 를 안 넘기고 있던 것을 고쳤다.
지금은 `info.sd` 를 아무도 안 실어 죽은 길이지만, 살아나는 날 그 노선에서만 조용히
기능이 굶는다 — D-55 와 똑같은 함정이라 미리 막았다.

**분명히 해 둘 것**: 이 장치는 「탈 때 앉을 확률이 낮다」를 지우지 않는다.
동래 13시에 재차 65명·좌석 44석이면 이미 스무 명이 서 있는 열차이고, 그때 **탈 때**
앉을 확률이 낮은 것은 틀린 게 아니라 맞는 말이다(D-33). 무디게 하는 것은 확신이지 사실이 아니다.

---

## D-105. 배차는 공표 문구가 아니라 자료에서 센다 — 서울 밖 다섯 도시로 (2026-09-08, 사용자 지시)

「다른 지역 다 업데이트」. 부산에서 세운 방법(D-103)을 **대구·대전·광주·인천**까지 넓혔다.
받는 법이 뚫려 있어(파일데이터 3단계) 다섯 도시 승하차가 모두 2026년판으로 들어왔다.

| 도시 | 노선 | 자료 범위 |
|---|---|---|
| 부산 | 1~4호선 | 2026-01-01 ~ 07-31 |
| 대구 | 1·2·3호선 | 2026-01-01 ~ 07-31 |
| 대전 | 1호선 | 2026-01-01 ~ 07-31 |
| 광주 | 1호선 | 2026-01-01 ~ 07-31 |
| 인천 | 1·2호선 | 2025-01-01 ~ 2026-06-30 |

**★ 이번의 진짜 교훈은 배차다 ★**
집 규칙에 「이 나누는 수가 두 배 틀리면 앉을 확률이 통째로 뒤집힌다」고 적어 놓고도,
부산에서 **공표 문구(「출퇴근 4분·평시 6분」)를 그대로 믿었다.** 그런데 같은 공사가 내는
**운행실적**을 보니 1호선은 하루 편도 179회다 — 공표 문구대로 깔면 204회가 나온다(15% 과다).
운행거리로 검산해 「1회 = 편도 1회」임을 확인했다(424,089km ÷ 11,000회 = 38.6km ≈ 노선 40.5km).

그래서 **열차시각표 파일을 받아 열차를 직접 세는 단계**(`build_city_tph.py`)를 새로 두었다.
대구·대전·광주·인천은 시각표가 있어 정확히 센다. 실측 결과 공표 문구와 크게 다른 곳이 있다 —
**광주 1호선 평시는 「30분」이 아니라 10분**(2차 출처가 틀렸다), 대구 1호선 평시는 7대/시다.

부산만 시각표 파일이 없다. 하루 총량은 운행실적이 못 박고, 시간대 **모양**은 공표 배차비로
깔았는데 실측과 맞대 보니 그것도 틀렸다(09~16시 1.25~1.48배 과대, 새벽·밤 0.63~0.95배 과소).
네 무리(첨두·평시·퇴근·초말차)로 묶어 그만큼 되돌렸다 — **네 숫자만 맞췄으므로 역별 분포
546쌍의 검증력은 그대로다.** 그 결과 **상관 0.954 → 0.976, 평균오차 9.3 → 8.4%p**.
남은 기울기 1.10은 2021년(코로나)과 2026년의 차이로 본다.

**대구는 한낮이 출근길보다 붐빈다 — 이건 모형이 아니라 사실이다.**
처음엔 버그로 의심했는데 두 실측이 함께 그렇게 말한다: 13시 승차가 08시의 77%인데
**열차는 12대 → 7대로 줄어든다.** 대구는 무임 이용이 많아 한낮 수요가 두텁고 배차는 성기다.
상식과 어긋나 보이는 값을 만나면 자료를 먼저 볼 것 — 이번엔 자료가 옳았다.

**역 이름이 세 가지로 어긋난다** — ① 개명(대구 대공원→수성알파시티, 어린이회관→어린이세상),
② 괄호 병기(인천 「가정(루원시티)」), ③ 환승역 호선 꼬리표(대구 「반월당1」·「반월당2」).
꼬리표는 **떼어 낸 것이 실제 역 이름일 때만** 인정한다(D-48 의 rstrip 사고 방지).
인천 자료에는 **7호선 줄이 섞여 있다**(인천교통공사가 그 구간도 운영한다) — 서울 혼잡도가
따로 있으므로 거른다. 안 거르면 부평구청처럼 이름이 겹치는 역에서 승하차가 두 번 더해진다.

**차량은 다섯 가지가 됐다**: 서울 대형(54/160) · 부산 중형(44/118) · 지방 중형(46/120) ·
모노레일(30/88, 대구 3호선) · 경전철(29/103 인천 2호선, 21/53 부산 4호선).
좌석 만석 임계가 28~40%로 갈린다 — 한 값으로 뭉치면 그 노선이 통째로 틀린다.

**아직 비어 있는 곳**: 서울 9호선·우이신설·신림(서울교통공사가 아니라 원천이 다르다),
코레일 광역전철 전부(수인분당·경의중앙·경춘·1호선 인천방면 등 ~313역), 김포·의정부·용인·
부산김해경전철. 이들은 여전히 「모름 = 서서」다.

---

## D-106. 광역시 시내버스 — 망은 TAGO, 이름은 도시를 붙여서 (2026-09-09, 사용자 지시)

「전국의 모든 버스 ktx 시외버스 등은?」 → **KTX·SRT·고속버스·시외버스는 넣지 않기로 했다.**
전부 전 좌석 지정석이라 표를 끊으면 앉는다 — 「서서 가는 시간이 짧은 순」이라는 이 앱의
존재 이유가 성립하지 않는 영역이다. 넣으면 시간표·예매 안내가 되는데 그건 다른 제품이다.
대신 **광역시 시내버스**(부산·대구·대전·광주·인천)를 넣었다. 노선 1,394개·정류장 16,681곳.

**망은 TAGO(국토교통부)에서 받는다.** 지방은 도시마다 자료 모양이 제각각이고, 있어도
**정류장 순번이 없다**(대구 노선정보 파일은 기점·종점만 준다). 순번이 없으면 그래프를 못 만든다.
TAGO 는 138개 시를 한 규격으로 주고 순번·좌표·방향이 다 있다. 우리 키로 바로 됐다.

**노선 이름에 도시를 붙였다.** 서울에도 「101」이 있고 대구에도 있다. 이름이 겹치면 앱이
대구 101 의 혼잡도로 **서울 101 의 승하차 파일**을 읽는다 — 오류 없이 틀린 숫자가 나오는,
이 저장소에서 제일 위험한 종류의 사고다.

**방향값이 없는 노선은 회차점으로 가른다.** 광주는 356개 **전부**, 대구는 절반이
`updowncd` 를 안 준다. 한 줄로 두면 돌아오는 구간을 타려고 노선을 한 바퀴 다 도는 것으로
계산돼 그 방향이 통째로 사라진다. 서울 버스에 쓰던 `split_round_trip` 규칙을 그대로
적용해 179개를 갈랐다(순환 노선은 안 가르도록 네 가지를 확인한다).

**★ 이번에 실제로 깨진 것: 이름이 같은 역 ★**
전국 정류장이 들어오자 **서울 경로가 통째로 안 나오기 시작했다.** 「시청역」이 서울·부산·
대전 셋에 있어 검색이 대전 시청역을 먼저 내놓았고, 「월곡」은 대구 정류장이 정확히 맞아
서울 「월곡역」을 밀어냈다. 시험 16개가 한꺼번에 깨졌는데 원인은 **검색 한 곳**이었다.
고친 것 셋:
  · 「역」이 붙고 안 붙고를 등급 차이로 치지 않는다(사람이 「월곡」이라 치면 그 역을 찾는다)
  · 같은 점수면 **노선이 많이 서는 곳**이 먼저다
  · 기준점을 셋으로 넓혔다 — 반대편 지점 → **마지막에 고른 곳**(새로 기억한다) → 없으면 이름순
검증 도구도 서울 기준점을 넘기게 했다(무엇을 보는 도구인지 밝히는 것이지 봐주는 것이 아니다).

**크기를 줄였다.** 그냥 얹으면 nodes 3.1MB + routes 4.6MB 였다. 실행 중에 아무도 안 읽는
두 가지를 뺐다 — 노드의 `members`(그래프를 만들 때만 쓴다)와 지방 버스의 `stops`
(승하차 파일과 이어 붙일 때만 쓰는 열쇠인데 그 파일이 없다). routes 가 **4.6 → 1.7MB**.

**울산을 빠뜨렸다가 뒤늦게 넣었다** (2026-09-09). 「광역시 시내버스」라고 해 놓고 다섯 곳만
했는데, 전국 범위를 세어 보다가 울산 499개 노선이 통째로 빠진 것을 발견했다. 세는 일을
안 했으면 못 찾았을 실수다.

**방향값은 도시마다 사정이 아주 다르다** (실측): 부산 99% · 대전 98% · 인천 94% 는 자료에
`updowncd` 가 들어 있는데 **광주와 울산은 0%**, 대구는 49%다. 자료에 방향이 있는 것은
전체의 47%뿐이고, 회차점 분리가 64%까지 살려 낸다. 그래서 시험도 「전체 비율 하나」로 재면
안 된다 — ① 방향값을 주는 도시는 90% 넘게 두 방향이어야 하고(파싱 사고를 잡는다)
② 전체는 60%를 넘어야 한다(회차점 분리가 죽으면 47%로 떨어진다).

**남은 한계 (화면에 밝힌다)**: 지방 시내버스는 노선별 승하차가 없어 **모든 버스가 같은
어림값**(같은 종류·같은 시간대의 서울 평균, D-86)을 받는다. 즉 도시 안에서 버스끼리는
붐빔으로 우열이 안 갈린다 — 정렬은 타는 시간·걷는 시간이 가른다. 대구·인천은
「정류소별 시간대별 승하차」 파일이 있어 다음 단계로 정밀화할 수 있다(경기도는 API 활용신청 필요).

---

## D-107. 도시 간 이동 — 지정석은 확률이 아니라 사실이다 (2026-09-09, 사용자 지시)

D-106 에서 「KTX·SRT·고속·시외버스는 빼자」고 했는데 사용자가 **넣으라고 했다.** 넣되,
이 영역에서 우리가 답할 질문이 무엇인지는 자료를 보고 정했다.

**셋 다 전 좌석 지정석이다.** 표를 끊었으면 앉고 매진이면 못 탄다 — 예측할 것이 없다.
그래서 **퍼센트로 말하지 않는다.** 차종에 `reserved` 를 달고 화면은
「지정석입니다 — 예매하면 앉아 갑니다 / 매진이면 탈 수 없습니다」로 답한다.
확신을 확률처럼 꾸미지 않는 것이 D-101(「1%와 99%는 유의하라」)의 반대편 짝이다.

**★ 여기서 제일 위험한 것 ★** 이 저장소의 기본 규칙은 「모르는 구간 = 서서 간다」(D-25)다.
지정석 노선을 그 규칙에 그냥 흘리면 **KTX 를 두 시간 반 내내 서서 가는 것으로** 계산한다.
정반대의 답인데 오류가 안 나서 눈에 안 띈다. `loads.js` 가 `route.reserved` 를 먼저 보고
빠져나가게 했고, 시험(`tests/intercity.test.js`)이 그 자리를 지킨다.

**「기다림은 서 있는 것」(D-74)은 지정석에도 그대로 적용한다.** 차 안에서는 앉지만
승강장에서 기다리는 시간까지 앉았다고 하면 도시 간 경로가 늘 1위로 올라와 목록을 못 믿게 된다.

**자료는 TAGO 세 서비스**(열차 15098552 · 고속버스 15098522 · 시외버스 15098541).
주소를 찾는 데 두 번 헛짚었다 — `TrainInfoService` 가 아니라 **`TrainInfo`** 이고,
오퍼레이션이 **대문자로 시작**한다(`GetStrtpntAlocFndTrainInfo`). 두 오류가 각각 다른
답을 준다: 주소가 틀리면 400 `NO_OPENAPI_SERVICE_ERROR`, 키가 등록 안 됐으면
403 `SERVICE_KEY_IS_NOT_REGISTERED_ERROR`. `--probe` 가 이 둘을 갈라 말한다.

**조회형이라 미리 받아 둔다.** 「출발지-도착지-날짜」로 묻는 API 인데 우리 앱은 정적
페이지라 키를 브라우저에 둘 수 없다. 그래서 **시내 교통을 가진 여섯 도시**(서울·부산·대구·
대전·광주·인천) 사이만 미리 받아 노선으로 굽는다 — 도시 쌍 × 수단 × 등급 하나가 노선 하나다.
정류장이 둘뿐인 노선이고, `minutes` 는 소요시간 중앙값, `headwayMin` 은 운행 시간대 ÷ 편수다
(엔진이 그 절반을 기다림으로 잡는다, D-57). 요금도 함께 싣는다 — 어르신에게 등급과 요금은
실제로 중요한 정보다.

**노드는 이름으로 맞춘다.** TAGO 의 역·터미널 목록에는 **좌표가 없다.** 도시 상자 안에서만
이름을 맞추므로 같은 이름이 딴 도시에 있어도 안 섞인다(D-106 에서 겪은 그 문제다).

**승인 뒤 실제로 돌려 보니 네 가지가 어긋났다** — 전부 「오류는 안 나는데 답이 틀린」 종류다.

① **대표 역·터미널을 잘못 골랐다.** 「편성이 나오는 첫 조합」을 쓰자 서울→부산이 **구포**
   경유 27편으로 잡혔다(본선 부산역은 75편이다). 광주는 광주송정이 아니라 광주역, 대전→광주는
   신탄진이었다. → 후보를 다 돌려 **편수가 가장 많은 조합**을 고른다. 그게 그 도시의 본역이다.

② **수단마다 볼 수 있는 날이 다르다.** 열차는 일주일 뒤도 나오는데 **고속·시외버스는 이틀
   뒤까지만** 나온다(사흘 뒤면 0편). 한 날짜로 다 받다가 버스가 통째로 비었다 — 30쌍 중 0쌍.
   → 수단별로 날짜를 따로 고른다.

③ **고속버스 터미널 목록은 `cityCode` 를 무시한다.** 도시를 줘도 전국 453곳을 그대로 준다.
   그래서 목록 앞머리의 「병점역」이 서울 대표로 잡혀 조회가 전부 0이 됐다.
   → 고속버스만 **도시 이름으로 검색**(`terminalNm`)한다. 시외버스는 `cityCode` 가 먹는다.

④ **★ 가장 위험했던 것: 느슨한 이름 맞추기 ★** 「이름이 들어 있으면 맞다」로 했더니
   **KTX 가 서울월드컵경기장에서 출발해 부산원동역에 닿는** 노선이 만들어졌다.
   「서울」이 「서울월드컵경기장…」에, 「부산」이 「부산원동역」에 걸린 것이다.
   오류는 안 나고 경로만 여섯 시간이 된다. → 순서를 못 박았다:
   별칭 표 → 이름 그대로 → **이름+「역」**(TAGO 는 「서울·부산」, 우리는 「서울역·부산역」) →
   그래도 없으면 그 이름으로 시작하고 세 글자 이내로만 긴 이름 중 지하철역 먼저.

고친 뒤 실측: **서울역 → 부산역 09시 = KTX 지정석 · 서는 시간 13분(기다림뿐) · 총 178분**
(승차 165분 + 배차 25분의 절반). 도시 간 노선 174개(열차 93 · 고속 61 · 시외 20).

**한 가지 더**: 서울↔부산·대구·대전·광주 사이에 **시외버스는 거의 없다** — 장거리는 고속버스가
담당하고 시외버스는 중·단거리다(동서울→춘천은 47편으로 잘 나온다). 0편인 것은 버그가 아니다.

**★ SRT 는 들어 있는데 이름이 KTX 로 붙어 온다 ★**
`GetVhcleKndList` 에는 코드 17 = SRT 가 분명히 있다. 그런데 **등급코드 17 로 물으면 0편**이다.
등급을 안 주고 수서→부산을 물으면 **34편이 나오는데 전부 「KTX·KTX-산천·KTX-산천(A-type)」**이다.
수서역은 SR 전용역이라 코레일 KTX 가 들어오지 않으므로, 그 편들은 SRT 다 —
이 API 가 SR 의 열차를 **차량 형식명으로 표기**해 주는 것이다.

그대로 두면 두 가지가 잘못된다: ① 화면이 「KTX 수서→부산」이라고 거짓말을 하고
② 등급이 KTX 와 겹쳐 「새 등급만 담는다」 규칙에 걸려 **수서 출발이 통째로 빠진다**
(실제로 처음엔 SRT 가 하나도 안 들어왔다). 그래서 `SRT_STATIONS = {'수서'}` 로
수서 출발·도착 편의 등급을 SRT 로 바로잡는다. 자료를 고치는 것이므로 근거를 여기 적어 둔다.

**후보 역을 넉넉히 봐야 한다는 것도 여기서 드러났다.** 서울 후보를 셋만 보면 수서가 안 들어와
SRT 가 통째로 사라지고, 서울→대구가 **대구역 21편**으로 잡힌다(동대구는 108편이다).
열차만 후보를 다섯으로 늘렸다. 그 결과 열차가 닿는 도시 쌍이 16개 → **24개**가 됐다.

실측: 수서역 → 부산역 = SRT 지정석 · 156분 · 34편 · 52,900원 (서는 시간 16분, 기다림뿐).

---

## D-108. 수도권 광역전철 — 승하차 27개 노선으로 「자료 없음」 5백 역을 채운다 (2026-09-09, 사용자 지시 「배차부터 풀고 해라」·「다해」)

**문제.** 그래프의 수도권 지하철 30개 노선 중 실측 혼잡도는 서울교통공사 관할(1~8호선 서울 구간)뿐.
경의중앙·수인분당·9호선·경춘·공항철도… 그리고 1·3·4호선의 코레일 직결 구간(경부·일산·과천안산선)까지
`자료 없음 = 서서 간다`로 남아 있었다. 코레일이 여는 승하차는 연간 총계라 시간대가 없다.

**원천 셋을 짜맞췄다.**
- 승하차: 서울 열린데이터광장 `CardSubwayTime` — 역별·시간대별, 실측해 보니 **27개 노선**이 들어 있다
  (경부·경인·경원·경의·중앙·분당·수인·안산·일산·과천·장항·경춘·경강·서해선·공항철도·신림·우이신설까지).
  월 합계라 요일이 없다 → 서울 실측(ride.json)의 요일 비로 나눈다. 모형은 D-103 그대로(중력 씨앗+IPF+최단경로).
- 배차(세 겹): ① 서울 시간표 API 는 노선 목록엔 없던 **1호선 연천~신창~인천 102역 전 구간**을
  「01호선」으로 세어 준다(3·4·7·8호선 연장 포함) — 역별 실측. ② 코레일 「여객열차 운행횟수」 파일의
  전동차 회/일(경인 474·경부 392·분당 360…)을 1호선 하루 모양으로 펴서 시간당으로.
  ③ 그마저 없는 민자·경전철(신분당·공항철도·김포·우이신설…)은 운영사 공표 배차간격.
  ★ 역별 배차는 (원천 노선, 역이름) 짝으로만 붙인다 — 이름만 보면 신림선 「신림」에 2호선 신림의
  열차 수가 붙는다.

**네 번 넘어진 곳 (그대로 두면 또 넘어진다).**
1. **방향 라벨.** 검증 상관이 -0.02 가 나왔다 — 모형이 틀린 게 아니라 1호선은 dirLabels 가
   `[상선, 하선]`(색인 증가 = 상선)인데 폴백 규칙(증가 = 하선)으로 맞대고 있었다.
   격자를 쓸 때도 검증할 때도 **그 노선의 dirLabels** 로 라벨을 정한다. 고치니 0.83.
2. **수인분당선 stops 는 왕복 한 바퀴(97역)가 이어붙어 있다.** 인접성이 꼬여 최단경로가 노선을
   통째로 기피, 전 구간이 바닥값 1%가 됐다. `clean_seq` 로 순수 되돌이만 끊는다 —
   ★ 새 역이 섞인 반복(5호선 마천지선·9호선 급행 이어붙임)은 자르면 역을 잃으므로 남긴다.
3. **갔다 바로 돌아오는 막다른 지선 [A,B,A]도 걷는다.** 9호선 머리의 [김포공항,개화,김포공항]이
   본선 한가운데 0% 「개화」 키를 만들었고 엔진이 「안 다님」으로 읽어 김포공항→여의도가 통째로
   죽었다. B(개화)는 키가 아예 없는 편이 낫다 — 이웃 메우기(D-79)가 옆 역 값으로 채워 준다.
4. **mixed(1·3·4호선) 판정을 그래프 상태(noCong 존재)에서 유도하면 안 된다.** 상태가 바뀌면
   판정도 따라 변해 두 번째 실행에서 노선이 통째로 빠졌다. 이름 상수로 못 박았다.
5. **구간 배차는 양 끝 역의 큰 쪽으로 잰다.** 종착·지선역(신창·서동탄·광명)은 시간표가 한쪽
   방향만 잡혀 tph 0 인 시간이 있다 — 그 역에서 시작하는 구간이 0%가 되고, 지나는 leg 전체가
   「안 다님」이 됐다(verify_rush 가 22역을 잡아냈다).

**실측 노선과 섞이는 자리의 규칙.**
- 1·3·4호선은 실측 역을 절대 덮지 않고, 채운 역만 `estimatedStations` 로 밝힌다(노선 단위
  estimatedLines 로 하면 서울 실측 구간까지 「추정」이 된다). 엔진은 leg 가 그 역을 지나면 추정 표시.
- **noCong(D-87)은 그래프에 그대로 둔다.** 엔진(loads.js)이 「그 역의 방향값이 실제로 있으면
  쓰고, 없으면 옛날처럼 leg 전체를 모름」으로 읽는다. 걷어내 버리면 rail.json 을 못 읽은 구성에서
  호선피크 폴백이 되살아나 없는 만원을 지어낸다 — D-87 이 막은 바로 그 사고다.
  (덤: 지축처럼 실측이 있으면서 noCong 에 적힌 역도 이제 실측을 제대로 읽는다.)
- **승하차 원천이 아예 없는 노선(신분당·김포골드·에버라인·의정부·동해선·대경선)은 혼잡도를 만들지
  않는다.** 동명 환승역 값만 걸려 0%대로 나온다 — 첨두 김포골드를 「빈 차」로 보여주는 것이
  「모름」보다 훨씬 나쁘다. 배차·편성·`headwayMin`(D-57 통로)만 적는다.
- **서해선은 원천이 있는데도 뺐다(반대쪽 사고).** 대곡소사 연장 구간의 김포공항(5·9호선·공항철도·
  김포골드 합산)·부천종합운동장(7호선) 승하차가 역 이름으로 합산돼 4량·시간 3~5대 노선에 폭주
  OD 가 실렸다 — 40칸 중 10칸이 천장 200%. 환승역 합산은 부산에서 통한 설계지만 **초대형 허브가
  저빈도 노선에 붙은 조합**에서는 깨진다. 노선 절반이 그 지경이면 값이 아니라 소음이다.
- 흐름이 안 실린 구간은 0%가 아니라 **바닥 1%** — 0 은 엔진이 「안 다님」으로 읽는다.
  **천장은 200%** — 그보다 크면 값이 아니라 배차 과소(광명셔틀만 세인 금천구청 254%)의 신호다.
- 1·3·4호선의 「호선피크(|전체|)」는 코레일 구간만으로 만든 반쪽이라 버린다(실측 |전체| 가 이긴다).
- 부산 동해선·대경선은 OD 망에서 뺀다 — 「교대」가 서울 2호선 교대에 붙는 유령 환승이 생긴다.

**검증 (1·3·4호선 서울 구간 실측과 828짝):** 상관 0.83 · 기울기 0.96 · 평균오차 12.4%p.
부산(0.95)보다 낮은 것은 대조 가능한 곳이 환승 밀집 도심 구간뿐이라서다. 오차 폭은 부산의 8 대신
**12** 로 싣는다(D-104 의 무딘 곡선이 그만큼 넓어진다). 합칠 때는 큰 오차 폭이 이긴다.

파이프라인: `fetch_seoul_rail.py`(승하차 6달) → `fetch_rail_tph.py`(역별 시간표, 하루 1,000건 제한)
→ `fetch_korail_runs.py`(전동차 회/일 — 기준일 2020이라 어림) → `build_rail_congestion.py --validate`
(rail.json 생성 + routes.json 에 편성·배차·headwayMin 패치).
★ build_graph.py 를 다시 돌리면 패치가 사라진다 — 그 뒤에 이 스크립트를 다시 돌려야 한다.
검증 도구(verify_rush·verify_deep)도 이제 앱과 같은 합치기(cities+rail)를 한다 —
안 그러면 새 노선들이 「모름 = 서서」로 조용히 빠져 검증이 서울만 본다.

---

## D-109. 경기 광역버스 436개 — 승하차가 서울 모양이라 엔진을 한 줄도 안 고쳤다 (2026-09-09, 사용자가 경기 키 발급)

**왜 광역버스인가.** 경기도 자료 중 「노선별 정류장별 시간대별」 승하차를 주는 것은 광역버스뿐이다
(경기데이터드림 「경기도 광역버스 …승하차 인원」, 월 400MB CSV ×12달. 정류소별 집계 자료는
하루 총계라 시간대가 없어 못 쓴다). 마침 앉을 자리가 가장 크게 갈리는 노선들이기도 하다 —
분당→강남을 지하철로 54분 서서 가느냐, G8110 으로 27분 앉아 가느냐.

**서울 버스 파일 모양을 그대로 재사용한다.** `data/bus/routes/…json`(stopId·on[24]·off[24])을
같은 모양으로 만들면 OD 배분·요일 되돌림·유령 노선 걸러내기·「내 차는 평균보다 붐빈다」까지
엔진이 전부 그대로 돈다. 광역버스는 `busExpress`(41석·입석 금지)라 만석이면 「못 탄다」로 계산된다 —
8100 출근 첨두 재차 43명이 그대로 그 계단을 밟는다.

**잇는 열쇠는 ID 다, 이름이 아니다.** TAGO 의 nodeid 가 `GGB`+GBIS 정류소아이디라 승하차의
정류소아이디와 문자 그대로 잇는다(D-106 의 이름 맞추기 고생이 여기엔 없다).

**넘어진 곳 셋.**
1. **경기데이터드림 다운로드는 네 단계다** — 상세 페이지(쿠키) → 파일 목록 AJAX(빼면 500) →
   이용목적 신고 POST(U01) → downloadFileData. 게다가 **AJAX 는 Accept 가 json 이어야 하고**
   (text/html 이면 같은 주소가 404), UA 가 브라우저 모양이어야 하며, 몰아 부르면 몇 분 문을 닫는다
   (40초 간격 재시도로 기다린다). 전부 실측으로 확인해 fetch_ggbus.py 주석에 적었다.
2. **노선번호만으로 묶으면 안 된다.** 「3100」이 네 회사에 있다 — 번호로 합치면 네 노선의 승객이
   한 노선에 눌어붙고 셋은 사라진다. (노선번호, 운행업체명) 짝으로 가르고, TAGO 쪽은
   정류장 ID 겹침이 가장 큰 후보에 붙인다(이미 쓴 후보는 제외).
3. **「(미정차)」 통과 지점이 정류장 나열에 끼어 있다** — 고속도로 요금소·나들목까지 정류장처럼
   온다. 걷어내지 않으면 그래프에 서지 않는 역이 선다.

**구간 시간은 좌표로 잰다.** 정류장 사이가 고속도로 십수 km 일 수 있어 시내버스 상수(4.2분)로는
소요시간이 엉터리가 된다. 좌표 거리 ÷ 표정속도 30km/h, 노선마다 1.2~6분으로 자른다.

**결과.** 노선 436개(직행좌석 411·광역급행 56·좌석 14 중 매칭분) · 정류장 +3,453 · 그래프
노선 3,306개. 승하차 (노선,업체) 449짝 중 13개는 못 이었다(예약·출퇴근 변형 P노선 등 — TAGO 에 없다).
시험 177개(경기 7 추가). 실측: 서현역→강남역 08시 1위 = 경기 G8110, 「타는 28분 중 27분 앉음」,
지하철 대안은 54분 중 1분. 검증 도구 기준선 변화 없음(서울 6건·deep 0건).

**남은 한계(알고 쓸 것).** 요일 없는 하루 평균이라 엔진의 요일 비 되돌림이 서울 실측 비를 빌린다.
경기 일반 시내버스(~1,700개)는 시간대별 원천이 없어 여전히 밖이다. 배차간격(headwayMin)은
아직 없어 종류 기본(광역 5대/시 안팎)으로 나눈다 — GBIS 배차 자료가 생기면 D-57 통로로 넣는다.

---

## D-110. 실시간 도착·남은 좌석 — 통계 옆에 「지금」 한 줄, 프록시로 (2026-09-10, 사용자 지시)

**사용자의 물음이 정확했다**: 「해당 버스의 혼잡도를 실시간으로 보여줄 수 있냐? 몇 자리 남았는지」.
경기 광역버스는 **GBIS 도착정보의 `remainSeatCnt`** 가 남은 좌석 수 그 자체를 주고(D-109 로
넣은 436개 노선이 정확히 그 대상), 서울 버스는 도착 분과 혼잡 단계(여유/보통/혼잡)를 준다.

**원칙 셋.**
1. **키는 브라우저에 못 둔다** — counsel-proxy 의 존재 이유 그대로. `/bus` 라우트(유일한 GET)를
   얹어 서버가 대신 부른다. 상담과 달리 민감정보가 아예 없다(정류장 번호와 도착 초뿐).
   방어선: Origin → 60초 30회 리밋(도착 조회는 상담보다 잦은 게 정상) → **15초 캐시**
   (같은 정류장을 여럿이 봐도 공공 API 는 한 번만 맞는다). 지킬 상한은 비용이 아니라
   공공데이터포털 일일 트래픽이다. 주소는 서버가 만든다 — 클라이언트는 정류장 번호만 준다.
2. **통계와 실시간을 섞지 않는다.** 위의 확률은 「그 시각의 평균」, 이 줄은 「지금 이 순간」 —
   줄 끝에 「— 실시간」을 붙여 가른다. 확률 계산에 실시간을 끼워 넣지 않는다(원천이 다르다).
3. **실시간은 덤이다.** 프록시·공공 API 어느 쪽이 못 답해도 줄째 조용히 접는다 —
   앱은 통계로 그대로 돈다. 실측: 프록시 배포 전 상태에서 오류 0, 줄만 사라짐.

대상: 경기 광역(GG-, GBIS 노선·정류소 ID 그대로) · 서울 버스(승하차 파일의 ARS 5자리).
광역시(CB-)는 실시간 원천이 도시마다 달라 이번엔 밖, 지정석(IC-)은 예매처가 답이다.
지하철 실시간 칸별 혼잡은 SK 퍼즐뿐이라(제휴 필요) 제외.

켜는 데 필요한 세 가지(사용자 몫): 활용신청 2건(경기 15080346 · 서울 15000314) +
Deno Deploy 에 `DATA_GO_KR_KEY` 환경변수 + Playground 재배포. 프록시 시험 15개
(`counsel-proxy/tests/testbus.mjs`) — Origin·키 부재·입력 검사·캐시·리밋·키 안 새는 것.

**배포 실측(2026-09-10 00:22)**: 경기 뚫림 — 당동주공 「4분 뒤 · 40석」, 한양대입구
「9분 뒤 · 0석(만석)」, 15초 캐시 hit 확인. 필드 이름이 파서와 그대로 일치
(predictTime·remainSeatCnt·crowded). 서울은 활용신청 반영 대기(401).
★ 파일을 Playground 로 나르다 한 번 넘어졌다 ★ — PowerShell `Get-Content` 는 BOM 없는
UTF-8 을 CP949 로 읽어 **한글이 깨진 채 클립보드에 실렸고** 배포가 SyntaxError 로 터졌다.
`[System.IO.File]::ReadAllText(경로, UTF8)` 로 복사해야 한다.

---

## 아직 못 정한 것

| 항목 | 막고 있는 것 | 정해야 할 시점 |
|---|---|---|
| 환승 승차가 승차인원에 합산되는가 (사양서 4.4) | 서울시 미래첨단교통과 문의 답변 | 재차인원 계산 전 |
| T-DATA 실제 응답 필드 이름 | T-DATA 키 | 파서 확정 전 (지금은 사양서 표기 + 별칭으로 대비) |
| 공공데이터포털 각 서비스의 정확한 엔드포인트 | 활용신청 승인 | 크론 가동 전 |
| 좌석수 매칭용 노선-차종 표 | 노선마스터 | 좌석 배치도(M3) 전 |
| 경로 탐색 알고리즘 최종안 | BFS 실측 | M2-2 후 |
