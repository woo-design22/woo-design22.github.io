# soccer — 감독 문서 3: 인수인계 일지

규칙: **최신 항목이 맨 위.** 세션이 끝날 때마다 항목을 추가하고 이전 항목은 지우지 않는다.
다음 세션은 맨 위 항목과 `ROADMAP.md`의 "현재 단계"만 읽어도 이어갈 수 있어야 한다. 실측값은 숫자로.

항목 서식:
```
## 세션 N — YYYY-MM-DD — 단계 M: 제목 — 결과(완료 / 진행 중 / 막힘)
- 한 일:
- 실측값:
- 미달/알려진 문제:
- 문서 수정:
- 다음 세션 첫 할 일:
- 사용자 확인 요청: (최대 3개)
- 커밋:
```

---

## 세션 18 — 2026-08-25 — 게시 + 로비가 화면 밖으로 나가던 버그 — 완료
- 한 일:
  - **main 에 게시했다**(PR #10). 대문 「게임」 칸 첫 카드. Pages 빌드 `built`, 라이브에서 자가 검사 오류 0.
  - **로비·방·안내 패널이 화면에 안 보이던 버그를 고쳤다.** `index.html` 의 `</div>` 하나가
    `#overlay` 를 panelHome 직후에 닫아서 `panelLobby`·`panelRoom`·`panelMsg` 세 개가
    **`<body>` 직속 자식**이 돼 있었다. `#overlay` 는 `position:fixed` 로 화면을 덮지만,
    밖으로 밀려난 패널은 문서 흐름을 따라 **화면 아래**에 놓여 아예 보이지 않았다.
    「여러 명이서 하기」를 누르면 시작 화면만 사라지고 아무것도 안 나와 "멈춘 것처럼" 보였다.
  - 서버가 꺼져 있을 때 무엇을 하면 되는지 알려 주는 안내 줄 `#lbNote` 를 로비에 추가했다.
- 실측값:
  - 고치기 전: `panelLobby.parentElement` = `BODY`, 사각형 top 720 / bottom 970 (뷰포트 720) → 보이는 단추 0개.
  - 고친 뒤: `parentElement` = `overlay`, top 204 / bottom 516 → 「접속」·「뒤로」 보임.
  - 온라인 전 과정 재확인(localhost:8080): 접속 지연 1ms → 비밀번호 방 생성 → 방 화면 top 198~522 →
    나가기 → 안내 패널 top 281~439. 콘솔 오류 0.
- 미달/알려진 문제:
  - **KV 미연결**(`store: memory`) — 그대로다.
  - **이 버그는 `?selftest=1` 자가 검사를 통과했다.** 자가 검사가 DOM 배치를 보지 않기 때문이다.
    화면 요소를 건드린 뒤에는 `getBoundingClientRect()` 로 **뷰포트 안에 있는지까지** 재야 한다.
- 이어서 고친 것(같은 날):
  - **게시판이 주소를 잊어 "서버가 없습니다"가 뜨던 문제.** 서버·터널은 5시간째 멀쩡했는데
    게시판이 KV 없이 메모리로 돌아 **앱이 잠들 때 주소를 잊었다**(실측: 올린 지 10분 뒤 빈 값).
    → `start-online.ps1` 이 터널이 사는 동안 **90초마다 같은 주소를 다시 올린다**. 잠들지도, 잊지도 않는다.
  - **주소를 손으로 넣지 않게 했다.** 게시판에서 주소를 받았고 저장된 아이디(`soc_nick`)가 있으면
    `maybeAutoConnect()` 가 스스로 접속한다. 첫 방문(아이디 없음)에는 자동 접속하지 않는다 — 이름은 본인이 정해야 한다.
    실측: 두 번째 방문부터 「여러 명이서 하기」 **클릭 한 번**으로 접속됨 · 지연 15ms · 방 목록까지 표시.
  - **로비에서 6초 만에 "응답이 없어 퇴장"되던 문제.** `startMonitor` 가 **접속 순간부터** 2초마다 핑을
    쏘고 3번 놓치면 끊었다. 브라우저가 숨은 탭을 얼리면 핑에 답할 수 없으므로, 다른 창을 잠깐 보는 것만으로
    쫓겨났다. → **경기 중(`r.phase === "playing"`)일 때만** 퇴장시키고, 기준도 "연속 3회"가 아니라
    **마지막 응답 이후 12초**(`inMatch.quietMs`)로 바꿨다. 경기가 아닐 때 잰 지연은 판정에 쓰지 않는다.
    실측: 로비에서 pong 을 22초 동안 막아도 접속 유지(전에는 6초에 끊김).
  - 게시판이 빈 값을 주면 클라이언트가 **2.5초 뒤 두 번까지 다시 물어본다**(`regTried`).
    메모리 저장이라 인스턴스가 바뀌면 빈 값이 오는 일이 있어서다.
  - **혼자 하기 인원 고르기.** 시작 화면에 「대칭」(1:1~10:10)·「비대칭」(우리 편·상대를 따로) 두 탭.
    실측: 5:3 → 팀0 5명 팀1 3명, 10:10 → 20명에 틱 2.05ms(예산 50ms). 고른 값은 `soc_settings` 에 남는다.
  - **캐릭터가 손그림 PNG 가 됐다**(`chars/*.png`, 6종 × 6동작 + 방귀 이펙트 = 37장, 1.7MB, 사용자 제공).
    도트 폴백은 그대로 남겼다. 그림에는 팀 색이 없어서 **발밑 원반을 팀 색으로 칠했다** — 안 그러면 아군·적군이 구분되지 않는다.
    골키퍼 그림이 없어 덩치형 그림을 빌려 쓴다. 방귀는 `fart-effect.png` 를 판정 사각형에 정확히 맞춰 늘린다.
  - **슛이 상대를 기절시키던 버그.** D 를 누른 채 슛을 쏘면 쿨다운이 끝나는 순간 같은 입력이 앞차기를 냈다.
    `p.kickLatch` 로 손을 뗄 때까지 막는다. 실측: D 유지 40틱 → shoot 1 · hit 0 · 상대 stunT 0.
  - **충전을 공 없이도 모은다.** 드리블(`chargeD`)과 감아차기(`chargeQ`)를 분리해 동시에 누를 수 있다.
    W+E 10틱 뒤 떼기 → 거리 225 · 회전 0.0267(감아차기 단독 0.052의 절반) = **짧게 휘는 드리블**.
  - 8방향 확인: 0·45·90·135·180·225·270·315도. 그림이 앞모습 한 장뿐이라 **발밑 흰 쐐기**로 방향을 보인다.
  - 달리기 2프레임(`-run2.png` 6장을 PIL 로 생성 — 허리 아래만 좌우 반전).
  - 골대 안쪽을 잔디로 칠하고, 트랙 + **관중석 네 면**을 그렸다. 골이 들어가면 2.2초간 크게 응원한다.
    카메라를 경기장 밖 62(`CAM_OUT`)까지 허용해야 관중석이 보인다.
  - `PROTOCOL_VERSION` 13 → 14(입력 해석이 바뀌었으므로 옛 클라이언트는 4002 로 거부된다).
  - **하프타임·종료 상황판**(`#panelBoard`). 점수 + 선수별 **골 · 유효슈팅 · 결정적 수비** 표,
    종료 화면에는 **승리 팀**을 팀 색으로 크게 적고 **MOM** 한 명을 뽑는다.
    기록은 `state.stats` 에 쌓고 스냅샷이 아니라 `half`·`end` 이벤트에 실어 보낸다.
    실측(3:3, 전후반 90초): 하프 「1반 2 : 6 2반 · 2반 앞서는 중」, 종료 「2반 승리! · 오늘의 선수 5번(골 5)」.
  - **결정적 수비 기준을 두 번 좁혔다** — 제한 없음 11개 → 우리 진영 절반 18개 → **우리 진영 3분의 1** 0~2개.
    발차기 연타가 곧 수비 기록이 되면 숫자가 의미를 잃는다.
  - **슛 세기를 6종 모두 1.00 으로 통일**(2026-08-26 지시). 이제 캐릭터 차이는 발차기 범위와 필살기뿐이다.
  - **기록에 도움·기절시킴 추가.** 도움은 `ball.assist`(골 넣은 선수 직전에 찬 우리 팀 선수, 상대가 건드리면 끊김),
    기절시킴은 **한 번에 세 명이면 3개**. MOM 가중치를 골 10 · 도움 6 · 유효슈팅 2 · 수비 2 · 기절 0.25 로 바꿨다
    (실측: 같은 무게면 골 2 + 기절 36 이 골 3 을 이겼다).
  - **앞차기 판정을 부채꼴 → 앞쪽 정사각형**으로 바꾸고 클라이언트가 같은 네모를 그린다(`kickHalf` 28.7).
    실측: 앞 0~57 맞음 · 62 빗나감, 옆 25 맞음 · 32 빗나감, 뒤 −20 빗나감. 넓이는 옛 부채꼴 3,284 ≈ 3,294 로 맞췄다.
  - **기절에서 깨어나면 눌러 둔 버튼이 즉시 먹힌다.** 깨는 틱에 `prevButtons` 를 0 으로 지운다.
    실측: 기절 34틱 내내 S 를 누르고 있으면 **깨어난 바로 그 틱(t34)** 에 방귀가 나갔다.
  - **인공지능 성향 3종**(`slot % 3`) — 공격형·중원형·수비형. 세로 폭(lane)도 150/90/55 로 갈랐다.
    같은 판단을 하던 봇들이 한 덩어리로 몰려다니는 문제를 줄이려는 것.
  - **감아차기 거리 = 슛 × 1.2**(400 → 480).
  - **캐릭터 9종**(사용자가 새 팩 제공, 95장 4.7MB). 달리기는 원본의 `-run-1/2/3` 3프레임을 쓴다
    (내가 만들었던 `-run2` 는 지웠다 — 예쁜이 하반신이 잘리던 원인이었다). 골키퍼 자리는 charId **9**.
  - **방귀·발차기 범위를 전 캐릭터 동일**(`kickRange` 전부 1.00, 캡틴 기준).
  - **경기장 4종·공 3종**을 시작 화면에서 고른다. 경기장 크기는 `state.W/H` 로 흐르고 스냅샷 헤더에
    경기장 번호 1바이트를 실었다(29 → 30B). 관중 밀도·관중석 폭도 규모를 따른다.
  - **전·후반 2분, 동점이면 연장 30초씩 2하프, 그래도 동점이면 무승부.** 하프가 4개라 스냅샷의
    half 를 1비트 → 2비트로 바꿨다. 실측 0:0 → 하프1→2→3→4→종료 0:0.
  - **논스톱 슛**: 공을 잡지 않은 채 발 앞 26 안의 공을 D 로 차면 1.3배(속도 832 · 거리 520).
    범위 밖이면 평범한 앞차기. **발차기·방귀를 쓰면 감아차기·드리블 게이지가 지워진다.**
  - **판정 네모를 걷어내고 이펙트만** 남겼다(크기는 판정 사각형에 그대로 맞춘다).
  - **상황판**: 글자·패널을 키우고 **팀별로 순위**를 매긴다. **MOM 은 승패와 무관**하게 뽑는다.
  - **경기 중 나가기**(HUD 「나가기」 · Esc). 혼자 하기는 시작 화면으로, 온라인은 방을 떠난다.
  - **수비 인공지능을 팀마다 하나 보장**(그 팀 마지막 슬롯). 나머지는 공격형·중원형이 번갈아.
  - `PROTOCOL_VERSION` 14 → 15.
  - **대기 운동장**: 접속하면 공용 프로구장에 모여 걸어 다니고, 방에 들어가면 그 방이 고른
    경기장으로 옮겨 방 사람들끼리만 논다. 방 목록·방 화면은 오른쪽 좁은 창(`#dock`)으로 옮겼다.
    실측: 접속 → mode lobby · field pro, 방 만들기 → field school 로 전환 · 방 UI 가 dock 으로 이동.
  - **채팅**: 머리 위 말풍선 + 왼쪽 아래 최근 5줄(배경 없음, 9초에 걸쳐 사라짐). 혼자 하기도 동일.
  - **방장 강퇴**(`room.kick`), 방 안 캐릭터 변경이 대기 운동장 몸에도 반영.
  - **「여러 명이서 하기」 → 「온라인」**, 시작 단추와 나란히 크게(파란색).
  - **경기 초반에 도트로 보이던 이유 = 그림을 그때서야 받기 시작해서**였다.
    `SoccerSprite.preload` 로 미리 받고, 못 받은 동작은 서 있는 그림으로 대신한다.
  - 농구부 캐릭터 그림 10장 교체(사용자 재업로드).
  - **온라인에서 좌우 이동이 안 되던 버그** — `encodeInput` 의 **인자 차례가 바이트 차례와 달랐다**.
    선언은 `(seq, dx, dy, buttons)` 인데 부르는 쪽(클라이언트·loadtest 둘 다)은 `(seq, buttons, dx, dy)` 로 넘겼다.
    그래서 가로 입력이 세로로 가고 버튼이 가로 이동이 됐다. 선언을 부르는 쪽에 맞췄다.
    **연습장은 이 함수를 안 거쳐서 멀쩡했기 때문에** 그동안 못 잡았다 — 온라인 조작은 반드시 실제 접속으로 재야 한다.
    실측(온라인): →+151x · ←−151x · ↓+151y · ↑−151y · 대각선 (95,95) facing 45°, 방귀·발차기 동작도 확인.
  - **온라인 체감 지연**을 세 군데에서 줄였다: 보간 버퍼 100 → 60ms · 입력을 바뀐 순간 즉시 전송
    (20Hz 는 그대로 유지) · **내 선수만 로컬 예측**(서버 값으로 0.12초 시상수로 수렴, 60 벌어지면 스냅).
    실측 서버 반영 지연 중앙값 50 → 34~61ms. 기절·동작 중에는 예측하지 않는다.
  - **채팅 자리**: 경기 전에는 오른쪽 창 안, 경기 중에는 왼쪽 아래. 보내면 입력창이 곧바로 닫힌다.
  - **대기 운동장은 점수가 없다**(`state.friendly`). 공은 그대로 굴러다니되 골대에 넣어도 아무 일도 없고
    점수판도 감춘다. 실측: 같은 상황에서 정식 경기 골 1·점수 0:1 / 대기 운동장 골 0·점수 0:0, 공은 골망 안(x −28)에 남는다.
  - **내가 몰고 있는 공도 예측**한다 — 나만 앞서 가고 공이 뒤따라오면 드리블이 가장 늦게 느껴진다.
    공 주인이 나일 때만이고, 놓는 순간부터는 서버 값 그대로다.
  - **틱 20Hz → 40Hz.** 시간을 틱 개수로 세는 수치 27개를 **정확히 2배**로 올려 실제 시간은 그대로 뒀다.
    30Hz 를 먼저 해 봤는데 0.05초(1.5틱)·0.25초(7.5틱)가 정수가 안 돼 네 값이 17ms 씩 어긋나서 버렸다.
    40 은 20 의 정수배라 반올림이 없다. **초 단위 대조표 27개 전부 일치**.
    걷기 1초 168 · 달리기 1초 252 · 체력 2초도 동일. **슛 거리만 368 → 383(+4%)** — 감속을 더 잘게
    적분해 설정값 400 에 가까워진 것이다. 되돌리려면 `shoot.range` 를 384 로 내리면 된다(사용자 확인 필요).
    보간 버퍼도 60 → 30ms(스냅샷 간격이 절반이라 여유는 같다). `PROTOCOL_VERSION` 15 → 16.
    실측 부하: 20명 틱 0.015ms(코어 0.1%), 스냅샷 290B × 40Hz = 한 명 11.3KB/s, 20명 방 1.86Mbps.
  - **40Hz 로 올리며 놓친 값: `spin`.** 이건 «틱마다 도는 각(라디안)»이라 주파수를 두 배로 올리면
    **초당 회전이 두 배**가 된다. 감아차기가 두 배로 휘었다(사용자 신고). `curve.spinMin/spinMax`,
    `dribble.spinMax` 를 절반으로 내려 되돌렸다. `turnMax` 는 «총 회전량»이라 그대로 둔다.
    실측 옆으로 벗어난 거리 — 최대 충전 20Hz 272 / 고치기 전 40Hz 351 / **고친 뒤 268**,
    최소 충전 63 / 122 / **62**, 휘는 드리블 108 / — / **106**.
  - **교훈: 주파수를 바꿀 때 확인할 것은 「Ticks」로 끝나는 이름만이 아니다.** 틱마다 더하거나 빼는 값
    (`staminaDrain`·`staminaRegen`·`spin`)도 전부 비율을 맞춰야 한다. 지금 그런 값은 이 셋뿐이다.
- 문서 수정: 이 항목.
- 다음 세션 첫 할 일: (선택) Deno KV 연결, 반 번호 선택 UI, 경기 중 난입.
- 커밋: `fix(soccer): 로비·방 화면이 화면 밖에 그려지던 문제`

---
## 세션 17 — 2026-08-25 — 주소 게시판(바뀌는 터널 주소를 대문이 자동으로 따라가기) — 완료(배포 대기)
- 배경: 사용자가 "주소가 계속 바뀌어도 대문에서 클릭할 때마다 그 주소로 연결하게 못 하냐"고 물었다.
  고정 주소를 사려면 도메인이 필요하고(연 1.5만원), ngrok 무료 고정 주소는 한국에서 편도 33ms(왕복 70~90ms)라 게이트 턱걸이였다.
  **주소를 적어 두는 고정된 자리 하나만 있으면 터널은 지금 그대로 두고 해결된다**고 판단했다.
- 만든 것:
  - **`server/url-registry.js`** — Deno Deploy 용 40줄. `GET /` 현재 주소, `POST /` 로 갱신(WRITE_KEY 필요), Deno KV 에 보관.
    30분 넘게 갱신이 없으면 `stale: true`. 이미 Deno Deploy 를 쓰고 계셔서 **새 가입이 없다.**
  - **`start-online.ps1` 4단계 추가** — 터널을 연 뒤 게시판에 주소를 올리고, 스크립트가 끝날 때 게시판을 **비운다**(꺼진 주소로 붙는 것 방지).
    설정은 `server/registry.txt`(1줄 주소 / 2줄 키), 없으면 그냥 건너뛴다. `.gitignore` 에 넣었다.
  - **클라이언트 자동 조회** — 온라인 화면을 열 때 서버 주소 칸이 비어 있으면 게시판을 읽어 채운다.
    우선순위 ①`?server=` ②직접 입력 ③게시판 ④이 페이지를 준 서버. `REGISTRY` 상수가 비면 꺼진 것과 같다.
- 실측값:
  - 게시판을 로컬로 흉내 내어(`?registry=http://127.0.0.1:8123`) 확인: 온라인 화면 진입 → **서버 주소 칸이 자동으로 채워짐**,
    상태 문구 "서버 주소를 받았습니다". `file://` 자가검사 `errors: []`, PowerShell·JS 구문 검사 통과.
  - 대안 비교(실측): ngrok 접속점 편도 **도쿄 33ms · 싱가포르 71ms · 미국 192ms**, Cloudflare 서울 **5ms**.
    ngrok 무료는 고정 주소를 주지만 월 1GB·2만 요청 제한이 있고 왕복 70~90ms 로 게이트(100ms) 턱걸이라 권하지 않았다.
- 배포 결과(같은 세션에서 이어서):
  - 게시판을 Deno Deploy 에 올렸다 → **`https://gray-macaque-4849.woo-design22.deno.net`**. `index.html` 의 `REGISTRY` 상수에 넣었다.
  - 처음 배포가 **`Deno.openKv() failed: no KV database is attached`** 로 통째로 실패했다. KV 를 안 붙이면 앱이 아예 안 뜬다.
    → `openKv()` 를 try/catch 로 감싸 **KV 없으면 메모리로 자동 전환**하도록 고쳤다. 응답의 `store` 로 어느 쪽인지 확인한다.
  - **Deno Deploy Classic 이 2026-07-20 에 종료**돼서 주소 형식이 바뀌었다. `*.deno.dev` 는 404(`DEPLOYMENT_NOT_FOUND`),
    새 형식은 `https://<앱>.<조직>.deno.net` 이다. 이것 때문에 주소를 한 번 헛짚었다.
  - 실측: `GET /` → `{"url":"","at":0,"stale":true,"store":"memory"}` (200, CORS `*` 정상).
    게임 페이지가 실제로 조회해 **"지금은 열려 있는 서버가 없습니다"** 를 표시하는 것까지 확인했다.
- 미달/알려진 문제:
  - **`server/registry.txt` 미작성** — 남은 마지막 한 걸음. 1줄에 게시판 주소, 2줄에 `WRITE_KEY` 를 넣어야
    `start-online.ps1` 이 터널 주소를 게시판에 올린다. **키는 자격증명이라 내가 대신 넣지 않는다**(사용자가 직접 만든다).
    그 전까지는 게시판이 비어 있어 대문 사본에서 "지금은 열려 있는 서버가 없습니다" 가 뜬다(정상 동작).
  - **KV 미연결** — 지금은 `store: memory`. 앱이 잠들면 주소를 잊는다(터널을 다시 켜면 복구). 대시보드에서 KV 를 붙이면 해결된다.
  - 게시판이 죽으면 대문 사본에서 자동 연결이 안 된다(그때는 주소를 직접 붙여넣으면 된다).
  - 게시판을 읽으면 **누구나 현재 터널 주소를 알 수 있다.** 방 비밀번호가 실제 차단 수단이라는 점은 그대로다.
- 문서 수정: `CLAUDE.md` §9.1 신설(구조·안전장치·우선순위·비밀 보관).
- 다음 세션 첫 할 일: 사용자가 게시판을 배포하고 주소를 주면 `REGISTRY` 상수 채우고 실제 대문 사본으로 끝까지 확인.
- 사용자 확인 요청:
  1. Deno Playground 에 게시판을 올릴지(새 가입 없음, 기존 계정 사용).
  2. 대문에는 혼자 하기만 올릴지, 온라인까지 열지.
- 커밋: 없음(사용자 지시 대기).

---

## 세션 16 — 2026-08-25 — 단계 7 배포(내 PC + Cloudflare 빠른 터널) — 완료
- 배경: 사용자가 "내 컴퓨터에서 무료로"를 골랐고, **예전에 Cloudflare 로 막힌 적이 있다**고 했다. 그래서 짐작하지 않고 순서대로 확인했다.
- 확인한 것:
  1. **무설치 대안 먼저 시험** — `ssh -R 80:localhost:8080 nokey@localhost.run`(윈도우 기본 ssh, 계정 없음).
     터널 자체는 되고 wss 도 통과했지만 **왕복 411ms** 라 게임의 지연 게이트에서 거부됐다. 실시간 게임에는 못 쓴다.
  2. **막힐 만한 지점을 미리 측정** — Cloudflare 서울 3ms, `region1/2.v2.argotunnel.com` TCP 7844 둘 다 열림, `api.trycloudflare.com` 응답.
     네트워크 요인은 없다고 판단했다.
  3. **예전에 막힌 이유 특정** — Cloudflare 터널은 두 가지다. `tunnel login → create → route dns`(**계정 + 본인 도메인 필요**, 브라우저 로그인)와
     `tunnel --url`(아무것도 필요 없음). 예전에 막힌 것은 앞쪽이고, 이번에 쓴 것은 뒤쪽이다.
  4. 사용자 허락을 받고 `winget install --id Cloudflare.cloudflared` (2026.8.2) 설치 → 빠른 터널 실행.
- 실측값:
  | 항목 | localhost.run | **Cloudflare 빠른 터널** |
  |---|---|---|
  | 접속 지연(서버가 잰 왕복) | 411ms → **게이트 거부** | **중앙값 14ms · 상위10% 26ms → 통과** |
  | 붙은 노드 | (해외) | **icn06 (서울)** |
  | 계정·도메인·로그인 | 불필요 | **불필요** |
  - 터널 주소로 전 과정 재현: 입장 4/4 · 틀린 비번 차단 · 미준비/방장 아님 시작 차단 · 스냅샷 120개 **20.2Hz · 81B** · 채팅 수신.
  - 페이지도 https 로 정상(200, 88,860B).
- 만든 것:
  - **`server/start-online.ps1`** — 서버(8080) + 터널을 함께 띄우고 주소를 크게 찍는다. 이미 떠 있으면 그것을 쓰고 `npm install` 도 알아서 한다.
  - **로비에 「서버 주소」 칸** — 대문(GitHub Pages)에 올린 사본은 정적이라 서버가 없다. 그 사본에서 온라인을 하려면 여기에 그날 터널 주소를 넣는다.
    `?server=https://…` 주소 파라미터로도 되고, 입력값은 `soc_server` 에 저장된다. 비우면 이 페이지를 준 서버로 붙는다(기본).
  - `loadtest.js --url` — 터널 주소에 대고 같은 검사를 그대로 돌린다.
- 미달/알려진 문제:
  - **주소가 켤 때마다 바뀐다.** 고정 주소는 Cloudflare 계정 + 본인 도메인이 있어야 한다. 대문에 고정 링크를 걸 수 없다는 뜻이다.
  - **PC 를 켜 두어야 한다.** 창을 닫으면 접속이 끊긴다. 재부팅 자동 시작은 일부러 안 넣었다.
  - **터널이 열린 동안은 누구나 그 주소로 들어올 수 있다.** 방 비밀번호가 유일한 차단 수단이다.
  - 대문에 올릴 정적 사본은 **혼자 하기만** 바로 되고, 온라인은 서버 주소를 넣어야 한다.
- 문서 수정: `CLAUDE.md` §9 를 Lightsail → **내 PC + 빠른 터널**로 교체(이유·실측·한계 포함). `ROADMAP.md` 7단계를 같은 내용으로 교체하고 체크.
- 다음 세션 첫 할 일: 대문 게시(정적 사본) 여부를 사용자가 정하면 그 작업. 아니면 3단계 미달 항목(난입·반 번호 UI).
- 사용자 확인 요청:
  1. 대문에는 **혼자 하기만** 올릴지, 서버 주소 칸 안내까지 같이 올릴지.
  2. 고정 주소가 필요한지(= Cloudflare 계정 + 도메인을 만들지).
- 커밋: 없음(사용자 지시 대기).

---

## 세션 15 — 2026-08-25 — 단계 2·3·4 동시 진행(사용자 요청: 여러 명이서 플레이) — 대부분 완료
- 배경: 사용자가 "대문에 올릴 건데 여러 명이서 플레이하는 기능"을 요청해 **한 세션에 한 단계** 원칙을 깨고 2·3·4단계를 함께 했다.
  미달 항목은 각 단계 체크리스트에 `[ ]`·`[~]` 로 남겨 두었다.
- 만든 것:
  - **`server/server.js`** — 정적 파일 + WebSocket 경기 서버. `../game-core.js` 를 그대로 `require` 하므로 물리가 브라우저와 갈라지지 않는다.
    방 관리, 20Hz 틱 루프, 바이너리 스냅샷 브로드캐스트, 지연 게이트, 경기 중 감시, 채팅, 방장 승계, 빈 방 삭제, `/stats`.
  - **`server/package.json`** — 의존성 `ws` 하나.
  - **`server/loadtest.js`** — 사람 없이 전 과정을 재현하는 검증 도구(`--players --rooms --delay --drop --delayAfter`).
  - **클라이언트 온라인 모드** — 시작 화면에 **혼자 하기 / 여러 명이서 하기**. 아이디 입력(회원가입 없음, `soc_nick` 에 저장),
    방 목록(2초 갱신)·방 만들기(이름·인원·전후반·**비밀번호**)·입장·팀/캐릭터 선택·준비·**방장만 시작**·채팅·퇴장 안내.
    경기 화면은 1단계 렌더 경로를 그대로 쓰고 스냅샷만 서버에서 받는다.
- 실측값:
  | 항목 | 값 |
  |---|---|
  | 틱 루프 | `setInterval(50ms)` **16.3Hz** → 드리프트 보정 루프 **20.1Hz** |
  | 브라우저 수신 스냅샷 | **19.8~20.2Hz**, 2명 55B · 4명 81B |
  | `/stats` `tickP95Ms` | **0.053~0.178ms** (기준 <5) |
  | 지연 게이트 | `--delay 200` → 전원 **4001**(중앙값 204·p90 216), `--drop 1` → 전원 **4001**(missing 1) |
  | 경기 중 감시 | `--delayAfter 200` → **둘 다 퇴장**("지연이 커서 퇴장되었습니다 (중앙값 203ms)") |
  | 비밀번호 | 틀리면 `error` 로 막히고 소켓 유지 |
  | 시작 조건 | 미준비 시 `"모두 준비해야 시작할 수 있습니다"`, 방장 아니면 `"방장만 시작할 수 있습니다"` |
  | 브라우저↔Node 실제 대전 | 1:1 방 생성 → 입장 → 양쪽 준비 → 시작 → 렌더 `phase play`·`tick 739`·`drawn 2`·공 (607,400) |
  - `file://` 자가검사 `errors: []`, 서버 `console.error` 0.
- 미달/알려진 문제 (중요):
  - ⚠ **GitHub Pages 에는 이 기능을 올릴 수 없다.** Pages 는 정적 호스팅이라 WebSocket 서버가 못 돈다.
    대문에 올리면 **혼자 하기만 동작**하고 "여러 명이서 하기"는 접속에 실패한다. 온라인을 공개하려면 서버를 어딘가에 띄워야 한다
    (설계도 §9 의 Lightsail 서울, 또는 사용자 PC + Cloudflare 터널). 이건 7단계 몫이고 **사용자 결정이 필요하다**.
  - **재접속 미구현**(4단계). 끊기면 안내 화면 + 다시 시도로 처음부터 붙는다.
  - **경기 중 난입 미구현**(3단계). 경기 중인 방은 목록에서 잠긴다.
  - **시계 동기(offset) 미구현**. 스냅샷 도착 로컬 시각으로 보간한다 — 설계 변경이라 §6 에 이유를 적었다.
  - **반 번호 선택 UI 없음**. 서버는 `classA/classB` 를 받지만 방 만들기 화면에 입력란이 없어 항상 1반 vs 2반이다.
  - 같은 IP 동시 접속 상한을 3 → 4 로 올리고 **로컬은 면제**했다. 같은 PC 에서 탭 두 개로 붙는 것과 `loadtest` 가 막혔기 때문이다.
  - 온라인에서는 `debug.raw()` 가 `null` 이다(서버만 state 를 갖는다). `charOfSlot`·`onScreenSlot`·`stats()` 를 그에 맞게 고쳤다.
- 문서 수정: `CLAUDE.md` §1(모드 둘·회원가입 없음)·§6(비밀번호·방장·시작 조건·새 메시지·시계 동기 변경)·§7(`debug.net`·`connect`)·§8(서버 실행법).
  `ROADMAP.md` 2·3·4단계 항목별 실측 기록과 미달 표시, "현재 단계" 갱신. 루트 `CLAUDE.md` 의 "서버는 아직 없다" 수정.
- 다음 세션 첫 할 일: 사용자가 **온라인 공개 방식**(Lightsail vs 터널 vs 비공개)을 정하면 7단계 배포. 정하기 전이면 3단계 미달 항목(난입·반 번호 UI)부터.
- 사용자 확인 요청:
  1. **대문 공개 범위** — 혼자 하기만 올릴지, 서버까지 띄워 온라인도 열지.
  2. 방 만들기에 **반 번호 선택**을 넣을지.
  3. 경기 중 **난입**을 허용할지.
- 커밋: 없음(사용자 지시 대기).

## 세션 14 — 2026-08-25 — 단계 1 13차 개편(감아차기 거리 / 방귀 쿨다운 / BGM 원인 추적) — 완료
- 한 일:
  1. **감아차기 거리 = 슛 거리** — `curve.range` 800 → **400**. 이제 이 둘은 항상 같은 값을 유지해야 한다(설계도 §5.2에 못 박았다).
  2. **방귀 쿨다운 절반** — `fartCdTicks` 16 → **8**(0.8초 → 0.4초). 앞차기 쿨다운(0.6초)보다 짧아졌다.
  3. **"음악이 안 나온다"의 원인 추적** — 짐작하지 않고 **사용자의 실제 크롬**에서 직접 확인했다:
     · `localStorage` 의 `soc_settings` = `{sfx:true, bgm:true}` → 설정 문제 아님
     · 진짜 클릭(신뢰된 제스처) 뒤 `debug.audio()` = `{ready:true, state:"running", bgmRunning:true, level:0.1019}` → **소리는 나가고 있었다**
     · 같은 잣대로 효과음을 재니 발차기 0.114 · 슛 0.200 · 골 0.148 → **BGM(0.10~0.13)은 효과음과 비슷한 크기**
     즉 오디오 그래프는 정상이고, 남는 원인은 (a) 음악이 "음악처럼" 안 들려서 배경 잡음으로 흘려듣거나 (b) 페이지 밖(탭 음소거·볼륨 믹서·출력 장치)이다.
     그래서 두 가지를 했다 —
     · **멜로디 한 줄 추가**(`MEL`, 마디마다 4분음표 4개). 화음만 굴리면 "웅웅"으로 들려서 음악으로 인식되지 않는다. `bgmG` 0.55 → 0.75.
     · **♪ 단추가 출력 세기에 맞춰 빛나게** 했다. **빛나는데 안 들리면 원인은 페이지 밖**이라고 사용자가 바로 판단할 수 있다.
- 실측값:
  | 항목 | 전 | 후 |
  |---|---|---|
  | 감아차기 실제 이동 경로 | 373(설정 800 이었으나 회전으로 짧아짐) | **373** (슛 365 와 같음, 둘 다 설정 400) |
  | 방귀 최소 간격 | 16틱(0.8초) | **8틱(0.4초)** — 4초에 10회, 간격 전부 8틱 |
  | BGM 마스터 RMS | 최대 0.101 · 평균 0.047 · 최소 0.018 | **최대 0.130 · 평균 0.060 · 최소 0.026** |
  | ♪ 단추 표시 | 없음 | `box-shadow: rgba(163,230,53,0.67) 0 0 12px` (실측) |
  - 결정성: seed1 600틱 **2213785294** — Node = `file://` 동일. `file://` `errors: []`.
  - 진짜 키보드(CDP): 드리블 `ballAhead` 76 · 슛 speed 640 · 감아 spin −0.052 · 방귀 1 · 필살 1 · 효과음 12종 `sfxOk: "ok"`.
- 미달/알려진 문제:
  - **음악이 여전히 안 들린다면 페이지 밖 원인이다.** ♪ 단추가 빛나는지 먼저 보고, 빛나면
    ① 크롬 탭 음소거(탭 우클릭 → 사이트 음소거 해제) ② 윈도우 볼륨 믹서에서 브라우저 볼륨 ③ 출력 장치 순으로 확인해야 한다.
    페이지 안에서 더 할 수 있는 것은 음량을 더 올리는 것뿐인데, 이미 효과음과 같은 크기라 더 올리면 효과음이 묻힌다.
  - 방귀 쿨다운이 앞차기보다 짧아졌다(0.4 vs 0.6초). **기절 잠금이 방귀로 더 쉬워졌다** — 면역이 없는 상태라 0.4초마다 2초 기절을 걸 수 있다.
    7차의 "자유 16%"보다 나빠졌을 것이다(재측정하지 않았다).
  - 감아차기는 회전 때문에 직선 거리보다 경로가 길어, 같은 `range` 400 이어도 **골대까지 도달하는 직선 거리는 슛보다 짧다**.
- 문서 수정: `CLAUDE.md` §5.2(감아차기 거리 = 슛 거리 규칙, 방귀 쿨다운 0.4초와 연타 판정)·§5.5(멜로디, ♪ 표시등, 소리 실측 수치 갱신).
  `ROADMAP.md` 1단계에 13차 조건 2개 추가와 BGM 항목 갱신.
- 다음 세션 첫 할 일: ROADMAP 2단계(서버 코어 + 온라인 1:1). 변경 없음.
- 사용자 확인 요청:
  1. **♪ 단추가 빛나는지** — 빛나는데도 안 들리면 페이지 밖 문제이고, 안 빛나면 다시 파고들 지점이 생긴다.
  2. 방귀를 0.4초마다 쓸 수 있게 된 뒤 **기절 잠금이 심해지지 않았는지**.
  3. 감아차기가 슛과 같은 거리로 느껴지는지.
- 커밋: 없음(사용자 지시 대기).

## 세션 13 — 2026-08-25 — 단계 1 12차 개편(그래픽·판정 일치 / 감속 곡선 / BGM 가청화) — 완료
- 한 일:
  1. **방귀 그래픽이 판정과 다른 자리에 그려지던 것** — 원인은 **화면 보간**이었다. 동작 중 선수는 서버에서 완전히 고정돼 있는데
     클라이언트는 스냅샷 두 장 사이를 계속 보간해서, 화면 좌표·각도가 판정에 쓰인 값보다 뒤처졌다.
     `sample()` 에서 **`motionName` 이 `fart`·`kick` 인 선수는 보간하지 않고 최신 스냅샷 값을 그대로 쓰도록** 바꿨다.
     11차에 넣었던 "동작 시작 각도 기억"(`animState.facing`)은 이제 필요 없어져 그리기 코드를 `p.facing` 으로 되돌렸다.
  2. **공이 갑자기 멈추던 것** — 선형 감속(`slowMin + (1−slowMin)·frac`)은 **끝에서 속도가 slowMin 인 채로 뚝 끊긴다**.
     남은 거리의 **제곱근**에 비례하도록 바꿨다 = 실제 공처럼 **일정한 감속도**라 0 에 수렴하며 멎는다. `slowMin` 은 0.06(바닥값)으로 낮췄다.
  3. **BGM 이 안 들리던 것** — 재생은 되고 있었지만 **너무 작았다.** 마스터 출력에 분석 노드를 달아 재 보니 RMS 최대 0.027 · 평균 0.004 · 표본 다수가 0 이었다.
     · `bgmG` 0.20 → 0.55 · 아르페지오 0.075 → 0.17(길이 0.16 → 0.26 초로 늘려 음 사이가 안 비게) · 베이스·하이햇도 상향
     · **마디를 덮는 패드 두 음 추가**. 처음엔 `tone()` 으로 넣었는데 지수 감쇠라 0.5초면 사라져 소용이 없었다 —
       "올라가서 유지하다 내려오는" 전용 `pad()` 포락선을 새로 만들었다.
  4. (곁가지) **정적 서버가 캐시 헤더를 안 보내던 문제** — 브라우저가 `game-core.js`·`sprites.js` 를 캐시해 두어서
     확인할 때마다 Ctrl+F5 를 눌러야 했다(실제로 이번에 옛 코드로 검사가 돌아 한 번 헛다리를 짚었다).
     `.claude/launch.json` 의 정적 서버를 **`Cache-Control: no-store` 를 붙이는 핸들러 + `ThreadingHTTPServer`** 로 바꿨다
     (단일 스레드라 탭 하나가 연결을 물고 있으면 다른 요청이 막히던 문제도 같이 해결).
- 실측값:
  | 항목 | 전 | 후 |
  |---|---|---|
  | 방귀 4틱 동안 서버 좌표 | (고정) | (635.6, 435.6, 45°) 고정 |
  | 같은 순간 화면이 쓰는 값 | 보간되어 뒤처짐 | **(635.8, 435.8, 45°)** — 차이는 스냅샷 ×4 양자화(≤0.25단위)뿐 |
  | 슛 틱당 이동 | 16틱에 30.8 → 18.5 (뚝 끊김) | **20틱에 30.8 → 29.5 → … → 6.6 → 3** (0 에 수렴) |
  | 슛 총거리 | 368 | **368**(변화 없음) |
  | BGM 마스터 RMS | 최대 0.027 · 평균 0.004 · 다수 0 | **최대 0.101 · 평균 0.047 · 최소 0.018** |
  - 결정성: seed1 600틱 **2371398952** — Node = `file://` 동일. `file://` `errors: []`, 콘솔 0건.
  - 서버 응답 헤더 `Cache-Control: no-store, must-revalidate` 확인, 서빙되는 `PROTOCOL_VERSION 12` · `fartHalf 44.26` · `slowMin 0.06` 확인.
- 미달/알려진 문제:
  - 화면 좌표와 판정 좌표의 **0.25단위 차이는 스냅샷 형식(좌표 ×4 고정소수) 때문**이라 더 줄이려면 프로토콜을 바꿔야 한다.
    화면에서 0.5px 수준이라 그대로 둔다.
  - 동작 중 보간을 끄면서 **방귀·발차기 시작 순간 캐릭터가 아주 살짝 튄다**(보간 위치 → 실제 위치). 서버에서 그 순간 멈추므로 원래 튀는 게 맞다.
  - BGM 은 여전히 합성음이다. 음량은 맞췄지만 "게임 음악"이라기보다 배경 루프에 가깝다.
  - `slowMin` 을 0.06 으로 낮췄으므로 **골키퍼가 막을 시간이 다시 늘었다**(공이 느리게 도착). 방어율은 재측정하지 않았다.
- 문서 수정: `CLAUDE.md` §2(감속 곡선을 √ 로 교체한 이유와 실측)·§5.5(패드와 `pad()` 포락선, **소리 실측 방법과 수치**).
  `ROADMAP.md` 1단계에 12차 조건 3개 반영. `.claude/launch.json`(no-store + ThreadingHTTPServer).
- 다음 세션 첫 할 일: ROADMAP 2단계(서버 코어 + 온라인 1:1). 변경 없음.
- 사용자 확인 요청:
  1. **BGM 음량**이 지금 적당한지(더 키우려면 `bgmG` 한 값).
  2. **공이 멎는 느낌**이 이제 자연스러운지.
  3. 방귀 사각형이 실제 맞는 자리와 일치하는지.
- 커밋: 없음(사용자 지시 대기).

## 세션 12 — 2026-08-25 — 단계 1 11차 개편(플레이 피드백 4건) — 완료
- 한 일:
  1. **방귀 면적 −15%** — 넓이를 줄이려면 한 변에 √0.85 를 곱해야 한다. `fartHalf` 48 → **44.26**(한 변 96 → 88.5, 넓이 85%).
  2. **방귀 각도가 여전히 바뀌던 문제** — 원인이 둘이었다.
     · 서버: 10차에서 `facing` 만 잠갔지 **이동은 막지 않아서** 사각형이 선수를 따라 평행이동했다.
     · 클라이언트: 스냅샷 사이 `lerpAngle` 보간 때문에 동작 **첫 한두 프레임 동안 사각형이 회전**해 보였다.
     둘 다 고쳤다 — 서버는 아래 3번으로 이동까지 막았고, 클라이언트는 `animState` 에 **동작이 시작된 순간의 방향**을 기억해 그 각도로 사각형을 그린다.
  3. **방귀·킥·기절 중 완전 고정** — `frozen = freezeT > 0 || kickT > 0` 로 바꿔 동작 내내 입력을 받지 않는다(이동·회전·새 기술 전부).
     기절 쪽은 **넉백을 폐지**했다(`knockback`·`knockTicks` → 0). 맞은 선수는 제자리에서 굳는다.
  4. **감속을 완만하게** — `slowMin` 0.28 → **0.55**. 공이 끝까지 훨씬 덜 느려진다.
- 실측값:
  | 항목 | 전 | 후 |
  |---|---|---|
  | 방귀 판정 한 변 | 96 (앞뒤 [−96,0]·좌우 ±48) | **88.5** (앞뒤 [−88,0]·좌우 ±44) — 넓이 85% |
  | 방귀 중 이동 | 정지 2틱 뒤 움직임 | **5틱 내내 이동 0 · 각도 0** → 끝난 틱부터 이동 8.4 · 각 45° |
  | 기절 중 이동 | 넉백으로 밀림 | **12틱 동안 이동량 0** |
  | 슛 틱당 이동 | 21틱에 30.2 → 10.4 | **16틱에 30.8 → 18.5** |
  | 슛 총거리 | 368 | **368**(변화 없음) |
  - 결정성: seed1 600틱 **2109647361** — Node = `http://` = `file://` 동일. `file://` `errors: []`, 콘솔 0건.
  - 진짜 키보드(CDP): 드리블 `ballAhead` 58 · 슛 speed 640 · 감아 spin −0.052 · 방귀 1 · 필살 1 · 효과음 12종 `sfxOk: "ok"`.
- 미달/알려진 문제:
  - **동작 중에는 필살기(A)도 못 쓴다.** 입력 자체를 막았기 때문이다. 동작은 0.25초이고 쿨다운이 그보다 길어 실전 손해는 없지만,
    "방귀 직후 곧바로 필살기"는 0.25초 기다려야 한다.
  - **넉백이 사라져 발차기의 '떼어내는' 효과가 없어졌다.** 이제 발차기는 순수하게 "2초 묶기"다.
    붙어 있는 상대를 밀어내지 못하므로 **기절 잠금이 더 쉬워졌을 수 있다**(7차의 자유 16%를 재측정하지 않았다).
  - 감속이 완만해지면서 **골키퍼가 막을 시간이 줄었다**(공이 더 빨리 도달한다). 골키퍼 방어율은 재측정하지 않았다.
  - 클라이언트의 "동작 시작 각도 기억"은 `animState` 에만 있다. 온라인(2단계)에서 슬롯이 재사용되면 `animState` 를 비워야 한다 — `startPractice` 는 이미 비운다.
- 문서 수정: `CLAUDE.md` §2(`slowMin` 0.55와 새 실측)·§5.2(방귀 크기 이력, **동작 중 완전 정지**, **기절 중 넉백 폐지**).
  `ROADMAP.md` 1단계에 11차 조건 3개 추가와 감속 항목 갱신.
- 다음 세션 첫 할 일: ROADMAP 2단계(서버 코어 + 온라인 1:1). 변경 없음.
- 사용자 확인 요청:
  1. **넉백을 없앤 뒤 기절 잠금이 심해지지 않았는지**(면역을 조금 되살릴지).
  2. **감속 0.55** 가 적당한지(0.28 이 너무 급했고 1.0 이면 다시 등속).
  3. 동작 중 필살기까지 막힌 것이 답답한지.
- 커밋: 없음(사용자 지시 대기).

## 세션 11 — 2026-08-25 — 단계 1 10차 개편(플레이 피드백 4건) — 완료
- 한 일:
  1. **캐릭터 걷는 속도 통일** — `CHARACTERS[].speed` 를 전학생 0.95 · 덩치 0.85 · 육상부 1.15 → **전부 1.00**.
     속도 차이는 이제 필살기(전력 질주 1.6배 · 황소 돌진 1.45배)에서만 난다.
  2. **발차기 범위 축소** — 9차에 48 → 68 로 넓혔던 것을 **56** 으로, 반각 70° → **60°** 로 되돌렸다.
  3. **공이 굴러가며 느려진다** — `TUNING.ball.slowMin`(0.28) 신설. 실제 이동 속도가 **남은 거리 비율**에 따라 처음의 100% → 28% 로 준다.
     핵심은 **`range` 를 "총 이동 거리"로 유지한 것**이다 — 그래서 날아가는 거리는 하나도 안 바뀌고 속도 곡선만 바뀐다.
     `b.range0`(찬 순간의 거리)를 새로 저장하고, 공에 거리를 주는 모든 자리(`releaseBall`·골키퍼 걷어내기·기절로 흘린 공·`debug.setBall`)에서 함께 갱신한다.
     결정성 해시에도 `range0` 를 넣었다.
     **`CLAUDE.md` §2 의 "물리는 등속" 규칙을 고쳐야 했다** — 이제 "선수는 등속, 공은 감속"이다.
  4. **카메라가 내 선수를 정중앙에** — 데드존(`CAM.deadX/deadY` 46/34)을 **0** 으로 두고 추종 시상수도 0.10 → 0.055 로 조였다.
     "경기장 가장자리에서는 가운데가 아니어도 된다"는 별도 처리가 필요 없었다 — 골망 밖을 안 보여 주려는 기존 클램프가 그대로 그 역할을 한다.
- 실측값:
  | 항목 | 전 | 후 |
  |---|---|---|
  | 캐릭터별 1초 이동 | 143~193 (배율 0.85~1.15) | **6종 모두 168** |
  | 발차기 판정 | 반지름 68·±70°, 861칸 중 355 명중 | **56·±60°, 224 명중** |
  | 최대 슛 틱당 이동 | 32 (등속) | **30.2 → 10.4 로 감속**(21틱) |
  | 최대 슛 총거리 | 368 | **368**(변화 없음) |
  | 드리블 1/6/12틱 | 58/178/382 | **58/178/382**(변화 없음) |
  | 카메라 − 선수 (한가운데) | 데드존만큼 어긋남 | **0, 0** |
  | 카메라 − 선수 (왼쪽 끝 / 위쪽 끝) | — | **x 240 / y 140**(클램프, 의도한 예외) |
  - 결정성: seed1 600틱 **2752247988**, 900틱 3902263890 — Node = `http://` = `file://` 동일. `file://` `errors: []`, 콘솔 0건.
  - 진짜 키보드(CDP): 드리블 `ballAhead` 58 · 슛 speed 640 · 감아 spin −0.052 · 방귀 1 · 필살 1 · 효과음 12종 `sfxOk: "ok"`.
- 미달/알려진 문제:
  - **캐릭터 개성이 얇아졌다.** 속도를 통일하면서 육상부형은 "슛 0.85 · 발차기 0.90 에 필살기만 빠름", 덩치형은 "느린 대신 강함"이 아니라
    그냥 "슛 1.10 · 발차기 1.40" 이 됐다. 육상부형이 특히 손해다 — 9단계 실사용 튜닝에서 다시 볼 것.
  - 공 감속은 **모든 킥에 적용**된다(슛·패스·드리블·감아차기·골키퍼 걷어내기). 지시는 "슛"이었지만 같은 `moveBall` 을 쓰므로 전부에 걸었다.
    거리가 안 변하니 부작용은 "도착까지 걸리는 시간이 약 1.5배" 뿐이다. 슛만 감속시키려면 공에 플래그를 하나 더 둬야 한다.
  - 카메라가 항상 정중앙이라 **빠르게 방향을 바꾸면 화면 전체가 같이 흔들린다**(데드존이 하던 일이 사라졌다). 어지러우면 `CAM.deadX/deadY` 를 10 정도만 주면 된다.
  - 7차에서 생긴 **기절 잠금(자유 16%)** 은 그대로다. 다만 이번에 발차기 범위를 줄였으니 조금 완화됐을 것이다(재측정하지 않았다).
- 문서 수정: `CLAUDE.md` §2(**"물리는 등속" → "선수는 등속, 공은 감속"**)·§5.2(발차기 범위)·§5.3(속도 열 전부 1.00 + 설명)·§7.1(카메라 추종 방식과 실측).
  `ROADMAP.md` 1단계에 10차 조건 4개 반영.
- 다음 세션 첫 할 일: ROADMAP 2단계(서버 코어 + 온라인 1:1). 변경 없음.
- 사용자 확인 요청:
  1. **카메라가 항상 정중앙이라 어지럽지 않은지**(데드존을 조금 되살릴지).
  2. **속도를 통일하면서 육상부형이 약해진 것**을 어떻게 할지(슛·발차기 배율을 올려 줄지).
  3. 공 감속을 **슛에만** 걸지, 지금처럼 전부에 걸지.
- 커밋: 없음(사용자 지시 대기).

## 세션 10 — 2026-08-25 — 단계 1 9차 개편(플레이 피드백 6건 + 이미지 규격 정의) — 완료
- 한 일:
  1. **슛·감아차기가 사람을 때리지 않는다** — `kickKind` 를 셋으로 나눴다(1 앞차기 · 2 방귀 · **3 공만 차는 동작**).
     `kickHits` 가 3 을 건너뛴다. 전에는 슛 동작도 앞차기 판정을 돌려서, 쏘는 순간 앞의 상대가 기절했다.
     `startKick` 의 `shot` 인자는 없앴다(kind 3 으로 대체).
  2. **동작 중 회전 고정** — `kickT > 0` 인 동안 `facing` 을 갱신하지 않는다. 방귀 뒤 몸을 돌리면 판정 사각형이 따라 돌던 문제.
  3. **속도 +5%** — 160 → 168.
  4. **드리블 연장** — 거리 곡선의 지수를 2 → **1.35**(`rangePow` 신설), 최소 8 → 24, 최대 300 → 360.
  5. **슛 거리 = 경기장 가로의 1/3** — 500 → **400**.
  6. **발차기 범위 확대** — 반지름 48 → **68**, 반각 55 → **70°**.
  7. **이미지 캐릭터 규격을 문서화**(`CLAUDE.md` §5.4.1). 파일은 아직 없고, 없으면 도트로 그리는 폴백은 그대로다.
- 실측값:
  | 항목 | 전 | 후 |
  |---|---|---|
  | 슛 — 바로 앞 상대 | 발차기에 맞아 기절 | **기절 0**(`shoot,kick` 이벤트만) |
  | 감아차기 — 바로 앞 상대 | 기절 | **기절 0** |
  | 발차기(공 없이) | 기절 | **기절 유지**(`kick,hit`) |
  | 방귀 뒤 방향키 꺾기 | 즉시 회전 | **5틱 동안 0° 고정 → 끝난 틱에 90°** |
  | 드리블(누른 틱 1/3/6/9/12) | 28/44/99/190/318 | **58/98/178/274/382** |
  | 슛 이동 거리 | 448 | **368**(설정 400 = 경기장 가로의 1/3) |
  | 발차기 판정 | 반지름 48·±55° | **68·±70°**(격자 861칸 중 355칸 명중) |
  | 걷기 | 160 | **168** |
  - 결정성: seed1 600틱 **3632116021** — Node = `http://` = `file://` 동일. `file://` `errors: []`.
  - 진짜 키보드(CDP): 드리블 `ballAhead` 38 → **76** · 슛 speed 640 · 감아 spin −0.052 · 방귀 1 · 필살 1 · 효과음 12종 `sfxOk: "ok"`.
- 미달/알려진 문제:
  - **감아차기(800)가 슛(400)보다 두 배 멀리 간다.** 슛만 계속 줄이다 보니 벌어졌다 — 감아차기도 줄일지는 지시를 받지 않아 그대로 뒀다.
  - **기절 잠금(자유 16%)** 은 7차 이후 그대로다. 발차기 판정을 넓혔으니 잠금이 **더 쉬워졌을 가능성**이 있다(재측정하지 않았다).
  - 회전 고정은 `facing` 만 막는다. 동작 중에도 **위치는 움직이므로** 화면의 방귀 사각형은 선수를 따라 평행이동한다(회전만 멈춘다).
  - 이미지 캐릭터는 규격만 정했고 로더는 아직 없다. 파일이 오면 `sprites.js` 에 이미지 경로 분기를 넣어야 한다.
- 문서 수정: `CLAUDE.md` §3(속도 이력)·§5.2(슛 거리, 드리블 곡선, 발차기 범위, **공 차는 동작은 사람을 안 때린다**, **동작 중 회전 고정**)·
  **§5.4.1 이미지 규격 신설**. `ROADMAP.md` 1단계에 9차 조건 6개 반영.
- 다음 세션 첫 할 일: ROADMAP 2단계(서버 코어 + 온라인 1:1). 변경 없음.
- 사용자 확인 요청:
  1. **감아차기 거리(800)** 를 슛(400)에 맞춰 줄일지.
  2. 넓어진 발차기 판정 때문에 **기절 잠금이 더 심해지지 않는지**(면역을 조금 되살릴지).
  3. 캐릭터 이미지 파일 — §5.4.1 규격대로 주면 붙인다.
- 커밋: 없음(사용자 지시 대기).

## 세션 9 — 2026-08-25 — 단계 1 8차 개편(플레이 피드백 4건) — 완료
- 한 일:
  1. **방귀 범위 축소** — `fartHalf` 70 → **48**. 뒤쪽 정사각형이 140×140 → **96×96**(넓이로는 47%).
  2. **속도 재하향** — `maxSpeed` 185 → **160**(달리기 240). 220 에서 두 번에 걸쳐 27% 낮췄다.
  3. **슛을 클릭식·고정 거리로** — 충전을 아예 없앴다. `TUNING.shoot` 이 `{minSpeed, maxSpeed, chargeTicks, range}` 에서
     **`{speed: 640, range: 500}`** 으로 바뀌었고, 입력도 `released` → `pressed` 로 옮겨 **누르는 순간** 나간다.
     세기를 바꾸는 것은 캐릭터 `shot` 배율과 에이스형 대포알뿐이다. 이제 **충전하는 기술은 감아차기(Q/E)와 드리블(W) 둘뿐**이라,
     스냅샷의 충전 눈금 기준도 `shoot.chargeTicks` → `curve.chargeTicks` 로 바꿨다(안 바꾸면 없는 값을 나눠 NaN 이 된다).
  4. **감아차기 덜 휨** — `spinMax` 0.072 → 0.052, `turnMax` 2.7 → 2.0rad(155° → 115°).
- 실측값:
  | 항목 | 전 | 후 |
  |---|---|---|
  | 슛 — 누른 틱 1/4/12/24 | 속도 350~750 (충전에 따라) | **전부 속도 640 · 거리 448** |
  | 감아차기 휨각(충전 1/4/8/12틱) | 42/57/74/91° | **25/44/63/74°** |
  | 방귀 판정 | 뒤 [−140,0] × ±70 | **뒤 [−96,0] × ±48** |
  | 걷기 / 달리기 | 185 / 277.5 | **160 / 240** |
  - 골문 앞 200 에서 슛 성공률 **62.5%**(40조건). 충전이 없어졌는데도 4차의 64.8% 와 거의 같다 — 고정 속도 640 이 예전 최대 충전(750)에 가깝기 때문.
  - 결정성: seed1 600틱 **3756775348** — Node = `http://` = `file://` 동일. `file://` `errors: []`.
  - 진짜 키보드(CDP): 드리블 1 · **슛 speed 640** · 감아 spin −0.052 · 방귀 1 · 필살 1 · 효과음 12종 `sfxOk: "ok"`.
  - 기술 회귀: 드리블·감아차기·발차기·패스·방귀 이벤트 전부 정상 발생.
- 미달/알려진 문제:
  - 검증기 `headless-probe.js` 의 슛 검사가 **속도 0** 으로 나오던 것을 고쳤다 — 0.7초 누른 뒤 재는 방식이라
    클릭식 슛에서는 공이 이미 멈춘 뒤였다. 짧게 누르고 바로 재도록 바꿨다(내 코드 결함이 아니라 검사 타이밍 문제).
  - **기절 잠금(자유 16%)은 그대로다.** 7차에서 기절 면역을 없앤 결과이고, 이번 지시에는 없어서 손대지 않았다.
  - 슛 거리 448 은 **골문 앞 200 에서는 충분하지만 중거리(400 이상)에서는 골라인 앞에서 멈춘다**. 의도한 것이지만 체감 확인 필요.
  - 감아차기 거리 800 은 슛 500 보다 길다 — 지금은 감아차기가 가장 멀리 가는 기술이다.
- 문서 수정: `CLAUDE.md` §3(속도 이력)·§5.1(슛 = 충전 없음)·§5.2(슛 규칙 교체, 감아차기 수치, 충전 기술이 둘뿐임).
  `ROADMAP.md` 1단계에 8차 조건 4개 반영(7차 항목 일부 교체).
- 다음 세션 첫 할 일: ROADMAP 2단계(서버 코어 + 온라인 1:1). 변경 없음.
- 사용자 확인 요청:
  1. **슛 거리 448·속도 640** 이 적당한지(더 짧게/길게 한 값으로 조절).
  2. **감아차기가 슛보다 멀리 가는 것**(800 vs 500)이 맞는지.
  3. 7차에서 생긴 **기절 잠금**을 이대로 둘지.
- 커밋: 없음(사용자 지시 대기).

## 세션 8 — 2026-08-25 — 단계 1 7차 개편(플레이 피드백 6건) — 완료
- 한 일 (사용자가 직접 해 보고 준 지시):
  1. **슛 거리 제한** — "장거리 슛이 가능하다" → `shoot.range` 1200 → **700**(경기장 가로의 58%). 자기 진영에서 상대 골대로 직접 못 쏜다.
     감아차기도 1300 → **800** 으로 같이 줄였다. 안 줄이면 감아차기가 그대로 장거리 수단이 되기 때문인데, **이건 지시에 없던 판단**이다.
  2. **선수를 크게** — `PLAYER_R` 14 → **18**. 따라 올린 것: 골키퍼 몸통 14 → 18, 공 소유 반지름 24 → 28, 공 붙는 거리 18 → 22, 앞차기 판정 44 → 48.
     그림도 같은 값을 쓰므로 캐릭터가 함께 커졌다.
  3. **정사각형에 가까운 모습** — 도안(`BODY`)은 그대로 두고 **그릴 때만** 가로 `WIDEN` 1.45 · 세로 `FLATTEN` 0.62 로 눌러 실루엣을 정사각형에 가깝게 했다.
     이제 그림 폭이 충돌 지름(36)과 거의 같아서 "어디까지가 판정인지"가 눈에 그대로 보인다.
  4. **속도 하향** — `maxSpeed` 220 → **185**(달리기 277.5).
  5. **방귀가 안 맞는 문제 + 뒤쪽 네모** — 원인은 **기절 면역 2.5초**였다. 한 번 눕힌 상대는 깨어난 뒤 2.5초 동안 어떤 타격도 무시되어
     "분명히 뀌었는데 안 맞는" 상황이 났다. 지시대로 **면역을 폐지**했고, 판정을 선수 중심 사방 → **바라보는 방향 뒤쪽 정사각형**으로 바꿨다
     (앞모서리가 선수 중심, 뒤로 2×`fartHalf`, 옆 ±`fartHalf`). 클라이언트도 같은 식으로 회전시켜 같은 사각형을 그린다.
  6. **딜레이 단축** — 시전자 정지를 한 틱씩 줄여 발차기 1틱(0.05초) · 슛 2틱(0.10초) · 방귀 2틱(0.10초).
- 실측값:
  | 항목 | 전 | 후 |
  |---|---|---|
  | 최대 충전 슛 비행 거리 | 1130 | **638** (설정 700) |
  | 선수 반지름 | 14 | **18** |
  | 걷기 / 달리기 | 220 / 330 | **185 / 277.5** |
  | 방귀 판정 | 선수 중심 [−70,70]² | **뒤쪽 [−138, 0] × 좌우 [−66, +66]** |
  | 방귀 — 앞쪽 50 상대 | 맞음 | **안 맞음**(뒤쪽 전용) |
  | 방귀 — 방금 깨어난 상대 | 면역으로 무시 | **3연속 전부 기절 40틱** |
  | 시전자 정지(발차기/슛/방귀) | 2 / 3 / 3틱 | **1 / 2 / 2틱** |
  - 방귀 사각형은 오른쪽 보기·아래 보기 둘 다 같은 모양으로 나왔다(회전이 판정에 제대로 반영됨).
  - 결정성: seed1 600틱 **584709283** — Node = `http://` = `file://` 동일. `file://` `errors: []`, `sprite: 납작한 종이 도트(정사각형에 가깝게)`.
  - 진짜 키보드(CDP): 드리블 1 · 슛 750 · 감아 spin −0.072 · 방귀 1 · 필살 1 · 효과음 12종 `sfxOk: "ok"`.
  - 인공지능 2:2(4000틱, 캐릭터 4조합): 7:15 / 12:5 / 6:8 / 7:14 — 골 14~22, 선방 18~25.
- 미달/알려진 문제:
  - ⚠ **기절 잠금이 생겼다.** 면역을 없앤 대가로, 붙어서 발차기를 반복하면 상대가 거의 못 움직인다 —
    최악 조건 실측 **200틱 중 자유 32틱(16%)**. 면역이 있던 3차까지는 56% 였다.
    지시("무조건 기절")를 그대로 따른 결과이고, 되돌리는 레버는 `TUNING.player.stunImmuneTicks` 하나다.
    실전에서는 넉백 때문에 이 정도까지 붙어 있기 어렵지만, **20명 난전에서 여럿이 한 명을 계속 눕힐 수 있다는 뜻**이기도 하다.
  - **감아차기 거리를 같이 줄인 것은 지시에 없던 판단**이다. 되돌리려면 `TUNING.curve.range` 만 800 → 1300 으로 되돌리면 된다.
  - 방귀 사각형(뒤로 140 × 옆 140)이 화면에서 꽤 크다. 시야가 640×360 이라 세로의 40% 가까이를 덮는다.
  - 슛 거리 700 이면 **골문 앞 200 에서 쏘는 것은 그대로**지만, 중거리 슛(400 이상)이 골라인 앞에서 멈추는 경우가 생긴다. 체감 확인 필요.
  - 캐릭터가 커지면서 20명이 모이면 화면이 더 빽빽해진다 — 6단계 20명 부하 시험에서 다시 볼 것.
- 문서 수정: `CLAUDE.md` §3(반지름 18과 딸린 값들)·§5.2(슛·감아차기 거리, 방귀 뒤쪽 정사각형, 정지 시간, **기절 면역 폐지 경고**)·§5.4(WIDEN·FLATTEN).
  `ROADMAP.md` 1단계에 7차 개편 조건 5개 추가.
- 다음 세션 첫 할 일: ROADMAP 2단계(서버 코어 + 온라인 1:1). 변경 없음.
- 사용자 확인 요청:
  1. **기절 잠금(자유 16%)** 을 이대로 둘지, 면역을 조금(예: 0.5초)만 되살릴지.
  2. **슛 700** 이 너무 짧지 않은지, **감아차기 800** 으로 줄인 것이 맞는지.
  3. **커진 캐릭터와 뒤쪽 방귀 네모** 크기가 적당한지.
- 커밋: 없음(사용자 지시 대기).

## 세션 7 — 2026-08-25 — 단계 1 6차 개편(사용자 지시 6건) — 완료
- 한 일:
  1. **뒷발차기 → 방귀(S)** — 이름·이벤트·모션·플래그를 전부 `fart` 로 바꿨다(`BTN.FART`, `MOTION.FART`, `ST.FART`, 이벤트 `fart`).
     능력치(기절 2초·쿨다운 0.8초·공 안 참·빼앗기)는 그대로 두고 **판정 모양만 정사각형**으로 바꿨다.
     선수를 중심으로 한 변 2×`fartHalf`(70) = **140×140** 정사각형이고 앞뒤 구분이 없다.
     부채꼴 대신 정사각형인 이유는 "타격 판정이 헷갈린다"는 지적 때문이다 — **클라이언트가 같은 정사각형을 화면에 그대로 그린다**(연두색 사각형).
  2. **슛·방귀의 정지 1.3배** — 발차기는 2틱(0.10초) 그대로, **슛과 방귀만 3틱(0.15초)**. 2 × 1.3 = 2.6 인데 틱이 정수라 올림했다.
     `startKick(state, p, kind, ev, shot)` 에 `shot` 인자를 더해 "공 없이 D = 짧은 정지 / 슛·방귀 = 긴 정지"를 구분한다.
  3. **닿아도 안 밀린다** — `separatePlayers` 에서 선수↔선수, 선수↔골키퍼 밀어내기를 없앴다. 이제 겹쳐 지나갈 수 있고,
     밀려나는 것은 발차기·방귀에 맞았을 때의 넉백뿐이다. 함수는 경기장 밖으로 못 나가게 하는 클램프만 남았다.
  4. **납작한 종이 캐릭터** — 입체 도트 3/4 시점에서 **종이 오려 붙인 느낌**으로. 테두리를 밝은 종이색(`#fbf3e0`)으로,
     명암을 `FLAT_SHADE` 0.22 로 줄이고, 화면에 그릴 때 세로로 `FLATTEN` 0.72 눌렀다.
     결정적으로 **발밑에 충돌 반지름(14) 그대로의 원반 + 흰 테두리**를 그린다 — 그 원이 곧 판정이다.
  5. **드리블 충전(W)** — "누르고 있으면 잡을 때마다 반복"을 버리고 **뗄 때 나가는 충전식**으로 바꿨다.
     거리는 충전의 **제곱**에 비례한다(선형이면 1틱만 눌러도 이미 꽤 나가서 "톡 치기"가 안 된다).
  6. 이름 정리: 도움말·터치 버튼(`bBack` → `bFart`)·효과음 키·검증 도구까지 전부 `방귀`/`fart` 로 맞췄다. 방귀 효과음은 낮은 톱니 + 새는 바람으로 새로 만들었다.
- 실측값:
  | 항목 | 전 | 후 |
  |---|---|---|
  | 방귀 판정 모양 | 뒤 부채꼴 반지름 58·±75° | **정사각형 x·y 모두 [−70, +70]** (2025칸 격자 실측, 모서리 명중·(75,0) 불발) |
  | 방귀가 앞쪽 상대 | 못 맞힘 | **맞힘**(`fart → hit → steal`) |
  | 시전자 정지 — 발차기 / 슛 / 방귀 | 2 / 2 / 2틱 | **2 / 3 / 3틱** |
  | 두 선수를 10 거리에 겹쳐 두고 5틱 | 서로 밀려 28로 벌어짐 | **이동 0, 간격 10 그대로** |
  | 드리블 1틱 탭 | 공이 앞 82 | **앞 28** |
  | 드리블 최대 충전 | (없음) | **앞 318** |
  | 진짜 키보드 `ballAhead` | 82 | **34** |
  - 결정성: seed1 600틱 **765613125** — Node = `http://` = `file://` 동일. `file://` `errors: []`, `sprite: 납작한 종이 도트`.
  - 회귀(1·2차 지시): 몰고 들어가 득점 44.4%, 충전 슛 64.8%, 골대 밖 0/36, 발차기 연타 시 자유 112/200(56%),
    달리기 220/330/275·2초·10초·지치면 158.4, 유령골 0/178, 스냅샷 20명 289B — 전부 그대로.
  - 진짜 키보드(CDP): 드리블 1 · 슛 750 · 감아 spin −0.072 · **방귀 1** · 필살 1 · 소리 12종 `sfxOk: "ok"`.
  - 인공지능 2:2 를 캐릭터 조합 6가지로 4000틱씩: 점수 7:13 / 10:9 / 10:10 / 13:7 / 6:10 / 8:10, **골 16~20 · 선방 15~22**.
    세션 6의 21:3 같은 일방적 경기가 사라졌다 — 밀어내기가 없어지면서 수비가 붙어 설 수 있게 된 효과다.
- 미달/알려진 문제:
  - **방귀 정사각형(140×140)이 상당히 크다.** 시야가 640×360 인데 한 변이 140이라 화면의 5분의 1을 덮는다.
    2초 기절 + 공 빼앗기까지 붙어 있어 강한 기술이다. `TUNING.kick.fartHalf` 한 값으로 조절된다 — 플레이해 보고 정해야 한다.
  - **방귀 판정을 선수 중심 정사각형으로 잡은 것은 해석이다.** 지시는 "범위가 크다 / 정사각형"까지였고 방향은 없었다.
    가스가 퍼지는 모양이고 판정이 눈에 그대로 보이는 쪽을 골랐다. "뒤쪽만" 으로 바꾸려면 `kickHits` 의 정사각형 검사에 방향 조건 한 줄만 더하면 된다.
  - **밀어내기를 없앤 대가**: 수비수가 공격수와 **완전히 겹쳐 설 수 있다**. 인공지능 경기가 접전이 된 것도 이 때문인데,
    사람끼리 붙으면 "몸으로 막기"가 사라진 대신 "겹쳐서 공 뺏기"가 강해진다. 실제로 어떤지는 플레이 확인 항목이다.
  - 세션 6의 "인공지능 3분 20골"은 16~20골로 조금 줄었을 뿐 여전히 많다. 원인과 레버(`keeper.saveR`)는 세션 6 항목과 같다.
  - 캐릭터가 납작해지면서 **위/아래 발차기가 옆차기로 그려지는 옛 결함이 덜 눈에 띈다** — 고친 것은 아니다(5단계).
- 문서 수정: `CLAUDE.md` §3(반지름 행에 "안 밀림")·§5.1(조작표 방귀·드리블)·§5.2(드리블 충전 절 교체, **방귀 절 교체**, 정지 시간 절 교체)·
  §5.4(납작한 종이 화풍과 이유)·§5.5(효과음 목록)·§6(buttons 비트 5·motion 3·state 비트 5·이벤트 목록).
  `ROADMAP.md` 1단계에 6차 개편 조건 6개 추가, 사용자 확인 항목 교체.
- 다음 세션 첫 할 일: ROADMAP 2단계(서버 코어 + 온라인 1:1). 변경 없음.
- 사용자 확인 요청:
  1. **방귀 정사각형 크기(한 변 140)** 와 **선수 중심(사방)** 이 맞는지 — "뒤쪽만"으로 바꿀지.
  2. **슛·방귀 정지 0.15초**가 역공당할 만한 길이인지(2.6틱을 3틱으로 올림했다).
  3. **납작한 종이 캐릭터**가 판정과 맞게 보이는지 — 더 납작하게(`FLATTEN`) 하거나 원반을 더 진하게 할 수 있다.
- 커밋: 없음(사용자 지시 대기).

## 세션 6 — 2026-08-25 — 단계 1 5차 개편(사용자 지시 12건) — 완료
- 한 일 (지시 순서대로):
  1. **체력 재조정** — 눈금을 120 → 200 으로 잡아 정수만으로 세밀하게 맞췄다. 소모 2 → 5/틱(3초 → **2초**에 바닥),
     회복 1/틱 그대로지만 상한이 커져 6초 → **10초**에 만충. 재개 기준 30%(60) = 바닥에서 3초.
  2. **지치면 느려진다** — 새 `tiredMul` 0.72. 바닥난 뒤 회복 전까지는 걷기(220)보다 느린 **158 u/s**. 전에는 "그냥 걷기 속도"였다.
  3. **뒷발차기 범위 확대** — 반지름 38 → 58, 반각 55 → 75°. 이제 앞차기(44·55°)보다 넓다.
  4. **뒷발차기로 공 빼앗기** — 맞은 상대가 공을 갖고 있었으면 그 공이 찬 사람에게 넘어온다(`steal` 이벤트).
     상대가 기절 면역이거나 돌진 중이면 기절도 빼앗기도 실패한다(면역 판정은 `stunPlayer` 한 곳 그대로).
  5. **뒷발차기는 공을 차지 않는다** — `kickBack()` 과 `TUNING.backkick` 을 지웠다. 공을 든 채로 써도 공은 그대로 내 발 앞에 있고 타격만 한다.
  6. **슛 거리 단축** — `shoot.range` 2200 → 1200(경기장 가로 폭과 같다).
  7. **감아차기 더 휘게** — `spinMax` 0.048 → 0.072, `turnMax` 1.9 → 2.7rad(109° → 155°).
  8. **효과음** — 음원 파일 없이 WebAudio 로 12종 합성(kick·backkick·shoot·curve·pass·dribble·hit·steal·keeperSave·ult·whistle·goal).
     `step()` 이 낸 이벤트에 붙이되 **위치가 있는 소리는 그 선수가 화면 안일 때만** 낸다.
  9. **BGM** — 132BPM 4마디(Am–F–C–G) 순환, 8분음표 격자에 베이스·아르페지오·하이햇을 0.25초 앞서 예약. 잡음원은 고정 시드.
     자동재생 정책 때문에 **첫 조작 뒤에만** AudioContext 를 만든다(그 전에는 조용히 무시 — 콘솔 경고 0).
  10. **혼자 하면 2:2 인공지능** — `SoccerCore.AI.think(state, slot)` 신설. **`step()` 안에서는 부르지 않는다** — 입력만 만들어
      사람 입력과 같은 통로로 넣으므로 서버 권위·결정성이 그대로다. 난수 없이 좌표와 `state.tick` 만 본다.
      루트 `CLAUDE.md` 와 `soccer/CLAUDE.md` §1 의 "봇 없음"은 **온라인 한정**으로 범위를 고쳤다.
  11. **전·후반 각 3분** — 기본 `halfSec` 150 → 180. 시계는 원래부터 `play` 중에만 줄어서 세리머니는 자동으로 빠진다(실측으로 확인).
  12. **캐릭터 그림** — 도트 격자 24 → **30**(발차기 발끝이 잘리던 세션 4 잔여 결함 해소), 캐릭터 고유색 **허리띠** 추가
      (팀색이 같아도 어느 방향에서든 구분된다), **걸음 위상차** `charId × 0.137`(20명이 똑같이 뛰던 문제).
  - 그 밖에: 골키퍼가 **몰고 오는 공**을 걷어내는 반지름을 `stealR` 로 분리해 30 → 52 로 올렸다(아래 미달 참고).
    PC 화면에서 체력·필살기 게이지가 왼쪽 아래 도움말과 겹쳐 라벨이 가려지던 것을 오른쪽 아래로 옮겨 해결(터치는 가운데 그대로).
    검증기 `headless-probe.js` 에 Shift 달리기·소리 검사를 추가.
- 실측값:
  | 항목 | 전 | 후 |
  |---|---|---|
  | 체력 바닥까지 | 60틱(3초) | **40틱(2초)** |
  | 만충까지 | 120틱(6초) | **200틱(10초)** |
  | 바닥난 뒤 속도 | 220(걷기) | **158**(걷기보다 느림) |
  | 최대 충전 슛 비행 거리 | 2200 설정 | **1130 실측**(1200 설정) |
  | 감아차기 휨(충전 1/4/8/12틱) | 29/46/60/72° | **42/57/74/91°** |
  | 뒷발차기 | 공을 뒤로 참 | **공 유지**(속도 0, owner = 나) |
  | 뒷발차기 빼앗기 | 없음 | `backkick → hit → steal`, owner = 나, 상대 기절 40틱 |
  | 몰고 들어가 득점 | 57.4% | **44.4%**(`stealR` 30 → 52) |
  | 골문 앞 200 충전 슛 | 64.8% | 64.8%(변화 없음) |
  - 뒷발차기 판정 범위: 거리 20~80 × 각도 ±90° 격자 209곳 중 105곳 명중(50.2%).
  - 인공지능 2:2: 4000틱(전반 3분 + 세리머니) 한 판에 **골 20~24, 패스 54~118, 발차기 117~155, 선방 3~21**. 남은 시간 50초 = 세리머니 350틱이 경기 시간에서 빠짐(설계대로).
    같은 조건 두 번 = 같은 해시(2323749936) — 인공지능도 결정적.
  - 결정성: seed1 600틱 **733639333** — Node = `http://` = `file://` 동일. `file://` `errors: []`, 렌더 0.41ms/프레임, `store: true`.
  - 소리: 진짜 키 입력 뒤 `debug.audio() = {ready:true, sfx:true, bgm:true}`, 12종 전부 예외 없이 재생(`sfxOk:"ok"`). 콘솔 0건.
  - 스냅샷 20명 289B 유지, fps 60.0, 스냅샷 20Hz.
  - 진짜 키보드(CDP): 드리블 1 · 슛 750 · 감아 spin −0.072 · 뒷발 1 · 필살 1 · Shift 달리기(공 몰고 0.6초 165 = 275u/s).
- 미달/알려진 문제:
  - **인공지능 2:2 가 전반 3분에 20골 안팎으로 너무 많이 난다.** 원인은 인공지능이 아니라 규칙이다 — 골대 폭 200 중
    골키퍼가 덮는 폭이 76(38%)뿐이라 슛의 65%가 들어간다(득점 34건 중 카운트: 슛 11·몰고 들어가기 5 → `stealR` 상향 후에도 슛이 주류).
    2:2 는 공간이 넓어 슛 기회가 계속 난다. **줄이려면 `TUNING.keeper.saveR`(30) 를 올리는 것이 유일한 레버**인데,
    이는 사용자가 이미 체감한 골키퍼 난이도를 바꾸는 일이라 **사용자 확인 없이 건드리지 않았다.**
  - 캐릭터 조합에 따라 21:3 처럼 한쪽이 압도한다. 속도 배율(육상부 1.15 vs 덩치 0.85)이 "몰고 들어가기·따라잡기"에 직결되기 때문이다.
    세션 4에서 "캐릭터 격차"를 오탐으로 기각했지만, 인공지능끼리 붙이면 다시 드러난다. 9단계 실사용 튜닝 항목.
  - 뒷발차기가 공을 안 차게 되면서 **"뒤로 공을 빼는" 수단이 없어졌다**(전에는 뒷발로 공을 뒤로 찼다). 사용자 지시대로지만 대체 수단은 없다.
  - 세션 4에서 남긴 스프라이트 경미 결함 중 **위/아래 발차기가 옆차기로 그려지는 것**과 **충전 링 이중 렌더**는 아직 그대로다(5단계).
  - BGM 은 합성음이라 "게임 음악"이라기보다 배경 루프에 가깝다. 실제 음원을 쓰려면 저작권·용량 문제를 따로 정해야 한다.
- 문서 수정: `CLAUDE.md` §1(연습장 인공지능이 범위 안으로, 온라인은 여전히 봇 없음)·§3(전반 3분·시계 규칙·골키퍼 stealR)·§5.1(소리 키)·
  §5.2(달리기 지침·슛 거리·감아차기·**뒷발차기 절 전면 교체**·골키퍼)·§5.4(격자 30·허리띠·걸음 위상차)·**§5.5 소리 신설**·**§5.6 연습장 인공지능 신설**·
  §6(이벤트 목록에 `steal`)·§7(`debug.ai`·`debug.audio`·practice 설명).
  `ROADMAP.md` 1단계에 5차 개편 조건 8개 추가와 실측값 교체, 3단계 `halfSec` 선택지 150 → 180, **5단계에서 사운드 제외**(1단계에서 끝남) 명시.
- 다음 세션 첫 할 일: ROADMAP 2단계(서버 코어 + 온라인 1:1). 변경 없음.
- 사용자 확인 요청:
  1. **인공지능 2:2 의 점수(3분에 20골 안팎)** — 골키퍼를 세게 할지(`saveR` 30 → 38 정도), 아니면 이대로 난타전으로 둘지.
  2. **소리** — 합성 효과음·BGM 이 쓸 만한지, 아니면 BGM 은 빼는 게 나은지.
  3. **체력 수치**(2초 달리고 10초 회복, 지치면 158) 가 너무 각박하지 않은지.
- 커밋: 없음(사용자 지시 대기).

## 세션 5 — 2026-08-25 — 단계 1 4차 개편(사용자 지시 3건: 몰고 들어가는 득점·왼쪽 Shift 달리기·발차기 지연/기절 2초) — 완료
- 한 일:
  - **몰고 들어가는 득점 허용** — 세션 4에서 넣은 `clampCarried` 를 뒤집었다. 이제 들고 있는 공도 **골대 입구 폭 안에서는** 골라인을 넘는다.
    입구 밖에서는 그대로 경기장 안에 잡아 둔다. 드리블 킥(W)으로 밀어 넣는 것은 원래 되던 것이라 그대로다.
  - 그대로 두면 세션 4에서 확인한 "걸어 들어가면 98%" 가 돌아오므로, **골키퍼가 몰고 오는 공도 걷어내게** 했다(`keeperSave`).
    **공격당하는 쪽 골키퍼만** 뺏는다 — 자기 진영 골키퍼까지 뺏으면 수비가 공을 몰고 나올 수 없다. 뺏긴 선수는 0.25초 다시 못 잡는다.
    이것이 몰고 들어가는 득점의 유일한 방어 수단이다.
  - **왼쪽 Shift 달리기 + 체력** — 새 입력 비트 7(`BTN.SPRINT`), `PROTOCOL_VERSION` 3 → 4.
    누르고 **실제로 움직이는 동안만** 체력이 줄고(2/틱), 아니면 회복한다(1/틱). 0이 되면 그대로 걷는 속도로 돌아가고,
    30%(36)까지 회복해야 다시 달릴 수 있다(1 남은 체력으로 깜빡이는 것 방지). 공을 몰 때는 배율이 낮다(1.5 → 1.25) —
    "몰고 들어가는 득점"이 달리기로 더 쉬워지지 않게. 달리기와 필살기 속도 효과는 **곱하지 않고 큰 쪽만** 쓴다.
    스냅샷은 선수 레코드 13B 를 지키려고 `ult` 바이트를 **하위 4비트 게이지 + 상위 4비트 체력**으로 쪼갰다(20명 289B 유지).
    클라이언트: `KEY.sprint = ShiftLeft`, 터치 `달리기` 버튼(필살 옆), HUD 체력 막대(30% 이하 빨강), `debug.stats().stam`.
  - **발차기 지연 단축 + 기절 2초** — 판정이 **누른 그 틱**에 일어나고(`hitTick` 4 → 5 = animTicks), 시전자 정지 3틱 → 2틱,
    쿨다운 16/20 → 12/16틱. 기절 12틱(0.6초) → **40틱(2초)**.
  - **기절 면역을 8 → 50틱(2.5초)으로 올렸다.** 기절 2초 > 쿨다운 0.6초라 세션 3에서 세운 "기절 < 쿨다운" 부등식이 성립할 수 없어졌고,
    영구 잠금을 막는 장치가 면역 하나만 남았기 때문이다. 이 값을 줄이면 한 명이 상대를 반영구적으로 눕힐 수 있다.
  - 검증기 `tools/headless-probe.js` 의 `reset()` 이 앞 검사의 발차기 쿨다운을 지우지 않아 **뒷발차기 검사가 계속 0으로 나오던 것**을 고쳤다
    (내 변경 때문이 아니라 원래 있던 검사 결함이다 — 쿨다운 0.8초 > 검사 간격 0.36초). Shift 달리기 검사도 추가했다.
- 실측값 (변경 전 코드와 **같은 검증 스크립트**로 A/B. 108조건 = 시작 y 9 × 골키퍼 위상 12):
  | 항목 | 변경 전 | 변경 후 |
  |---|---|---|
  | 몰고 걸어 들어가기 | 0/108 (0%) | **62/108 (57.4%)**, 평균 16틱 |
  | 몰고 달려 들어가기 | 0/108 | 66/108 (61.1%), 평균 13틱 |
  | 골문 앞 200 충전 슛 | 70/108 (64.8%) | 70/108 (64.8%) — 변화 없음 |
  | 드리블 킥(W)으로 밀어 넣기 | 106/108 (98.1%) | 106/108 (98.1%) — 변화 없음 |
  | 골대 입구 **밖**에서 몰고 나가기 | 0/36 | 0/36 |
  | 발차기 연타 시 피해자 자유 틱 | 121/200 (60.5%) | 112/200 (56%) |
  | 발차기 판정 시점 | 누른 다음 틱 | **누른 그 틱** |
  | 시전자 정지 | 3틱(0.15초) | 2틱(0.10초) |
  | 5초 홀드 발차기 횟수 | 7회 | 9회 |
  | 유령골 | 0/178 | 0/178 |
  | 스냅샷(20명) | 289B | 289B |
  - 달리기: 걷기 220 / 달리기 330 / 공 몰고 달리기 275 u/s. 60틱(3초)에 체력 0 → 그 뒤 눌러도 220. 36틱(1.8초)에 재개, 120틱(6초)에 만충.
  - 결정성: seed1 600틱 **270320390**, 900틱 **2973448050** — Node = `http://` 브라우저 = `file://` 전부 동일.
  - 브라우저 실측(연습장, 입력 주입): 몰고 들어가기 18/30 골 · 12/30 골키퍼 차단, 드리블 킥 12/12 골.
  - 진짜 키보드(헤드리스 크롬 CDP): 0.6초 걷기 132 vs Shift 달리기 179, 체력 120 → 80, 드리블 1·슛 750·감아 spin −0.048·뒷발 1·필살 1.
  - fps 60.0 ×5, 스냅샷 20Hz, 틱 0.12~0.16ms 평균 / 1.8ms 최대. `file://` `errors: []`, `selfTest 270320390`, 렌더 0.38ms/프레임, `store: true`. http 콘솔 0건.
  - 스크린샷: 스크래치패드 `soccer-pc.png`(PC 1264×671), `soccer-phone.png`(812×375 터치 — 달리기 버튼이 필살 옆).
- 미달/알려진 문제:
  - **자책골이 가능해졌다.** 자기 골대를 바라보고 공을 몰고 걸어 들어가면 상대 득점이 된다(변경 전에는 불가능). 이동이 곧 바라보는 방향이라
    "실수로" 되기는 어렵고 의도해야 가능하지만, 규칙상 열려 있다. 막으려면 "자기 진영 골라인은 소유 공이 못 넘는다"를 넣으면 되는데,
    그러면 "몰고 들어가는 득점" 규칙이 좌우로 비대칭이 된다. 사용자 판단이 필요해 그대로 뒀다.
  - **골문 바로 앞(x 1100)에서는 사실상 다 들어간다**(98%). 골키퍼가 공의 y 를 훑는 시간이 없기 때문이다. 거리 200에서 57%,
    골문 앞에서 98% 라는 뜻이라, "골문 앞까지 공을 몰고 가는 것" 자체가 난이도가 된다. 수비수의 발차기(기절 2초)가 그 역할을 한다.
  - **필살기 기절(광역 1.2초·돌진 0.8초)이 일반 발차기 기절(2초)보다 짧아졌다.** 필살기의 값은 "여러 명 동시"·"무적 이동"이라 그대로 뒀지만
    체감이 이상하면 올려야 한다 — 사용자 확인 항목.
  - 기절 면역 2.5초는 **다른 사람의 발차기도 막는다**. 20명 난전에서 여럿이 한 명을 계속 눕히는 것을 막아 주지만,
    "방금 맞은 사람은 잠시 무적"이라 수비 입장에서 답답할 수 있다.
  - 세션 4에서 남긴 경미 23건(스프라이트 그림 다듬기)은 그대로 5단계로 미룬다.
- 문서 수정: `CLAUDE.md` §3(골대 규칙 뒤집음)·§5.1(달리기 행·왼쪽 Shift 만 받는 이유)·§5.2(달리기 규칙 신설, 발차기 기절·지연·쿨다운,
  부등식 폐기와 면역 규칙, 골키퍼가 소유 공을 뺏는 규칙)·§5.3(필살기 기절이 더 짧아진 점)·§6(입력 비트 7, `ult` 바이트 4+4 분할)·§7(`stats().stam`).
  `ROADMAP.md` 1단계: "몰고 들어가 득점 불가" → "가능"으로 **뒤집고**(낮춘 것이 아니라 사용자 지시로 규칙이 바뀐 것) 실측값 교체, 달리기·발차기 지연 조건 추가,
  사용자 플레이 확인 항목 3개 추가.
- 다음 세션 첫 할 일: ROADMAP 2단계(서버 코어 + 온라인 1:1). 변경 없음. Node 는 이 PC에 설치돼 있다(v24.19.0 — 세션 0의 "없음"은 해소됨).
- 사용자 확인 요청:
  1. **몰고 들어가는 득점 난이도** — 거리 200에서 57%, 골문 앞에서는 거의 100%. 더 어렵게 하려면 골키퍼 판정 반지름을 키운다.
  2. **자책골을 막을지** — 지금은 자기 골대로 몰고 들어가면 자책골이 된다.
  3. **달리기 수치**(3초 달리고 6초 회복, 1.5배)와 **기절 2초**가 실제로 해 보니 어떤지.
- 커밋: 없음(사용자 지시 대기).

## 세션 4 — 2026-08-24 — 단계 1 적대적 검증과 수정 — 완료
- 한 일:
  - 3차 개편을 **3갈래 병렬 검증**(규칙 악용 / 문서-코드 일치 / 스프라이트)에 걸고, 보고된 치명·중대는 **다른 에이전트가 독립 재현**해 오탐을 걸렀다.
    41건 보고 → 확정 7건(치명 1, 중대 6), 오탐 기각 3건, 경미 27건.
  - **치명 1건 수정 — 공을 몰고 골망으로 걸어 들어가면 98% 득점**: 선수 x 상한 1186 + 공이 발 앞 18 → 공 x 1204 로 골라인(1200)을 넘었고,
    `keeperSave` 는 소유 중인 공을 아예 건드리지 않아 골키퍼 방어율이 **0%** 였다. 쏘는 것(63%)보다 걸어 들어가는 게 잘 들어가서 슛·감아차기·필살기가 전부 무의미했다.
    → `clampCarried()` 신설: **들고 있는 공은 항상 경기장 안**. 득점하려면 반드시 차야 한다.
  - **중대 6건 수정**:
    1. 유령골 — 골라인 통과 **시점**의 y 가 아니라 이동 후 y 로 입구를 판정해 기둥 밖 19단위를 지난 공이 골이 됐다. `clampBall` 에 선형 보간 추가.
    2. 순간이동으로 골망 진입 — 치명 1과 뿌리가 같아 `clampCarried` 로 함께 해결.
    3. 필살기 효과가 득점·하프 리셋을 넘겨 상대 킥오프까지 남았다 → `placePlayer` 에서 `ultT/ultAnimT/ultX/ultY` 초기화.
    4. 돌진 vs 돌진이 서로 기절 — 면역 검사가 `kickHits` 에만 있었다 → `stunPlayer` 한 곳으로 모음.
    5·6. 문서 누락 — 캐릭터 `kickRange`(발차기 반지름 0.9~1.4배)와 감아차기 속도의 `shot` 배율.
  - **경미 중 실질적인 것 4건도 수정**: 대포알이 자기 진영 골키퍼까지 뚫어 자책골이 되던 것, `dx=Infinity` 하나로 좌표·해시가 영구 NaN 이 되던 것,
    발차기 홀드가 무효라 연타와 성능이 2배 차이 나던 것, 골망 진입 첫 틱에 공이 뒷그물 밖(x 1243)까지 나갔다 되돌아오던 것.
  - 문서 정리: `step` 시그니처의 없는 `dt` 인자, `skill` → 실제 이벤트 이름들, `debug.events` 상한 500 → 800, `debug.touch/readInput` 누락,
    존재하지 않는 상수(`SNAPSHOT_EVERY`·`INPUT_HZ`)에 "2단계 도입 예정" 표기, 포트 표기 불일치, 루트 문서가 아직 없는 `soccer/server/` 를 실행하라고 안내하던 것, `sprites.js` 누락.
  - 이전 세션에서 중단된 사이 **카메라 규칙을 "세로 고정" → "보이는 넓이 고정"** 으로 교체(세로 폰에서 가로 160단위만 보이던 문제). 12종 화면에서 검증.
- 실측값(수정 후):
  - 몰고 걸어 들어가기 **0/108**(고치기 전 98%). 골문 앞 200에서 충전 슛 **52%** 득점 — 슛이 정상적인 득점 수단이 됐다.
  - 유령골 **0/775**(고치기 전 39/1428). 골키퍼 방어율은 그대로 근거리 37% / 원거리 44% / 약한 슛 50%.
  - 필살기 리셋 ultT 100 → 0. 돌진 vs 돌진 기절 0/0. 대포알: 상대 골대 `goal`, 자기 골대 `keeperSave`.
  - 발차기 5초 홀드 7회(쿨다운 16틱과 일치). `dx=Infinity` 20틱 후 좌표·facing·해시 전부 정상.
  - 결정성: seed1 600틱 **2214918852** (Node = `file://` 동일), 900틱 519599406. 스냅샷 20명 289B. `file://` errors [].
  - 카메라: PC 640×360 / 폰 가로 650×300 / 폰 세로 300×650 / 21:9 742×311 / 32:9 880×248. 클램프 3화면×7지점 전부 통과.
- 미달/알려진 문제:
  - **오탐으로 기각한 3건**(고치지 않음): 캐릭터 득점 속도 격차 38% — 슛을 금지하고 도보 경주만 잰 인공물이고 슛을 허용하면 순위가 뒤집힌다.
    시야 문서 불일치 2건 — 재확인 결과 문서와 코드가 이미 일치.
  - **남은 경미 23건**(대부분 스프라이트 그림 다듬기, 5단계로 미룸): 발차기 발이 24×26 격자 밖으로 잘림(7종 전부),
    위/아래 발차기가 옆차기로 그려짐, 등번호가 2종에만 나옴, 20명이 완전히 같은 걸음으로 뜀(달리기 프레임이 절대시각 기준),
    충전 링이 두 번 그려짐(스프라이트와 `index.html` 양쪽), 카메라 배율이 0.14% 변하면 정수 배율 계단 때문에 선수 크기가 33% 튐(창 크기 변경 시에만).
  - 동거리 공 다툼은 항상 슬롯 번호가 낮은 쪽이 이긴다 — 결정성을 위해 필요한 규칙이라 그대로 둔다(실전에서 정확히 같은 거리는 거의 없다).
  - 검증 산출물은 임시 폴더 `%LOCALAPPDATA%\Temp\claude\C--Claude\soccer-verify\`·`soccer-verify2\` 에 있다(저장소 밖).
- 문서 수정: `CLAUDE.md` §3(골대 득점 규칙·없는 상수 표기)·§4(step 시그니처·이벤트 이름·버퍼 규칙)·§5.2(발차기 배율·홀드·감아차기 배율)·§5.3(발차기 열 추가·리셋 규칙)·§5.4(캐시 키)·§7(debug 목록)·§8(포트).
  ROADMAP 1단계에 수정 후 실측값과 적대적 검증 통과 항목 추가. 루트 `CLAUDE.md` 의 soccer 항목(아직 없는 `server/`, `sprites.js`).
- 다음 세션 첫 할 일: ROADMAP 2단계(서버 코어 + 온라인 1:1). 변경 없음.
- 사용자 확인 요청:
  1. **직접 플레이 체감** — 좁아진 시야, 감아차기 휨, 발차기 손맛, 골키퍼 난이도, 도트 캐릭터. (이제 걸어 들어가서는 득점이 안 된다)
  2. **태클(C) 삭제** 동의 여부.
  3. git 정리·커밋 허락(미해결).
- 커밋: 없음(사용자 지시 대기).

## 세션 3 — 2026-08-24 — 단계 1 재개편(사용자 지시 8건: 조작 체계·감아차기·발차기·골키퍼·시야·2D 캐릭터·필살기) — 완료
- 한 일:
  - **조작 전면 개편**: 이동 방향키 · W 드리블 · Q/E 감아차기 · D 슛/발차기 · S 뒷발차기 · A 필살기 · Z 패스.
    **C 태클은 없앴다** — 사용자의 새 동작 목록에 없고, D 발차기가 같은 역할(상대 기절)을 하므로 두 메커닉이 겹쳤다. 되살리려면 말씀만 하면 된다.
  - **감아차기**: 등속을 유지한 채 매 틱 속도 벡터를 회전(`ball.spin`). 충전 0~0.6초 → 0.008~0.048 rad/틱, 총 회전 1.9rad 상한(`turnMax`).
  - **발차기/뒷발차기**: 부채꼴 판정으로 상대 기절 + 넉백. 뒷발차기는 공을 가진 채로도 발동(공은 뒤로). 시전자 0.15초 정지.
  - **골키퍼 재설계**: 공 추적을 버리고 `state.tick` 만의 사인 함수로 주기 왕복(주기 3초·진폭 86). 잡지 않고 튕겨낸다. 스윕 판정으로 강슛 터널링 차단.
  - **필살기(A)**: 게이지 45초 만충. 캐릭터 6종 각각 다른 효과(광역 기절 / 대포알 1발 / 순간이동 / 무적 돌진 / 질주 / 감속 장판).
  - **RPG 카메라**: 보이는 월드 넓이를 고정(PC 세로 360·터치 300, 가로 최대 760)해 화면 크기와 무관하게 공평. 데드존 + 지수 추종 + 경기장 클램프.
    좁은 시야 보완으로 **미니맵**(M 토글)과 **화면 밖 공 화살표**를 넣었다 — 없으면 공을 찾을 수 없어 플레이가 불가능했다.
  - **2D 캐릭터**: 화풍 후보 3종(도트 3/4 · 둥근 치비 · 레트로 탑뷰)을 병렬로 만들어 미리보기 PNG 를 직접 보고 **도트 3/4 시점**을 채택 → `sprites.js`.
    치비는 확대하면 예쁘지만 게임 크기(r 약 30)에서 8방향 구분이 안 됐고, 레트로 탑뷰는 얼굴이 정수리로 보여 캐릭터성이 약했다.
  - `PROTOCOL_VERSION` 2 → 3. 선수 레코드 13B 유지(cd1/cd2 → ult 게이지 + motion/충전).
- 실측값:
  - 결정성: seed1 600틱 = **221387816** (Node = 브라우저 = `file://` 동일), 900틱 = 3468330750.
  - 감아차기: 충전 1/12 → 27도 휨, 12/12 → 109도(상한). 실제 경기 궤적 `342,399 → 608,299 → 776,70 → 869,209`.
  - 발차기: `kick,hit` 상대 기절 12틱, 시전자 이동량 0,0,0 뒤 재개(정확히 3틱 = 0.15초). 뒷발차기 `backkick,hit` + 공 뒤로.
  - 골키퍼 방어율: 근거리 강슛(750, 376 거리) 37% / 원거리(876) 44% / 약한 슛(400) 50%. 최고 속도 180u/s.
  - 필살기: 6종 전부 `ult` 이벤트 + 효과 확인. 대포알은 슛 1발 뒤 게이지 0(속도 1453·관통), 감속 장판은 상대 1초 이동 121(정상 220).
  - 카메라: PC 1366×768 에서 `view 678×360 @1.86`, 폰 812×375 에서 `view 760×300`. 그리는 선수 수는 `stats().drawn`.
  - 성능: 헤드리스 크롬 fps 60.0 ×5, 스냅샷 20Hz, 틱 0.065~0.11ms 평균, 렌더 0.38ms/프레임. 20명 스냅샷 289B.
  - 콘솔: http 0건, `file://` `errors: []` + `sprite:"도트 3/4 시점"`.
- 미달/알려진 문제:
  - **설계 검토에서 찾아 고친 결함 3개**(병렬 설계 에이전트가 계산으로 지적):
    1. **무한 기절 잠금** — 기절 14틱 > 발차기 쿨다운 10틱이라 한 명이 상대를 영구히 눕힐 수 있었다. 기절 12 / 쿨다운 16 + 깨어난 뒤 면역 8틱으로 수정, 최악 조건 200틱 중 자유 121틱으로 확인.
    2. **눈덩이 게이지** — 득점한 팀에만 게이지를 줘서 이긴 팀이 더 강해졌다. 실점 팀 +8초 / 득점 팀 +2초로 뒤집음.
    3. **에이스 8초 관통 창** — 8초 동안 모든 슛이 관통 강슛이었다. "다음 한 발"만 강화하도록 변경(5초 유효).
  - 그 밖에 고친 것: 감아차기 사거리 900 → 1400(하프라인 밖에서 못 쓰던 문제), 골키퍼 진폭 78 → 86(기둥에 정확히 닿는 값), 주기 3.4 → 3.0초.
  - **드리블 킥이 이동 수단이 아니다**: 공만 320으로 나가고 선수는 220이라 평균 전진 속도는 걷기와 같다. 더 빠르게 하려면 `TUNING.dribble.range` 를 올린다.
  - 감아차기 최대 충전 구간(8~12틱)은 총 회전이 상한에 걸려 휨 각도가 같다(호가 더 급해질 뿐). 의도한 동작이지만 "더 눌러도 같다"고 느낄 수 있다.
  - 필살기 게이지·발차기 판정 각도 등은 전부 추정치다. 9단계 실사용 테스트에서 확정한다.
  - 화풍 후보 3종의 코드와 미리보기 PNG 는 임시 폴더 `%LOCALAPPDATA%\Temp\claude\C--Claude\soccer-work\` 에 남아 있다(저장소 밖).
- 문서 수정: `CLAUDE.md` §1(범위)·§2(파일)·§3(카메라·골키퍼 행)·§4(sprites.js)·§5 전면 재작성(5.1 조작표 / 5.2 규칙 / 5.3 캐릭터·필살기 표 / 5.4 스프라이트 계약)·§6(버튼 비트·스냅샷)·§7(debug API, 7.1 카메라)·§8.
  ROADMAP 1단계 완료 조건을 새 규칙으로 교체(낮춘 것이 아니라 사용자 지시로 바뀐 규칙), 5단계를 "이펙트·사운드·필살기 연출"로 재정의.
- 다음 세션 첫 할 일: ROADMAP 2단계(서버 코어 + 온라인 1:1). 변경 없음.
- 사용자 확인 요청:
  1. **직접 해 보고 체감**: 좁아진 시야가 답답하지 않은지, 감아차기 휨 정도, 발차기 손맛, 골키퍼 난이도, 도트 캐릭터 그림.
     PC `file:///C:/Claude/soccer/index.html` / 폰 `http://192.168.0.2:8765/soccer/`(같은 와이파이, 루트 정적 서버 실행 중일 때).
  2. **태클(C) 삭제** 동의 여부. 되살리길 원하면 한 줄이면 된다.
  3. git 정리·커밋 허락(미해결).
- 커밋: 없음(사용자 지시 대기).

## 세션 2 — 2026-08-24 — 단계 1 보완(사용자 지시: 등속·드리블 킥·자동 골키퍼·모바일) — 완료
- 한 일:
  - 사용자 지시 4건 반영: ① 가속 제거 → 전부 등속(선수: 입력 즉시 220, 떼면 0. 공: 찬 속도로 `range`만큼 가고 정지, 마찰 없음).
    ② 드리블 킥(스페이스/V, 터치 "드리블" 버튼): 짧게 차기 64단위, 꾹 누르면 잡을 때마다 반복. ③ 자동 골키퍼(진영마다 1, 공 y 추적, 잡기→0.5초→걷어내기).
    ④ **모바일 버전** — 같은 `index.html` 에 터치 조이스틱·버튼 6개·세로 화면 안내·safe-area·작은 화면 HUD. 터치 기기 자동 감지, `?touch=1/0` 강제.
  - `PROTOCOL_VERSION` 1 → 2 (입력 비트 5 드리블, 스냅샷 헤더 24B → 29B: 골키퍼 y 2개 + 플래그). 20명 289B.
  - `tools/headless-probe.js`: `PROBE_SIZE`·`PROBE_SKIP_KEYS` 환경변수, 키보드 검사에 스페이스 드리블 추가, 터치 UI 캡처.
- 실측값:
  - 결정성: seed1 = 1842491738 (브라우저 = Node = file://), seed7 = 3545131542.
  - 등속: 첫 틱 220, 해제 0. 드리블 탭: 공 x 300 → 382 에서 정지(2틱). 꾹: 3초에 킥 10회(틱 1,7,13,…), 선수 660 전진(=220u/s).
  - 킥 거리: 드리블 0.2초/64, 정면 패스 1.0초/400, 최대 슛 등속 750(마찰 없음, 골망까지). 키보드: → 0.6초 132단위, 스페이스 1회 킥 → 공 82 앞, X 0.7초 → 750.
  - 골키퍼: 9단위/틱(180u/s). 정면 500 슛(300 거리) 10틱째 잡고 20틱째 걷어냄. 750 코너 슛(y=320) 200 거리에서는 골, 400 거리에서는 잡힘.
    잡기 판정을 공 이동 **뒤**로 옮긴 뒤의 값이다(전엔 한 틱 유리하게 잡았음).
  - 터치(합성 PointerEvent): 조이스틱 (0.71, −0.71), 슛 버튼 buttons=2, 드리블 buttons=32, 떼면 0·조이스틱 숨김. 812×375 캡처 정상.
  - 헤드리스 크롬 fps 60.0 ×5, 스냅샷 20Hz. 콘솔 0(새 탭), file:// errors [].
  - 스크린샷: 스크래치패드 `soccer-stage1b-desktop.png`, `soccer-stage1b-phone.png`.
- 미달/알려진 문제:
  - 고친 버그: 합성 터치 이벤트에서 `setPointerCapture` 가 NotFoundError → try/catch(`capture()`). 실제 손가락에선 안 나던 예외.
  - 골키퍼 강도는 추정치: 가까운 강슛·좌우 흔들기로만 뚫린다. `TUNING.keeper.speed`(180)·`catchRadius`(24)가 조절 손잡이. 9단계 실사용 테스트에서 확정.
  - 드리블 킥 중 공이 선수보다 빠르지만(320 vs 220) 재획득 간격 때문에 평균 전진 속도는 걷기와 같다. "드리블이 더 빠르게" 원하면 `dribble.range`를 늘리거나 재획득 대기(`kickCdTicks`)를 줄인다.
  - 등속이라 `vx/vy` 스냅샷 필드는 이제 보간에 쓰이지 않지만 10단계 예측 대비로 남겨 둔다.
  - 실제 폰 멀티터치(조이스틱 + 버튼 동시)는 합성 이벤트로만 확인. 사용자 폰 확인 필요.
- 문서 수정: `CLAUDE.md` §1(범위: 모바일 브라우저 포함, APK 는 범위 밖), §2(등속 결정), §3(골키퍼 행), §5(조작·등속·드리블 킥·공 이동·골키퍼·터치 배치), §6(비트 5, 헤더 29B), §8(모바일 검증법).
  ROADMAP 1단계 완료 조건 중 마찰·반발(조건 3)·드리블(조건 5)을 사용자 지시에 따른 새 규칙으로 교체하고 골키퍼·터치 조건을 추가. 8단계를 "윈도우·모바일 마감"으로. 루트 `CLAUDE.md` soccer 항목의 "터치 미적용" 예외 삭제.
- 다음 세션 첫 할 일: ROADMAP 2단계(서버 코어 + 온라인 1:1). 변경 없음. 단 온라인 모드는 터치 클라이언트도 같은 경로를 타므로 추가 작업 없음.
- 사용자 확인 요청:
  1. 등속 이동·드리블 킥 간격(0.3초)·골키퍼 난이도 체감. PC: `file:///C:/Claude/soccer/index.html`. 폰: 같은 와이파이에서 `http://<PC IP>:8765/soccer/`(루트 정적 서버 실행 중일 때).
  2. git 정리·커밋 허락(미해결).
- 커밋: 없음(사용자 지시 대기).

## 세션 1 — 2026-08-24 — 단계 1: 경기 코어 + 연습장 — 완료
- 한 일:
  - `game-core.js`(UMD, 순수 시뮬레이션: 이동·드리블·패스·슛 충전·태클·기절·벽/골망·득점·전후반·해시·코덱·selfTest·sim 프로브).
  - `index.html`(캔버스 레터박스 렌더, 방향키+Z/X/C+A/S, 스냅샷 버퍼 보간 렌더러, 연습장 모드, HUD·배너·시작/종료 오버레이, 캐릭터 선택 1~6, `SoccerCore.debug`, `?selftest=1`).
  - `tools/headless-probe.js`(헤드리스 크롬 + DevTools 프로토콜로 fps·키보드·전체 화면 캡처).
  - 사용자가 Node.js LTS 설치(v24.19.0 확인).
- 실측값:
  - 결정성: seed1 600틱 = 2684203638 (브라우저 2회·Node·file:// 전부 동일), seed7 = 3993966857.
  - 공: 초속 600 → 4.3초/1271단위 정지. 벽 반발 0.69. 최대 충전 슛 743(750 − 한 틱 마찰 7). 키보드 X 0.7초 홀드 → 729.
  - 득점: goal → 24틱 → kickoff → 16틱 → play. 골망 안 공 최소 x −34(뒷그물 −40+6), 다시 안 나옴.
  - 드리블 오프셋 (18,0). 태클 hit 후 기절 12틱 정확. 키보드 → 0.6초 이동 109단위(최고속 220 도달 포함).
  - 전후반(halfSec 90): play@40 half@1840 kickoff@1940 play@1980 end@3780 lobby@3980.
  - 렌더: 헤드리스 크롬 fps 60.0 / 16.67ms / 스냅샷 20Hz, 틱 0.13ms 평균·0.9ms 최대, 그리기 0.09~0.24ms/프레임. 보간 중간 프레임 오차 0px.
  - 스냅샷 크기: 2명 50B, 4명 76B, 20명 284B(Node). 입력 6B 왕복 OK.
  - 콘솔: http(새 탭) 0건, file://(헤드리스 엣지 `?selftest=1`) errors [] · store OK.
  - 스크린샷: 스크래치패드 `soccer-stage1-shotB.png`(캔버스), `soccer-stage1-full.png`(HUD 포함, 헤드리스 크롬). 저장소엔 넣지 않음.
- 미달/알려진 문제:
  - **설계도 산수 수정**: 입력 프레임은 5B가 아니라 6B, 선수 레코드는 12B가 아니라 13B → 스냅샷 20명 284B(≤300B 조건은 유지).
  - **Claude Code 인앱 브라우저 탭은 숨김 상태**라 rAF·resize 이벤트가 멈추고 스크린샷이 안 된다. 대응책을 `CLAUDE.md` §8에 적었다.
    `frame()`이 창 크기를 매 프레임 직접 비교해 `resize` 이벤트 누락을 보완하도록 했다.
  - 고친 버그: ① 빠른 공(틱당 37단위)이 골대 밖 벽을 한 틱에 뚫어 "골망 안"으로 오판 → `clampBall(b, bounce, wasInNet)`으로 이동 전 위치 기준 판정.
    ② 득점 후 공이 골망에서 다시 튀어나옴 → 골망 안에서는 골라인 쪽이 벽(반발 0.3). ③ 창 크기 0(백그라운드 탭)이면 scale 음수 → `arc()` 예외 → scale 하한 0.02 + 크기 0이면 렌더 생략.
    ④ 디버그 `run()`이 스냅샷 시각을 미래로 찍어 렌더러가 가장 오래된 스냅샷을 고름 → 과거 시각으로 스탬프.
  - 연습장 허수아비는 `team 1`에 서 있기만 한다(봇 아님). 패스 대상(같은 팀)이 없어 패스는 항상 정면으로 나간다 — 팀원 테스트는 2단계 1:1에서.
  - `soc_settings`는 캐릭터 선택(1~6)할 때만 저장된다. `interpMs` 설정 UI는 8단계.
  - 사용자 플레이 확인(손맛)은 아직 없음.
- 문서 수정: `CLAUDE.md` §3 단계 표에 `goal` 단계(1.2초) 추가, §5 조작에서 WASD 제외(A/S 스킬과 충돌, 8단계 키 재설정으로), §6 바이트 수 정정, §4·§7·§8에 tools·debug API·인앱 브라우저 한계 추가. ROADMAP 1단계 실측값 기록.
- 다음 세션 첫 할 일: ROADMAP 2단계 — `server/server.js`(http 정적 + ws, 방 1개 고정, 20Hz 틱, 입력 수집, 바이너리 스냅샷), `server/package.json`(ws), 클라이언트 온라인 모드(접속·시계 동기·스냅샷 버퍼·보간·`latencyProbe`).
  `npm install`은 `C:\Program Files\nodejs\npm.cmd` 전체 경로로. 검증은 브라우저 탭 2개(인앱 탭은 숨김이라 `debug.run` 불가 → 서버 틱이 돌므로 오히려 온라인 모드는 인앱 탭에서도 스냅샷 수신 검증 가능).
- 사용자 확인 요청:
  1. `http://localhost:8765/soccer/`(루트 정적 서버) 또는 `file:///C:/Claude/soccer/index.html`을 열어 Enter → 방향키로 공을 몰고 X 길게 눌러 슛. 손맛 한 줄(무게감·속도·태클 타이밍).
  2. git 정리·커밋 허락(세션 0과 동일 질문).
- 커밋: 없음(사용자 지시 대기). 변경 파일: `soccer/game-core.js`, `soccer/index.html`, `soccer/tools/headless-probe.js`, `soccer/CLAUDE.md`, `soccer/ROADMAP.md`, `soccer/HANDOFF.md`.

## 세션 0 — 2026-08-24 — 단계 0: 감독 문서 — 완료
- 한 일:
  - `CLAUDE.md`·`ROADMAP.md`·`HANDOFF.md` 작성. 루트 `CLAUDE.md`에 항목과 예외(다중 파일·서버·npm 의존성 1개) 추가. 메모리 기록.
  - 사용자 결정(이 세션에서 확정): **봇 없음**(빈자리는 빈 채로), **윈도우 PC 전용**(터치·APK 없음), **Node.js + ws 서버**,
    **AWS Lightsail 서울 $7/월**, 느린 유저는 접속 시 거부.
  - 제가 정한 해석: "윈도우용" = 크롬/엣지로 실행하는 PC 게임. 실행 파일이 꼭 필요하면 8단계의 엣지 앱 모드 바로가기로 해결.
    다르게 원하면(진짜 .exe) 8단계를 고치면 되고 앞 단계엔 영향 없음.
- 실측값: 해당 없음.
- 미달/알려진 문제:
  - **Node.js가 이 PC에 없다.** 1단계의 결정성 검증(Node에서 `require`)과 2단계부터의 서버 실행에 필요하다. 설치는 사용자가 한다.
  - git: `main`은 첫 커밋, 최신 작업은 `feat/mood-log`. `voxel-world/`와 `soccer/` 감독 문서가 둘 다 커밋되지 않은 상태로 같은 작업 트리에 있다.
  - 지인 테스트용 Cloudflare 퀵 터널(`cloudflared`)도 사용자가 내려받아야 한다(3단계 전후).
- 문서 수정: 최초 작성. 같은 날 추가 — 사용자가 이름을 **"반대항축구"**로 확정(후보 벌떼·막·뻥·난장·스무축구 검토 후 사용자 제안 채택,
  게임·브랜드 충돌 검색 결과 없음). 이에 맞춰 `CLAUDE.md` §1·§5에 세계관(팀 = ○반, 캐릭터 = 반 친구 유형)을 넣고
  ROADMAP 3단계에 "반 번호 선택·○반 득점판" 조건을 추가했다. 상표 등록 여부(키프리스)는 사용자 확인 항목으로 남김.
- 다음 세션 첫 할 일: ROADMAP 1단계 — `game-core.js`와 `index.html` 연습장. Node가 설치돼 있으면 `node -e "require('./game-core')"`로 해시 검증까지, 없으면 브라우저 검증만 하고 Node 항목은 미달로 남긴다.
- 사용자 확인 요청:
  1. **Node.js LTS 설치** (관리자 PowerShell에서 `winget install OpenJS.NodeJS.LTS`, 설치 후 새 터미널에서 `node -v`). 제가 대신 설치하지 않는다.
  2. git 정리 방향: `feat/mood-log`를 `main`에 합친 뒤 `feat/soccer`·`feat/voxel-world`를 따는 것을 권장. 커밋 허락 여부.
  3. ~~가칭 "스무축구"~~ → "반대항축구"로 확정됨. 남은 것: 키프리스(kipris.or.kr) 상표 검색 1회.
- 커밋: 없음(사용자 지시 대기).
