# soccer — 감독 문서 2: 로드맵

**현재 단계: 4 (2·3 대부분 완료, 4 부분 완료)** ← 세션이 끝날 때마다 이 줄을 갱신한다.
2026-08-25 에 사용자가 "여러 명이서 플레이"를 한 번에 요청해 2·3·4단계를 함께 진행했다. 미달 항목은 각 단계에 `[ ]` 로 남아 있다.

읽는 법: 한 단계 = 한 세션이 원칙. `[ ]` 미착수 · `[~]` 진행 중 · `[x]` 완료(날짜, 커밋).
**완료 조건은 전부 충족해야 체크**한다. 조건을 낮춰 통과시키는 것은 금지(`voxel-world/CLAUDE.md` §0.2와 동일 규칙).
`debug.*`는 `SoccerCore.debug`, `/stats`와 `loadtest.js`는 서버(`CLAUDE.md` §7). 수치는 개발 PC(윈도우, 크롬) 기준.

---

## 0. 감독 문서 — [x] 2026-08-24 (커밋 전)
- [x] `CLAUDE.md`·`ROADMAP.md`·`HANDOFF.md` 작성, 루트 `CLAUDE.md`에 항목·예외 추가, 메모리 기록

## 1. 경기 코어 + 연습장(혼자 하기) — [x] 2026-08-24 — 2026-08-25 사용자 지시로 4·5차 개편 후 재검증
   (1차: 등속·드리블 킥·자동 골키퍼·모바일 / 2차: 새 조작 체계·감아차기·발차기·필살기·RPG 카메라·도트 캐릭터 /
    3차: 적대적 검증 수정 / 4차: 몰고 들어가는 득점 허용·왼쪽 Shift 달리기·발차기 지연 단축·기절 2초 /
    5차: 체력 재조정·뒷발차기 개편·슛 거리·감아차기·효과음·BGM·연습장 2:2 인공지능·전후반 3분·캐릭터 그림 /
    6차: 뒷발차기 → 방귀(정사각형)·슛·방귀 정지 1.3배·안 밀림·납작한 종이 캐릭터·드리블 충전 /
    7차: 슛 거리 700·선수 반지름 18·속도 185·방귀 뒤쪽 네모·무조건 기절·정지 단축 /
    8차: 방귀 축소·속도 160·슛 클릭식 고정거리·감아차기 덜 휨 /
    9차: 슛·감아차기는 사람 안 때림·동작 중 회전 고정·슛 1/3·드리블 연장·발차기 범위 확대·속도 +5% /
    10차: 캐릭터 속도 통일·발차기 범위 축소·공이 굴러가며 느려짐·카메라 정중앙 /
    11차: 방귀 면적 −15%·동작 중 완전 정지·넉백 폐지·감속 완만하게 /
    12차: 방귀 그래픽·판정 일치·공 감속을 일정 감속도로·BGM 실제 가청화 /
    **13차: 감아차기 거리 = 슛 거리·방귀 쿨다운 절반·BGM 멜로디와 출력 표시등**)
**목표**: 서버 없이 혼자 공을 차는 연습장이 돌아간다. 시뮬레이션이 결정적이다.
**산출물**: `game-core.js`(TUNING·CHARACTERS·필살기 6종, 등속 이동, 드리블 킥·패스·슛·감아차기·발차기·뒷발차기, 자동 골키퍼, 벽/골망, 득점, 단계 전이, hashState, encode/decode 4종),
`index.html`(RPG 카메라·미니맵, 캔버스 렌더, 키보드 + 터치 7버튼, 보간 렌더러, 연습장 모드, `debug`), `sprites.js`(도트 캐릭터), `tools/headless-probe.js`.
**완료 조건** (실측값은 HANDOFF 세션 1·2. 조건 3·5는 사용자가 물리를 바꾸면서 교체됨 — 낮춘 것이 아니라 다른 규칙)
- [x] `file://`와 루트 정적 서버(`http://localhost:<autoPort>/soccer/`) 둘 다 콘솔 에러·경고 0, 전역은 `SoccerCore` 하나 (빈 iframe 과 window 차집합 = `SoccerCore`)
- [x] 결정성: seed 1 600틱 두 번 동일, **Node `require` = 브라우저 = `file://` 전부 2214918852** (900틱은 519599406)
- [x] 등속: 입력 첫 틱부터 220, 떼면 즉시 0. 공: 드리블 킥 0.2초·64단위 뒤 정지(발 앞 82), 정면 패스 400, 최대 슛 750 등속, 벽 반사 후 남은 거리 60%
- [x] 득점: y=400 → `goal`, y=250 → 반발. 득점 → `goal` 24틱 → `kickoff` 16틱 → `play` (합계 40틱 = 2초)
- [x] **공을 몰고 걸어 들어가 득점 가능**(2026-08-25 사용자 지시로 규칙이 바뀌었다 — 조건을 낮춘 것이 아니라 뒤집은 것):
      108가지 조건(시작 y 9 × 골키퍼 위상 12)에서 **48/108 = 44.4%**(걸어서·달려서 모두). 나머지는 골키퍼가 걷어냄.
      같은 잣대로 잰 충전 슛은 **70/108 = 64.8%** 로 그대로다 — 슛이 여전히 더 확실한 득점 수단이다.
      드리블 킥(W)으로 밀어 넣기는 106/108 = 98.1%(이 값은 개편 전후가 같다 — 원래 되던 것이다)
- [x] **골대 입구 밖에서는 몰고 나가도 득점되지 않는다**: 입구 밖 y 9곳 × 골키퍼 위상 4가지 = 0/36
- [x] **유령골 없음**(기둥 바깥을 지난 공이 골 처리되지 않음): 775회 시도 0건 (고치기 전 1428회 중 39건)
- [x] 소유: 소유 즉시 공 오프셋 (18, 0). 드리블 킥 꾹: 3초에 10회(0.3초 간격)
- [x] **감아차기(Q/E)**: 충전에 따라 회전율 0.008~0.048 rad/틱, 총 회전 1.9rad 상한. 실제 경기 궤적이 눈에 띄게 휨(HANDOFF 좌표)
- [x] **발차기(D)·뒷발차기(S)**: `kick,hit` / `backkick,hit` 이벤트, 상대 기절, 시전자 3틱 정지(x 증가분 0,0,0 뒤 이동), 공 있으면 슛/뒤로 참
- [x] **영구 기절 잠금 없음**: 최악 조건(상대를 계속 앞에 붙이고 발차기 반복) 200틱 중 **자유 112틱(56%)** — 기절 40 · 쿨다운 12 · **깨어난 뒤 면역 50**.
      기절이 2초가 되면서 "기절 < 쿨다운" 부등식은 버렸고, 잠금을 막는 것은 면역 시간 하나다(`CLAUDE.md` §5.2)
- [x] **발차기 지연 단축**: 판정이 누른 그 틱에 일어나고(전 1틱 뒤), 시전자 정지 2틱(전 3틱), 5초 홀드에 9회(전 7회 — 쿨다운 12틱과 일치)
- [x] **달리기(왼쪽 Shift)**: 걷기 220 → 달리기 330 u/s, 공을 몰면 275. 계속 달리면 **40틱(2초)** 에 체력 0 →
      그 뒤에는 눌러도 **158 u/s(걷기 220 보다 느림)**. 떼면 60틱(3초)에 다시 달릴 수 있고 200틱(10초)에 만충.
      터치도 같은 경로(`달리기` 버튼 = `ShiftLeft` 키 코드 주입). HUD 에 체력 막대(30% 이하면 빨강)
- [x] **방귀(S)는 순수 타격기, 판정은 정사각형**: 공을 든 채 써도 공은 그대로 내 것(`ownerAfter = 나`, 공 속도 0),
      맞은 상대가 공을 갖고 있었으면 `fart → hit → steal` 순으로 공이 나에게 넘어온다.
      판정 격자 실측(5단위 간격 2025칸): 맞는 범위가 x·y 모두 **정확히 [−70, +70]**, 모서리 (70,70)·(−70,−70) 명중, (75,0)·(0,−100) 불발 — 원이 아니라 정사각형
- [x] **슛·방귀의 정지가 더 길다**: 발차기 2틱(0.10초) / **슛 3틱 · 방귀 3틱(0.15초)**. 실측 이동량 0,0,0 뒤 재개
- [x] **닿아도 밀리지 않는다**: 두 선수를 10 거리에 겹쳐 두고 5틱 — 이동량 0, 간격 10 그대로(전에는 서로 28까지 밀어냈다)
- [x] **드리블이 충전식**: 1틱 탭 = 공이 앞 28, 최대 충전 = 앞 318(제곱 곡선). 진짜 키보드 실측 `ballAhead` 82 → 34
- [x] **납작한 종이 캐릭터**: `sprite: 납작한 종이 도트`, 발밑에 충돌 반지름 그대로의 원반 + 흰 테두리. 방귀는 판정 정사각형을 화면에 그대로 그린다
- [x] **슛은 충전 없는 클릭식·고정 거리**: 누른 시간과 무관하게 속도 640. 거리는 **368 실측 = 경기장 가로의 31%**(설정 400 = 정확히 1/3).
      2200 → 1200 → 700 → 500 → 400 으로 줄여 왔다. 감아차기는 800
- [x] **슛·감아차기는 사람을 때리지 않는다**(9차): 바로 앞에 상대를 두고 슛 → `shoot,kick` 이벤트만, 상대 기절 0.
      감아차기 → `curve,kick` 만, 기절 0. 발차기(공 없이 D)는 그대로 `kick,hit` + 기절
- [x] **동작 중 회전 고정**(9차): 방귀 직후 계속 방향키를 꺾어도 동작 5틱 동안 facing 0° 유지 → 끝난 틱에 90°
- [x] **발차기 범위**(10차): 48 → 68 로 넓혔다가 **56·±60°** 로 되돌림. 격자 861칸 중 224칸 명중(26%)
- [x] **캐릭터 걷는 속도 통일**(10차): 6종 모두 1초에 **168** 이동(전 0.85~1.15 배율). 속도 차이는 필살기에서만
- [x] **공이 일정 감속도로 멎는다**(12차): 속도가 남은 거리의 **제곱근**에 비례. 최대 슛 20틱 동안 틱당
      **30.8 → 29.5 → 28.3 → … → 8.3 → 6.6 → 3** 으로 0 에 수렴(선형일 때는 끝에서 18.5 인 채로 뚝 끊겼다).
      **총 이동 거리 368 은 그대로**라 드리블(1/6/12틱 → 58/178/382) 등 기존 거리 실측값이 하나도 안 바뀌었다
- [x] **방귀 그래픽이 판정과 같은 자리에**(12차): 동작 중인 선수는 보간을 끄고 스냅샷 값을 그대로 쓴다.
      실측 — 방귀 4틱 동안 서버 좌표 (635.6, 435.6, 45°) 고정, 화면이 쓰는 스냅샷 (635.8, 435.8, 45°). 차이는 스냅샷 ×4 고정소수 양자화(≤0.25단위)뿐
- [x] **BGM 이 실제로 들린다**(12·13차): 마스터 출력 RMS 실측 **최대 0.130 · 평균 0.060 · 최소 0.026**(개선 전 최대 0.027 · 평균 0.004 · 표본 다수 0).
      같은 잣대의 효과음 최고치 발차기 0.114 · 슛 0.200 · 골 0.148 — BGM 이 효과음과 비슷한 크기다.
      **♪ 단추가 출력 세기에 맞춰 빛난다**(실측 `box-shadow: rgba(163,230,53,0.67) 0 0 12px`) — 빛나는데 안 들리면 원인은 페이지 밖
- [x] **감아차기 거리 = 슛 거리**(13차): 실제 이동 경로 슛 **365** · 감아차기 **373**(둘 다 설정 400)
- [x] **방귀 쿨다운 절반**(13차): 최소 간격 16틱 → **8틱(0.4초)**. 4초 동안 10회, 간격이 전부 8틱으로 일정
- [x] **방귀 면적 15% 축소**(11차): 한 변 96 → **88.5**(반변 48 → 44.26 = 48×√0.85). 실측 판정 앞뒤 [−88, 0] · 좌우 ±44
- [x] **동작 중 완전 정지**(11차): 방귀 직후 대각선 입력을 계속 줘도 5틱 동안 **이동 0 · 각도 0**, 동작이 끝난 틱부터 이동 8.4 · 각 45°
- [x] **기절 중 밀리지 않는다**(11차): 발차기에 맞은 뒤 12틱 동안 **이동량 0**(넉백 폐지)
- [x] **카메라가 내 선수를 정중앙에**(10차): 한가운데·중간 지점에서 카메라−선수 차이 **0,0**.
      경기장 왼쪽 끝에서는 x 차이 240, 위쪽 끝에서는 y 차이 140(클램프가 이긴다 — 의도한 예외)
- [x] **드리블 연장**(9차): 누른 틱 1/3/6/9/12 에 공이 선수 앞 **58 / 98 / 178 / 274 / 382**(전 28/44/99/190/318)
- [x] **속도 +5%**(9차): 걷기 160 → **168**
- [x] **감아차기 덜 휨**(8차): 벽에 닿기 전 휨각 충전 1/4/8/12틱에서 42/57/74/91° → **25/44/63/74°**(`spinMax` 0.072 → 0.052, `turnMax` 155° → 115°)
- [x] **방귀 범위 축소**(8차): 4단위 격자 실측 **앞뒤 [−96, 0] · 좌우 ±48**(전 [−140,0]·±70). 넓이로는 절반이 안 된다
- [x] **속도 재하향**(8차): 걷기 185 → **160**, 달리기 277.5 → **240**
- [x] **선수가 커졌다**: 반지름 14 → **18**, 소유 반지름 24 → 28, 앞차기 판정 44 → 48
- [x] **방귀는 뒤쪽 정사각형**: 6단위 격자 실측으로 맞는 범위가 **앞뒤 [−138, 0] · 좌우 [−66, +66]**(설정 뒤로 140·옆 ±70).
      앞쪽 50 불발 · 뒤쪽 150 불발 · 옆 75 불발. 오른쪽 보기와 아래 보기 모두 같은 모양(회전 반영). 클라이언트가 같은 사각형을 그린다
- [x] **판정 안이면 무조건 기절**: 기절 면역 폐지. 방금 깨어난 상대에게 3연속 방귀 — 3번 다 `hit`, 매번 기절 40틱
- [x] **정지 단축**: 발차기 1틱(0.05초) · 슛 2틱(0.10초) · 방귀 2틱(0.10초)
- [x] **감아차기 더 휨**: 벽에 닿기 전 휨각이 충전 1/4/8/12틱에서 29/46/60/72° → **42/57/74/91°**
- [x] **소리**: 효과음 12종(kick·backkick·shoot·curve·pass·dribble·hit·steal·keeperSave·ult·whistle·goal) + 132BPM BGM 을
      WebAudio 로 합성. 진짜 키 입력(신뢰된 제스처) 뒤 `debug.audio().ready = true`, 12종 전부 예외 없이 재생. N/B 로 끄고 켜기
- [x] **혼자 하면 2:2 인공지능**: 슬롯 0(나)·1(우리 편 AI) vs 2·3(상대 AI). `AI.think` 는 `step()` 밖에서만 부르고,
      같은 조건 두 번 = 같은 해시(결정성 유지). 30초에 골·패스·발차기·선방이 모두 발생
- [x] **전·후반 각 3분**: `halfTicks = 3600`, 시계는 `play` 중에만 감소 — 4000틱 돌린 뒤 남은 시간 50초(=세리머니 350틱이 경기 시간에서 빠짐)
- [x] **캐릭터 그림**: 도트 격자 24 → 30 으로 넓혀 발차기 발끝 잘림 해소, 캐릭터 고유색 허리띠 추가, 걸음 위상차(`charId × 0.137`)
- [x] **골키퍼**: `state.tick` 만의 사인 함수로 왕복(주기 3초·진폭 86), 스윕 판정으로 강슛도 막음. 방어율 근거리 37% / 원거리 44% / 약한 슛 50%
- [x] **필살기 6종**: 전부 `ult` 이벤트 + 효과 확인(광역기절 상대 23틱 / 대포알 1발 소모 후 게이지 0·속도 1453·관통 / 순간이동 240 / 돌진 접촉 기절 / 질주 / 감속 장판 안에서 상대 속도 0.6배)
- [x] 전후반: `play@40 half@1840 kickoff@1940 play@1980 end@3780 lobby@3980` (halfSec 90 = 1800틱 정확)
- [x] **RPG 카메라**: 보이는 넓이 고정(PC 640×360, 실측 `view 678×360 @1.86`), 데드존·부드러운 추종·경기장 클램프, 미니맵 + 화면 밖 공 화살표
- [x] **도트 캐릭터**: 화풍 후보 3종 중 채택. 6종 + 골키퍼, 8방향, 6동작. `sprite:도트 3/4 시점` 로드 확인, 파일 없으면 원으로 폴백
- [x] 렌더: 1280×720·900×1000·1366×768·812×375 확인, 헤드리스 크롬 fps 60.0 / 16.67ms, 보간 중간 프레임 오차 0px, 스크린샷(HANDOFF)
- [x] 터치: 812×375 에서 조이스틱·버튼 7개 입력 생성, 떼면 0. 키보드 경로 실측: 방향키 132/0.6초·W 드리블 82·D 슛 750·Q 감아 spin −0.048·A 필살기
- [x] **적대적 검증 통과**(3갈래 병렬 + 독립 재현): 확정 결함 7건 전부 수정 — 아래 실측으로 재확인
      · 필살기 효과가 득점 리셋을 넘기지 않음(ultT 100 → 0) · 돌진 vs 돌진 서로 기절 안 함(0/0)
      · 대포알이 자기 진영 골키퍼는 못 뚫음(자책 방지) · `dx=Infinity` 입력에도 좌표·해시 정상
      · 발차기 홀드 = 연타(5초에 7회, 쿨다운 16틱과 일치)
- [x] `debug.*` 전체 동작(`raw`·`camera`·`minimap`·`fillUlt`·`clearInput`·`touch`·`readInput` 포함)
**확인 방법**: `javascript_tool`로 `debug.*`, Node 스크립트로 해시 비교, `tools/headless-probe.js`(PC·폰 크기), 헤드리스 엣지 `--dump-dom` (`?selftest=1`).
**사용자 플레이 확인**: 등속 이동·드리블 충전 감각·감아차기 휨·발차기 손맛·골키퍼 난이도·필살기 6종·**좁아진 시야가 답답하지 않은지**·폰 터치 감도 ·
몰고 들어가는 득점 난이도 · 달리기 2초·회복 10초·지치면 158 · 기절 2초 · 효과음·BGM ·
**방귀 정사각형 크기(한 변 140)가 적당한지** · **슛·방귀 정지 0.15초가 역공당할 만한지** · **납작한 종이 캐릭터가 판정과 맞게 보이는지** ·
인공지능 2:2 의 세기(3분 한 판에 16~20골). — 미확인

## 2. 서버 코어 + 온라인 1:1 — [x] 2026-08-25 (커밋 전)
**산출물**: `server/server.js`(http 정적 + ws, 방 1개 고정, 틱 루프, 입력 수집, 바이너리 스냅샷), `server/package.json`, 클라이언트 온라인 모드(접속·시계 동기·스냅샷 버퍼·보간). 게이트는 아직 통과 고정.
**완료 조건**
- [x] `npm start` → 서버가 `soccer/` 전체를 `no-cache` 로 서빙. 브라우저 클라이언트 + Node 클라이언트가 **같은 경기**를 봤다(양쪽 스냅샷 동일 틱)
- [x] 스냅샷: 2명 55B · 4명 81B(20명이면 289B — 1단계에서 확인). 브라우저 수신 `snapshotHz` **19.8~20.2**
- [x] 틱 루프: `setInterval(50ms)` 은 실측 **16.3Hz** 밖에 안 나와 **드리프트 보정 루프**로 교체 → **20.1Hz**. `/stats` `tickP95Ms` **0.053~0.178**(<5)
- [~] 시계 동기: **구현하지 않았다.** 스냅샷 도착 로컬 시각으로 보간한다(§6). 설계 변경이며 이유는 §6에 적었다
- [~] `debug.latencyProbe()` 는 서버가 잰 왕복 지연(로컬 **1ms**)을 돌려준다. 렌더 지연 측정으로 바꾸는 것은 선택 10단계로 미룸
- [x] 클라이언트 하나가 나가도 남은 쪽 경기는 계속(상대 종료 뒤에도 `/stats` phase playing, 틱 300 → 739 증가 확인)
- [x] 서버 `console.error` 0(전 검증 동안 로그에 시작 줄 하나뿐), `file://` 자가검사 `errors: []`
**확인 방법**: 브라우저 탭 2개 + `/stats` + `debug.stats`.
**사용자 플레이 확인**: 탭 두 개로 혼자 양쪽 조작해 보기.

> **5단계 변경**: "이펙트·사운드·필살기 연출" 중 **사운드(효과음·BGM)는 1단계에서 끝났다**(2026-08-25 사용자 지시). 5단계에는 화면 이펙트와 필살기 연출만 남는다.

## 3. 로비·방·팀·경기 흐름 — [x] 2026-08-25 (커밋 전, 일부 미달)
**완료 조건**
- [x] 아이디 입력(`store` `soc_nick`), 방 목록 2초 자동 갱신, 방 생성(이름·1:1~10:10·전후반 1.5/3/5분·**비밀번호**), 입장·퇴장
- [x] **비밀번호 방**: 틀린 비밀번호는 `error` 로 막히고 소켓은 유지(loadtest `틀린비번_막힘: true`)
- [ ] 득점판·채팅·종료 화면의 팀 표시가 "3반 2 : 1 7반" 형식 — 반 번호는 서버가 보내지만 **방 만들기 UI 에 아직 없다**
- [x] 팀 선택(정원 찬 팀은 막음), 캐릭터 선택, 준비, **방장만 시작**(loadtest `방장아님_시작막힘: true`).
      시작 조건은 정원·양팀 동수·전원 준비 셋 다 — 미준비 시 `"모두 준비해야 시작할 수 있습니다"`
- [ ] 경기 중 난입 — **미구현**. 지금은 경기 중인 방에 들어갈 수 없다(목록에서 "경기중"으로 잠긴다)
- [~] 하프타임·득점판·시계 HUD 는 1단계 것을 그대로 쓴다. 종료 시 `match.end` 로 방 화면 복귀 + 준비 해제(코드) — 전후반 완주 검증은 미실시
- [ ] 채팅: 방과 경기 중(Enter), 200자·초당 5개 제한, 초과는 조용히 버림
- [x] 방장 퇴장 시 다음 사람에게 승계(코드), 빈 방은 30초 후 삭제(코드) — 스크립트 검증은 미실시
- [x] `loadtest.js` 로 입장→비번→팀→준비→시작→경기까지 재현. 4명 기준 스냅샷 160개·20.1Hz·81B, 채팅 수신 확인
**사용자 플레이 확인**: 방 만들고 지인 1명과 탭으로 붙어 보기(같은 PC).

## 4. 지연 게이트·경기 중 감시·재접속·버전 — [~] 2026-08-25 (게이트·감시·버전 완료, 재접속 미구현)
**완료 조건**
- [x] `hello` → `ping`×8 → 판정. `--delay 200` → **전원 4001**(중앙값 204ms·p90 216ms), 지연 0 → 전원 통과. `/stats.rejected.NET_SLOW` 집계됨
- [ ] 거부 화면: "네트워크 오류 — 지연이 커서 접속할 수 없습니다 (측정 중앙값/상위10%: NNN/NNNms, 기준 100/160ms)" + 다시 시도 버튼
- [x] `--drop 1` → **전원 4001**(`missing: 1`)
- [x] 경기 중 2초마다 핑 → `--delayAfter 200` 으로 **둘 다 퇴장**(`"지연이 커서 퇴장되었습니다 (중앙값 203ms)"`), `/stats.rejected.KICKED` 2
- [ ] 재접속 — **미구현**. 끊기면 안내 화면 + 다시 시도 단추로 처음부터 붙는다
- [x] 버전 불일치 → 4002 + "새 버전이 있습니다. F5로 새로고침하세요"(코드, 자동 검증은 미실시)
- [x] 같은 IP 상한 초과 4006(기본 4, `MAX_PER_IP`; **로컬은 면제**), 프레임 초과 4005, 잘못된 JSON 은 버리고 유지 — 검증 내내 서버 에러 0
- [ ] `config.json` 값을 바꾸면 재시작 없이… 는 범위 밖. 재시작으로 반영되는지만 확인
**사용자 플레이 확인**: 없음(사람 테스트는 9단계).

## 5. 이펙트·사운드·필살기 연출 — [ ]
(캐릭터 6종과 필살기 자체는 1단계에서 이미 구현·검증됐다. 이 단계는 "보이고 들리는" 부분이다.)
**완료 조건**
- [ ] 필살기 6종 전용 이펙트: 호루라기 파동 링 / 대포알 궤적 잔상 / 순간이동 잔상 / 돌진 먼지 / 질주 속도선 / 미끄러운 장판(바닥에 원, 발동 지점 고정)
      · 장판 위치는 스냅샷에 없다. **클라이언트가 `ULT_ON` 이 켜지는 순간의 그 선수 좌표를 기억**해서 그린다(프로토콜 확장 불필요).
- [ ] 발차기·뒷발차기 타격감(히트 스파크·화면 흔들림 소폭), 골키퍼 선방 이펙트, 감아차기 궤적에 회전 방향 표시
- [ ] Web Audio 합성 효과음: 킥·패스·발차기 히트·골·휘슬·필살기·골키퍼 선방·채팅 알림, 볼륨 설정, 첫 입력 전 재생 시도 시 에러 0
- [ ] 결정성 유지: 이펙트는 전부 클라이언트 전용 — 600틱 해시가 이펙트 유무와 무관하게 동일
- [ ] 20명 + 이펙트 동시에 fps ≥ 55 (헤드리스 크롬 실측)
**사용자 플레이 확인**: 타격감과 필살기 연출.

## 6. 부하 테스트·보안·안정성 — [ ]
**완료 조건**
- [ ] `loadtest.js --rooms 5 --players 20 --seconds 300`: 방 5개 × 20명 = 100접속, `/stats` `tickP95Ms < 10`, 각 클라이언트 수신 `snapshotHz ≥ 19.5`, 서버 송신 ≤ 5Mbps
- [ ] 1시간 부하 후 서버 RSS 증가 < 20%(시작 대비), 접속 해제 후 방·슬롯·타이머 누수 0(`/stats.connections === 0`)
- [ ] 퍼즈: 무작위 바이트·초대형 프레임·잘못된 JSON 1만 건에 서버 에러 0, 정상 클라이언트 영향 없음
- [ ] 입력 범위 검사(dx/dy/buttons), 닉네임 길이·제어문자 제거, 채팅 HTML 이스케이프(클라이언트 `textContent`만 사용)
- [ ] 틱 루프가 느려지면(인위적 50ms 지연 주입) 따라잡기 상한 5틱, 이후 드롭하고 로그
**사용자 플레이 확인**: 없음.

## 7. 배포 — **내 PC + Cloudflare 빠른 터널** — [x] 2026-08-25 (커밋 전)
> 유료 호스팅(Lightsail)은 **쓰지 않기로 했다**(2026-08-25 사용자 결정: "내 컴퓨터에서 무료로").
> 방식·실측·한계는 설계도 §9. 나중에 24시간 상시 운영이 필요해지면 그때 다시 볼 것.

**사용자 선행**: `winget install --id Cloudflare.cloudflared` (계정·도메인·로그인 없음)
**완료 조건**
- [x] `start-online.ps1` 하나로 서버(8080) + 터널이 뜨고 `https://<무작위>.trycloudflare.com` 주소가 찍힌다
- [x] 그 주소에서 페이지가 열리고(200, 88,860B) **wss 가 붙는다** — 서버가 잰 왕복 **중앙값 14ms · 상위10% 26ms**(서울 노드 `icn06`)
- [x] 터널 주소로 전 과정 재현: `loadtest.js --url <주소> --players 4` → 입장 4/4 · 비밀번호 차단 · 준비/방장 조건 · 스냅샷 **20.2Hz · 81B**
- [x] 무설치 대안(localhost.run)과 비교 실측: **411ms** 로 게이트 거부 — 실시간 게임에는 못 쓴다는 것을 숫자로 확인
- [ ] 재부팅 후 자동 시작 — **안 한다**(PC 를 켤 때만 여는 방식이라 필요 없다)
- [ ] 고정 주소 — **없다**(빠른 터널은 켤 때마다 주소가 바뀐다). 필요하면 Cloudflare 계정 + 본인 도메인
**사용자 플레이 확인**: 터널 주소를 지인에게 주고 함께 한 판.

## 8. 윈도우·모바일 마감·설정 — [ ]
**완료 조건**
- [ ] `make-shortcut.ps1`: 바탕화면에 "반대항축구" 바로가기(엣지 앱 모드 `--app=http://<서버>/`, 창 1280×800), 크롬 폴백
- [ ] 설정 화면(Esc / 터치 버튼): 키 재설정, `INTERP_MS` 50~150, 볼륨, 닉네임, 터치 UI 강제 on/off·버튼 크기. `store` `soc_settings` 저장·복원
- [ ] 모바일: 세로 화면 안내, safe-area, 홈 화면 추가(manifest + 아이콘), 주소창 숨김 대응(100dvh), 멀티터치(이동 + 버튼 동시) 실기기 확인
- [ ] 도움말 화면(조작·규칙), 접속 상태 표시(RTT·끊김)
- [ ] `README.md`·루트 `CLAUDE.md` 갱신, 알려진 문제 목록 HANDOFF에 확정
**사용자 플레이 확인**: 바로가기로 실행, 폰 홈 화면에서 실행.

## 9. 실사용자 테스트·튜닝 — [ ] (사용자 주도, 2세션 이상)
**절차**: 사용자 + 지인 3~6명, PC·폰 섞어서, 유선·와이파이·LTE 섞어서 10분 경기 3판. 각자 `debug.stats()`의 `rtt/p90`과 체감(5점)을 HANDOFF에 기록.
**완료 조건**
- [ ] 실측 RTT 분포를 바탕으로 게이트 기준(100/160)과 경기 중 기준(150) 확정, `config.json` 반영
- [ ] `INTERP_MS` 기본값 확정(목표 50~100)
- [ ] 캐릭터 밸런스 조정 1회 이상, 변경 전후 수치 기록
- [ ] "느린 유저 거부"가 실제 사례에서 한 번 이상 발생하고 문구가 이해됐는지
**사용자 플레이 확인**: 전부.

## 10. (선택) 내 캐릭터 예측 — [ ]
9단계에서 체감 지연이 문제일 때만. 공유 `step`으로 클라이언트가 내 선수만 예측하고 서버 스냅샷으로 되감기 보정. 완료 조건은 착수 시 작성.
