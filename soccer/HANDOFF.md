# soccer — 감독 문서 3: 인수인계 일지

규칙: **최신 항목이 맨 위.** 세션이 끝날 때마다 항목을 추가하고 이전 항목은 지우지 않는다.
다음 세션은 맨 위 항목과 `ROADMAP.md`의 "현재 단계"만 읽어도 이어갈 수 있어야 한다. 실측값은 숫자로.

항목 서식:
```
## 세션 N — YYYY-MM-DD — 단계 M: 제목 — 결과(완료 / 진행 중 / 막힘)
- 한 일:
- 실측값:
- 미달/알려진 문제:
- 문서 수정:
- 다음 세션 첫 할 일:
- 사용자 확인 요청: (최대 3개)
- 커밋:
```

---

## 세션 4 — 2026-08-24 — 단계 1 적대적 검증과 수정 — 완료
- 한 일:
  - 3차 개편을 **3갈래 병렬 검증**(규칙 악용 / 문서-코드 일치 / 스프라이트)에 걸고, 보고된 치명·중대는 **다른 에이전트가 독립 재현**해 오탐을 걸렀다.
    41건 보고 → 확정 7건(치명 1, 중대 6), 오탐 기각 3건, 경미 27건.
  - **치명 1건 수정 — 공을 몰고 골망으로 걸어 들어가면 98% 득점**: 선수 x 상한 1186 + 공이 발 앞 18 → 공 x 1204 로 골라인(1200)을 넘었고,
    `keeperSave` 는 소유 중인 공을 아예 건드리지 않아 골키퍼 방어율이 **0%** 였다. 쏘는 것(63%)보다 걸어 들어가는 게 잘 들어가서 슛·감아차기·필살기가 전부 무의미했다.
    → `clampCarried()` 신설: **들고 있는 공은 항상 경기장 안**. 득점하려면 반드시 차야 한다.
  - **중대 6건 수정**:
    1. 유령골 — 골라인 통과 **시점**의 y 가 아니라 이동 후 y 로 입구를 판정해 기둥 밖 19단위를 지난 공이 골이 됐다. `clampBall` 에 선형 보간 추가.
    2. 순간이동으로 골망 진입 — 치명 1과 뿌리가 같아 `clampCarried` 로 함께 해결.
    3. 필살기 효과가 득점·하프 리셋을 넘겨 상대 킥오프까지 남았다 → `placePlayer` 에서 `ultT/ultAnimT/ultX/ultY` 초기화.
    4. 돌진 vs 돌진이 서로 기절 — 면역 검사가 `kickHits` 에만 있었다 → `stunPlayer` 한 곳으로 모음.
    5·6. 문서 누락 — 캐릭터 `kickRange`(발차기 반지름 0.9~1.4배)와 감아차기 속도의 `shot` 배율.
  - **경미 중 실질적인 것 4건도 수정**: 대포알이 자기 진영 골키퍼까지 뚫어 자책골이 되던 것, `dx=Infinity` 하나로 좌표·해시가 영구 NaN 이 되던 것,
    발차기 홀드가 무효라 연타와 성능이 2배 차이 나던 것, 골망 진입 첫 틱에 공이 뒷그물 밖(x 1243)까지 나갔다 되돌아오던 것.
  - 문서 정리: `step` 시그니처의 없는 `dt` 인자, `skill` → 실제 이벤트 이름들, `debug.events` 상한 500 → 800, `debug.touch/readInput` 누락,
    존재하지 않는 상수(`SNAPSHOT_EVERY`·`INPUT_HZ`)에 "2단계 도입 예정" 표기, 포트 표기 불일치, 루트 문서가 아직 없는 `soccer/server/` 를 실행하라고 안내하던 것, `sprites.js` 누락.
  - 이전 세션에서 중단된 사이 **카메라 규칙을 "세로 고정" → "보이는 넓이 고정"** 으로 교체(세로 폰에서 가로 160단위만 보이던 문제). 12종 화면에서 검증.
- 실측값(수정 후):
  - 몰고 걸어 들어가기 **0/108**(고치기 전 98%). 골문 앞 200에서 충전 슛 **52%** 득점 — 슛이 정상적인 득점 수단이 됐다.
  - 유령골 **0/775**(고치기 전 39/1428). 골키퍼 방어율은 그대로 근거리 37% / 원거리 44% / 약한 슛 50%.
  - 필살기 리셋 ultT 100 → 0. 돌진 vs 돌진 기절 0/0. 대포알: 상대 골대 `goal`, 자기 골대 `keeperSave`.
  - 발차기 5초 홀드 7회(쿨다운 16틱과 일치). `dx=Infinity` 20틱 후 좌표·facing·해시 전부 정상.
  - 결정성: seed1 600틱 **2214918852** (Node = `file://` 동일), 900틱 519599406. 스냅샷 20명 289B. `file://` errors [].
  - 카메라: PC 640×360 / 폰 가로 650×300 / 폰 세로 300×650 / 21:9 742×311 / 32:9 880×248. 클램프 3화면×7지점 전부 통과.
- 미달/알려진 문제:
  - **오탐으로 기각한 3건**(고치지 않음): 캐릭터 득점 속도 격차 38% — 슛을 금지하고 도보 경주만 잰 인공물이고 슛을 허용하면 순위가 뒤집힌다.
    시야 문서 불일치 2건 — 재확인 결과 문서와 코드가 이미 일치.
  - **남은 경미 23건**(대부분 스프라이트 그림 다듬기, 5단계로 미룸): 발차기 발이 24×26 격자 밖으로 잘림(7종 전부),
    위/아래 발차기가 옆차기로 그려짐, 등번호가 2종에만 나옴, 20명이 완전히 같은 걸음으로 뜀(달리기 프레임이 절대시각 기준),
    충전 링이 두 번 그려짐(스프라이트와 `index.html` 양쪽), 카메라 배율이 0.14% 변하면 정수 배율 계단 때문에 선수 크기가 33% 튐(창 크기 변경 시에만).
  - 동거리 공 다툼은 항상 슬롯 번호가 낮은 쪽이 이긴다 — 결정성을 위해 필요한 규칙이라 그대로 둔다(실전에서 정확히 같은 거리는 거의 없다).
  - 검증 산출물은 임시 폴더 `%LOCALAPPDATA%\Temp\claude\C--Claude\soccer-verify\`·`soccer-verify2\` 에 있다(저장소 밖).
- 문서 수정: `CLAUDE.md` §3(골대 득점 규칙·없는 상수 표기)·§4(step 시그니처·이벤트 이름·버퍼 규칙)·§5.2(발차기 배율·홀드·감아차기 배율)·§5.3(발차기 열 추가·리셋 규칙)·§5.4(캐시 키)·§7(debug 목록)·§8(포트).
  ROADMAP 1단계에 수정 후 실측값과 적대적 검증 통과 항목 추가. 루트 `CLAUDE.md` 의 soccer 항목(아직 없는 `server/`, `sprites.js`).
- 다음 세션 첫 할 일: ROADMAP 2단계(서버 코어 + 온라인 1:1). 변경 없음.
- 사용자 확인 요청:
  1. **직접 플레이 체감** — 좁아진 시야, 감아차기 휨, 발차기 손맛, 골키퍼 난이도, 도트 캐릭터. (이제 걸어 들어가서는 득점이 안 된다)
  2. **태클(C) 삭제** 동의 여부.
  3. git 정리·커밋 허락(미해결).
- 커밋: 없음(사용자 지시 대기).

## 세션 3 — 2026-08-24 — 단계 1 재개편(사용자 지시 8건: 조작 체계·감아차기·발차기·골키퍼·시야·2D 캐릭터·필살기) — 완료
- 한 일:
  - **조작 전면 개편**: 이동 방향키 · W 드리블 · Q/E 감아차기 · D 슛/발차기 · S 뒷발차기 · A 필살기 · Z 패스.
    **C 태클은 없앴다** — 사용자의 새 동작 목록에 없고, D 발차기가 같은 역할(상대 기절)을 하므로 두 메커닉이 겹쳤다. 되살리려면 말씀만 하면 된다.
  - **감아차기**: 등속을 유지한 채 매 틱 속도 벡터를 회전(`ball.spin`). 충전 0~0.6초 → 0.008~0.048 rad/틱, 총 회전 1.9rad 상한(`turnMax`).
  - **발차기/뒷발차기**: 부채꼴 판정으로 상대 기절 + 넉백. 뒷발차기는 공을 가진 채로도 발동(공은 뒤로). 시전자 0.15초 정지.
  - **골키퍼 재설계**: 공 추적을 버리고 `state.tick` 만의 사인 함수로 주기 왕복(주기 3초·진폭 86). 잡지 않고 튕겨낸다. 스윕 판정으로 강슛 터널링 차단.
  - **필살기(A)**: 게이지 45초 만충. 캐릭터 6종 각각 다른 효과(광역 기절 / 대포알 1발 / 순간이동 / 무적 돌진 / 질주 / 감속 장판).
  - **RPG 카메라**: 보이는 월드 넓이를 고정(PC 세로 360·터치 300, 가로 최대 760)해 화면 크기와 무관하게 공평. 데드존 + 지수 추종 + 경기장 클램프.
    좁은 시야 보완으로 **미니맵**(M 토글)과 **화면 밖 공 화살표**를 넣었다 — 없으면 공을 찾을 수 없어 플레이가 불가능했다.
  - **2D 캐릭터**: 화풍 후보 3종(도트 3/4 · 둥근 치비 · 레트로 탑뷰)을 병렬로 만들어 미리보기 PNG 를 직접 보고 **도트 3/4 시점**을 채택 → `sprites.js`.
    치비는 확대하면 예쁘지만 게임 크기(r 약 30)에서 8방향 구분이 안 됐고, 레트로 탑뷰는 얼굴이 정수리로 보여 캐릭터성이 약했다.
  - `PROTOCOL_VERSION` 2 → 3. 선수 레코드 13B 유지(cd1/cd2 → ult 게이지 + motion/충전).
- 실측값:
  - 결정성: seed1 600틱 = **221387816** (Node = 브라우저 = `file://` 동일), 900틱 = 3468330750.
  - 감아차기: 충전 1/12 → 27도 휨, 12/12 → 109도(상한). 실제 경기 궤적 `342,399 → 608,299 → 776,70 → 869,209`.
  - 발차기: `kick,hit` 상대 기절 12틱, 시전자 이동량 0,0,0 뒤 재개(정확히 3틱 = 0.15초). 뒷발차기 `backkick,hit` + 공 뒤로.
  - 골키퍼 방어율: 근거리 강슛(750, 376 거리) 37% / 원거리(876) 44% / 약한 슛(400) 50%. 최고 속도 180u/s.
  - 필살기: 6종 전부 `ult` 이벤트 + 효과 확인. 대포알은 슛 1발 뒤 게이지 0(속도 1453·관통), 감속 장판은 상대 1초 이동 121(정상 220).
  - 카메라: PC 1366×768 에서 `view 678×360 @1.86`, 폰 812×375 에서 `view 760×300`. 그리는 선수 수는 `stats().drawn`.
  - 성능: 헤드리스 크롬 fps 60.0 ×5, 스냅샷 20Hz, 틱 0.065~0.11ms 평균, 렌더 0.38ms/프레임. 20명 스냅샷 289B.
  - 콘솔: http 0건, `file://` `errors: []` + `sprite:"도트 3/4 시점"`.
- 미달/알려진 문제:
  - **설계 검토에서 찾아 고친 결함 3개**(병렬 설계 에이전트가 계산으로 지적):
    1. **무한 기절 잠금** — 기절 14틱 > 발차기 쿨다운 10틱이라 한 명이 상대를 영구히 눕힐 수 있었다. 기절 12 / 쿨다운 16 + 깨어난 뒤 면역 8틱으로 수정, 최악 조건 200틱 중 자유 121틱으로 확인.
    2. **눈덩이 게이지** — 득점한 팀에만 게이지를 줘서 이긴 팀이 더 강해졌다. 실점 팀 +8초 / 득점 팀 +2초로 뒤집음.
    3. **에이스 8초 관통 창** — 8초 동안 모든 슛이 관통 강슛이었다. "다음 한 발"만 강화하도록 변경(5초 유효).
  - 그 밖에 고친 것: 감아차기 사거리 900 → 1400(하프라인 밖에서 못 쓰던 문제), 골키퍼 진폭 78 → 86(기둥에 정확히 닿는 값), 주기 3.4 → 3.0초.
  - **드리블 킥이 이동 수단이 아니다**: 공만 320으로 나가고 선수는 220이라 평균 전진 속도는 걷기와 같다. 더 빠르게 하려면 `TUNING.dribble.range` 를 올린다.
  - 감아차기 최대 충전 구간(8~12틱)은 총 회전이 상한에 걸려 휨 각도가 같다(호가 더 급해질 뿐). 의도한 동작이지만 "더 눌러도 같다"고 느낄 수 있다.
  - 필살기 게이지·발차기 판정 각도 등은 전부 추정치다. 9단계 실사용 테스트에서 확정한다.
  - 화풍 후보 3종의 코드와 미리보기 PNG 는 임시 폴더 `%LOCALAPPDATA%\Temp\claude\C--Claude\soccer-work\` 에 남아 있다(저장소 밖).
- 문서 수정: `CLAUDE.md` §1(범위)·§2(파일)·§3(카메라·골키퍼 행)·§4(sprites.js)·§5 전면 재작성(5.1 조작표 / 5.2 규칙 / 5.3 캐릭터·필살기 표 / 5.4 스프라이트 계약)·§6(버튼 비트·스냅샷)·§7(debug API, 7.1 카메라)·§8.
  ROADMAP 1단계 완료 조건을 새 규칙으로 교체(낮춘 것이 아니라 사용자 지시로 바뀐 규칙), 5단계를 "이펙트·사운드·필살기 연출"로 재정의.
- 다음 세션 첫 할 일: ROADMAP 2단계(서버 코어 + 온라인 1:1). 변경 없음.
- 사용자 확인 요청:
  1. **직접 해 보고 체감**: 좁아진 시야가 답답하지 않은지, 감아차기 휨 정도, 발차기 손맛, 골키퍼 난이도, 도트 캐릭터 그림.
     PC `file:///C:/Claude/soccer/index.html` / 폰 `http://192.168.0.2:8765/soccer/`(같은 와이파이, 루트 정적 서버 실행 중일 때).
  2. **태클(C) 삭제** 동의 여부. 되살리길 원하면 한 줄이면 된다.
  3. git 정리·커밋 허락(미해결).
- 커밋: 없음(사용자 지시 대기).

## 세션 2 — 2026-08-24 — 단계 1 보완(사용자 지시: 등속·드리블 킥·자동 골키퍼·모바일) — 완료
- 한 일:
  - 사용자 지시 4건 반영: ① 가속 제거 → 전부 등속(선수: 입력 즉시 220, 떼면 0. 공: 찬 속도로 `range`만큼 가고 정지, 마찰 없음).
    ② 드리블 킥(스페이스/V, 터치 "드리블" 버튼): 짧게 차기 64단위, 꾹 누르면 잡을 때마다 반복. ③ 자동 골키퍼(진영마다 1, 공 y 추적, 잡기→0.5초→걷어내기).
    ④ **모바일 버전** — 같은 `index.html` 에 터치 조이스틱·버튼 6개·세로 화면 안내·safe-area·작은 화면 HUD. 터치 기기 자동 감지, `?touch=1/0` 강제.
  - `PROTOCOL_VERSION` 1 → 2 (입력 비트 5 드리블, 스냅샷 헤더 24B → 29B: 골키퍼 y 2개 + 플래그). 20명 289B.
  - `tools/headless-probe.js`: `PROBE_SIZE`·`PROBE_SKIP_KEYS` 환경변수, 키보드 검사에 스페이스 드리블 추가, 터치 UI 캡처.
- 실측값:
  - 결정성: seed1 = 1842491738 (브라우저 = Node = file://), seed7 = 3545131542.
  - 등속: 첫 틱 220, 해제 0. 드리블 탭: 공 x 300 → 382 에서 정지(2틱). 꾹: 3초에 킥 10회(틱 1,7,13,…), 선수 660 전진(=220u/s).
  - 킥 거리: 드리블 0.2초/64, 정면 패스 1.0초/400, 최대 슛 등속 750(마찰 없음, 골망까지). 키보드: → 0.6초 132단위, 스페이스 1회 킥 → 공 82 앞, X 0.7초 → 750.
  - 골키퍼: 9단위/틱(180u/s). 정면 500 슛(300 거리) 10틱째 잡고 20틱째 걷어냄. 750 코너 슛(y=320) 200 거리에서는 골, 400 거리에서는 잡힘.
    잡기 판정을 공 이동 **뒤**로 옮긴 뒤의 값이다(전엔 한 틱 유리하게 잡았음).
  - 터치(합성 PointerEvent): 조이스틱 (0.71, −0.71), 슛 버튼 buttons=2, 드리블 buttons=32, 떼면 0·조이스틱 숨김. 812×375 캡처 정상.
  - 헤드리스 크롬 fps 60.0 ×5, 스냅샷 20Hz. 콘솔 0(새 탭), file:// errors [].
  - 스크린샷: 스크래치패드 `soccer-stage1b-desktop.png`, `soccer-stage1b-phone.png`.
- 미달/알려진 문제:
  - 고친 버그: 합성 터치 이벤트에서 `setPointerCapture` 가 NotFoundError → try/catch(`capture()`). 실제 손가락에선 안 나던 예외.
  - 골키퍼 강도는 추정치: 가까운 강슛·좌우 흔들기로만 뚫린다. `TUNING.keeper.speed`(180)·`catchRadius`(24)가 조절 손잡이. 9단계 실사용 테스트에서 확정.
  - 드리블 킥 중 공이 선수보다 빠르지만(320 vs 220) 재획득 간격 때문에 평균 전진 속도는 걷기와 같다. "드리블이 더 빠르게" 원하면 `dribble.range`를 늘리거나 재획득 대기(`kickCdTicks`)를 줄인다.
  - 등속이라 `vx/vy` 스냅샷 필드는 이제 보간에 쓰이지 않지만 10단계 예측 대비로 남겨 둔다.
  - 실제 폰 멀티터치(조이스틱 + 버튼 동시)는 합성 이벤트로만 확인. 사용자 폰 확인 필요.
- 문서 수정: `CLAUDE.md` §1(범위: 모바일 브라우저 포함, APK 는 범위 밖), §2(등속 결정), §3(골키퍼 행), §5(조작·등속·드리블 킥·공 이동·골키퍼·터치 배치), §6(비트 5, 헤더 29B), §8(모바일 검증법).
  ROADMAP 1단계 완료 조건 중 마찰·반발(조건 3)·드리블(조건 5)을 사용자 지시에 따른 새 규칙으로 교체하고 골키퍼·터치 조건을 추가. 8단계를 "윈도우·모바일 마감"으로. 루트 `CLAUDE.md` soccer 항목의 "터치 미적용" 예외 삭제.
- 다음 세션 첫 할 일: ROADMAP 2단계(서버 코어 + 온라인 1:1). 변경 없음. 단 온라인 모드는 터치 클라이언트도 같은 경로를 타므로 추가 작업 없음.
- 사용자 확인 요청:
  1. 등속 이동·드리블 킥 간격(0.3초)·골키퍼 난이도 체감. PC: `file:///C:/Claude/soccer/index.html`. 폰: 같은 와이파이에서 `http://<PC IP>:8765/soccer/`(루트 정적 서버 실행 중일 때).
  2. git 정리·커밋 허락(미해결).
- 커밋: 없음(사용자 지시 대기).

## 세션 1 — 2026-08-24 — 단계 1: 경기 코어 + 연습장 — 완료
- 한 일:
  - `game-core.js`(UMD, 순수 시뮬레이션: 이동·드리블·패스·슛 충전·태클·기절·벽/골망·득점·전후반·해시·코덱·selfTest·sim 프로브).
  - `index.html`(캔버스 레터박스 렌더, 방향키+Z/X/C+A/S, 스냅샷 버퍼 보간 렌더러, 연습장 모드, HUD·배너·시작/종료 오버레이, 캐릭터 선택 1~6, `SoccerCore.debug`, `?selftest=1`).
  - `tools/headless-probe.js`(헤드리스 크롬 + DevTools 프로토콜로 fps·키보드·전체 화면 캡처).
  - 사용자가 Node.js LTS 설치(v24.19.0 확인).
- 실측값:
  - 결정성: seed1 600틱 = 2684203638 (브라우저 2회·Node·file:// 전부 동일), seed7 = 3993966857.
  - 공: 초속 600 → 4.3초/1271단위 정지. 벽 반발 0.69. 최대 충전 슛 743(750 − 한 틱 마찰 7). 키보드 X 0.7초 홀드 → 729.
  - 득점: goal → 24틱 → kickoff → 16틱 → play. 골망 안 공 최소 x −34(뒷그물 −40+6), 다시 안 나옴.
  - 드리블 오프셋 (18,0). 태클 hit 후 기절 12틱 정확. 키보드 → 0.6초 이동 109단위(최고속 220 도달 포함).
  - 전후반(halfSec 90): play@40 half@1840 kickoff@1940 play@1980 end@3780 lobby@3980.
  - 렌더: 헤드리스 크롬 fps 60.0 / 16.67ms / 스냅샷 20Hz, 틱 0.13ms 평균·0.9ms 최대, 그리기 0.09~0.24ms/프레임. 보간 중간 프레임 오차 0px.
  - 스냅샷 크기: 2명 50B, 4명 76B, 20명 284B(Node). 입력 6B 왕복 OK.
  - 콘솔: http(새 탭) 0건, file://(헤드리스 엣지 `?selftest=1`) errors [] · store OK.
  - 스크린샷: 스크래치패드 `soccer-stage1-shotB.png`(캔버스), `soccer-stage1-full.png`(HUD 포함, 헤드리스 크롬). 저장소엔 넣지 않음.
- 미달/알려진 문제:
  - **설계도 산수 수정**: 입력 프레임은 5B가 아니라 6B, 선수 레코드는 12B가 아니라 13B → 스냅샷 20명 284B(≤300B 조건은 유지).
  - **Claude Code 인앱 브라우저 탭은 숨김 상태**라 rAF·resize 이벤트가 멈추고 스크린샷이 안 된다. 대응책을 `CLAUDE.md` §8에 적었다.
    `frame()`이 창 크기를 매 프레임 직접 비교해 `resize` 이벤트 누락을 보완하도록 했다.
  - 고친 버그: ① 빠른 공(틱당 37단위)이 골대 밖 벽을 한 틱에 뚫어 "골망 안"으로 오판 → `clampBall(b, bounce, wasInNet)`으로 이동 전 위치 기준 판정.
    ② 득점 후 공이 골망에서 다시 튀어나옴 → 골망 안에서는 골라인 쪽이 벽(반발 0.3). ③ 창 크기 0(백그라운드 탭)이면 scale 음수 → `arc()` 예외 → scale 하한 0.02 + 크기 0이면 렌더 생략.
    ④ 디버그 `run()`이 스냅샷 시각을 미래로 찍어 렌더러가 가장 오래된 스냅샷을 고름 → 과거 시각으로 스탬프.
  - 연습장 허수아비는 `team 1`에 서 있기만 한다(봇 아님). 패스 대상(같은 팀)이 없어 패스는 항상 정면으로 나간다 — 팀원 테스트는 2단계 1:1에서.
  - `soc_settings`는 캐릭터 선택(1~6)할 때만 저장된다. `interpMs` 설정 UI는 8단계.
  - 사용자 플레이 확인(손맛)은 아직 없음.
- 문서 수정: `CLAUDE.md` §3 단계 표에 `goal` 단계(1.2초) 추가, §5 조작에서 WASD 제외(A/S 스킬과 충돌, 8단계 키 재설정으로), §6 바이트 수 정정, §4·§7·§8에 tools·debug API·인앱 브라우저 한계 추가. ROADMAP 1단계 실측값 기록.
- 다음 세션 첫 할 일: ROADMAP 2단계 — `server/server.js`(http 정적 + ws, 방 1개 고정, 20Hz 틱, 입력 수집, 바이너리 스냅샷), `server/package.json`(ws), 클라이언트 온라인 모드(접속·시계 동기·스냅샷 버퍼·보간·`latencyProbe`).
  `npm install`은 `C:\Program Files\nodejs\npm.cmd` 전체 경로로. 검증은 브라우저 탭 2개(인앱 탭은 숨김이라 `debug.run` 불가 → 서버 틱이 돌므로 오히려 온라인 모드는 인앱 탭에서도 스냅샷 수신 검증 가능).
- 사용자 확인 요청:
  1. `http://localhost:8765/soccer/`(루트 정적 서버) 또는 `file:///C:/Claude/soccer/index.html`을 열어 Enter → 방향키로 공을 몰고 X 길게 눌러 슛. 손맛 한 줄(무게감·속도·태클 타이밍).
  2. git 정리·커밋 허락(세션 0과 동일 질문).
- 커밋: 없음(사용자 지시 대기). 변경 파일: `soccer/game-core.js`, `soccer/index.html`, `soccer/tools/headless-probe.js`, `soccer/CLAUDE.md`, `soccer/ROADMAP.md`, `soccer/HANDOFF.md`.

## 세션 0 — 2026-08-24 — 단계 0: 감독 문서 — 완료
- 한 일:
  - `CLAUDE.md`·`ROADMAP.md`·`HANDOFF.md` 작성. 루트 `CLAUDE.md`에 항목과 예외(다중 파일·서버·npm 의존성 1개) 추가. 메모리 기록.
  - 사용자 결정(이 세션에서 확정): **봇 없음**(빈자리는 빈 채로), **윈도우 PC 전용**(터치·APK 없음), **Node.js + ws 서버**,
    **AWS Lightsail 서울 $7/월**, 느린 유저는 접속 시 거부.
  - 제가 정한 해석: "윈도우용" = 크롬/엣지로 실행하는 PC 게임. 실행 파일이 꼭 필요하면 8단계의 엣지 앱 모드 바로가기로 해결.
    다르게 원하면(진짜 .exe) 8단계를 고치면 되고 앞 단계엔 영향 없음.
- 실측값: 해당 없음.
- 미달/알려진 문제:
  - **Node.js가 이 PC에 없다.** 1단계의 결정성 검증(Node에서 `require`)과 2단계부터의 서버 실행에 필요하다. 설치는 사용자가 한다.
  - git: `main`은 첫 커밋, 최신 작업은 `feat/mood-log`. `voxel-world/`와 `soccer/` 감독 문서가 둘 다 커밋되지 않은 상태로 같은 작업 트리에 있다.
  - 지인 테스트용 Cloudflare 퀵 터널(`cloudflared`)도 사용자가 내려받아야 한다(3단계 전후).
- 문서 수정: 최초 작성. 같은 날 추가 — 사용자가 이름을 **"반대항축구"**로 확정(후보 벌떼·막·뻥·난장·스무축구 검토 후 사용자 제안 채택,
  게임·브랜드 충돌 검색 결과 없음). 이에 맞춰 `CLAUDE.md` §1·§5에 세계관(팀 = ○반, 캐릭터 = 반 친구 유형)을 넣고
  ROADMAP 3단계에 "반 번호 선택·○반 득점판" 조건을 추가했다. 상표 등록 여부(키프리스)는 사용자 확인 항목으로 남김.
- 다음 세션 첫 할 일: ROADMAP 1단계 — `game-core.js`와 `index.html` 연습장. Node가 설치돼 있으면 `node -e "require('./game-core')"`로 해시 검증까지, 없으면 브라우저 검증만 하고 Node 항목은 미달로 남긴다.
- 사용자 확인 요청:
  1. **Node.js LTS 설치** (관리자 PowerShell에서 `winget install OpenJS.NodeJS.LTS`, 설치 후 새 터미널에서 `node -v`). 제가 대신 설치하지 않는다.
  2. git 정리 방향: `feat/mood-log`를 `main`에 합친 뒤 `feat/soccer`·`feat/voxel-world`를 따는 것을 권장. 커밋 허락 여부.
  3. ~~가칭 "스무축구"~~ → "반대항축구"로 확정됨. 남은 것: 키프리스(kipris.or.kr) 상표 검색 1회.
- 커밋: 없음(사용자 지시 대기).
